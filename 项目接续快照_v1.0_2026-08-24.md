# VideoDownloader (VDL) 项目接续快照 v1.0.1

> 基线存档：2026-08-24 21:00（v1.0 / commit 1a624c3）
> 本轮更新：2026-08-24 22:49（稳定性加固 v1.0.1 / commit 0ba06b9，9 轮巡检 + 7 处加固，已全部上线）
> Git tag：v1.0（1a624c3）+ v1.0.1-stability（0ba06b9）｜ 线上 Railway：hanyuxz.top（git_sha=0ba06b9）

---

## 1. 当前项目原始需求

做一个**跨平台视频下载工具（App + Web 网页版）**，核心能力：
- **下载**：粘贴视频链接 → 解析出真实视频直链/多清晰度 → 下载保存。支持抖音/快手/微博/爱奇艺/腾讯/B站/微视/小红书/红果/1905/网易云MV等国内平台 + YouTube/TikTok 等国际平台（119 个平台清单）。
- **网页版 4 大 tab 固定**：下载、视频处理、图片/PDF去水印、更多功能介绍（不可动；已从 DOM 移除 App 专属 tab：媒体库/视频解说/订阅追更/种子/百度网盘/百度下载）。
- **跨平台 Cookie 共享池**：用户自愿贡献登录态到公共池，其他访客复用（仅登录态，不含密码）。
- **VPS Playwright worker 体系**：国内反爬平台（抖音/快手/微博/爱奇艺/央视频/1905/百视TV/红果/映客/网易CC/斗鱼/微视等）由国内 VPS 真实浏览器解析，Railway 海外节点经反向隧道调用。
- **AI 去水印 / 视频处理 / 解说管线**（桌面版能力，已并入架构）。

---

## 2. 已经完成的全部工作成果

### 2.1 本轮（8/24 下午-晚上）—— 平台实测 + 6 个 bug 修复 + 1.0 存档
- **全平台真实链接实测，10 个平台验证通过**（每个都有真实直链 + GET/HEAD 200 验证）：
  | 平台 | 验证结果 |
  |---|---|
  | 抖音 | 47分钟视频 douyinvod 直链 ✅ |
  | B站 | 1080P 多档清晰度 ✅ |
  | 腾讯视频 | 直链+音频轨 ✅ |
  | 微视 | 4.36MB mp4 ✅ |
  | 快手 | 106MB hd15 直链 ✅ |
  | 爱奇艺 | qy.net 短链 → m3u8 ✅ |
  | 1905 电影网 | 18.6MB mp4 ✅ |
  | 小红书 | 3 个 mp4 格式 ✅ |
  | 红果短剧 | v3-share.qznovel 流 ✅ |
  | 微博 | 19.6MB 720p ✅ |
- **修复 6 个真 bug**（全部已 commit + 部署）：
  1. 快手签名流无 .mp4 后缀（kuaishou_resolve.py 放宽匹配 + video src 优先）
  2. qy.net 爱奇艺短链域名映射缺失
  3. xhslink.cn 小红书域名映射缺失 + `_expand_generic_302` 域名判断写反 + referer 参数化
  4. YY 提取器函数定义被误删（悬空 docstring → AttributeError）
  5. 红果分享页 novelquickapp.com 域名缺失 + `_STREAM_RE` 漏 qznovel.com + 字节 CDN 校验 Referer 域
  6. add_cookie 统一存根域（weixin.qq.com→qq.com.json，子域回退可读）
- **B 类 6 平台提取器**已实现部署：tudou（停运提示）/ weishi（可用）/ netease（weapi 加密选最高清晰度）/ yy（修复后优雅提示）/ hotstar / kinopoisk（走 yt-dlp，地区限制）
- **微视接入 VPS worker**：H5 接口 WSH5GetPlayPage 无水印直链 + Playwright 兜底，真实链接实测成功
- **微信视频号接入（判定不可行）**：完成用户自带 Cookie 透传/共享池/错误引导全链路，但实测确认**技术不可行**（客户端协议鉴权，非 HTTP Cookie），用户确认不投入，代码保留不影响其他平台

### 2.2 本轮上午 —— 网页版 UI 迭代
- tab 等宽、输入框对称；网页版固定 4 大 tab（从 DOM 移除 6 个 App 专属 tab，修 tabPcs null）
- 手机端 2x2 网格 tab 适配（CSS 媒体查询移到文件末尾 + 缓存版本号）
- 4 个域名映射 bug 修复（migu/wasu/taolive/kinopoisk）

### 2.3 历史成果（已在架构中）
- 跨平台 Cookie 共享池（cookie_pool.py，白名单 chrqj/bilibili/youku/weixin.qq.com）
- VPS worker 体系 15 平台注册（daemon + 隧道 + token 全通）
- 桌面版 app（PyInstaller 打包）+ 视频处理/去水印/解说管线
- 优酷 UPS+ckey 通道（绕开 yt-dlp -3007）

### 2.4 稳定性巡检加固（21:58-22:49，9 轮巡检 + 7 处修复，全部已上线）
**巡检覆盖**：前端交互/请求超时 → 后端 API/限流/SSRF/鉴权 → 下载管线（停滞检测/断点/并发）→ 隧道（重连/自启）→ VPS daemon（锁/清理/收集）→ Cookie 池（加密/验真/持久卷）→ yt-dlp（自动更新）→ 磁盘/安全/冷启动。

**7 处加固（commit）**：
1. **cookie 推送重试**（`ca025fe`）：`_push_cookie_to_cloud` 3 次尝试 + 指数退避（429/5xx 重试，网络异常立即抛），同步 app-dev 已有加固
2. **前端 fetch 超时**（`049a3d9`）：统一 request() 加 AbortController（默认 120s 覆盖 VPS 85s，可 options.timeout 覆盖），超时抛可读错误 category=timeout——此前后端/VPS 挂起时前端无限等待
3. **VPS profile 缓存清理**（`66f9c41`）：daemon 加 `_prune_profile_caches`（启动即跑 + 12h 周期），只删 48h 前 Chromium 缓存（保留 Cookies/IndexedDB 登录态）——11 个 profile 总量 2G→790M
4. **daemon 锁超时**（`d982a31`）：解析锁/收集锁加 8s 等待上限，防并发请求无限排队饿死 + 线程堆积撑爆小内存 VPS
5. **yt-dlp 自动更新**（`74c9b57`）：lifespan 启动后 60-180s 随机延迟后台调用 ydlp_update.update()（PyPI wheel + sha256 校验 + 原子替换），7 天冷却——平台反爬频繁改版，此前固定版 2026.7.4 落后最新 1 个多月
6. **yt-dlp 持久卷**（`0ba06b9`）：自动更新目录优先写 Railway 持久卷（RAILWAY_VOLUME_MOUNT_PATH），无卷回退 home——否则部署重启即丢
7. **VPS 隧道开机自启**（运维级，无代码提交）：`systemctl enable cn_tunnel_client`——此前 disabled，VPS 重启后隧道不拉起会导致所有 VPS worker 平台全挂

**9 轮巡检确认稳定的关键机制**（无需改，供后续运维参考）：
- VPS daemon：systemd `Restart=always`+RestartSec=5 自愈；85s 超时 + `_kill_all_chromium` 全覆盖；B 站 Cookie 收集 passed=True 有效
- 隧道：client 20s ping/10s 超时/RECONNECT=5s；cn_tunnel_client + cn_proxy 均已 enabled，进程 3 天零重启
- Cookie 池：`_LOCK` 线程锁 + 原子写（跨进程安全）；验真只对超 TTL(30天) 项 + 明确无效才删；Railway 走持久卷
- 下载管线：180s 停滞检测 + 7200s 硬超时 + 5s 看门狗 + 并发 clamp 64 + .part 断点续传 + urllib3 重试
- 安全：SSRF 6 处入口（拒 localhost/.local/全私网含 169.254.169.254）；token 鉴权中间件；下载 task_id+device_id 双校验；daemon 双 token 鉴权
- 前端：直接 fetch（contribute/blob）均有 catch 兜底；转化轮询 1.5s 单任务 try/catch；静态资源带版本指纹
- 线上回归：核心测试 39+51 passed；抖音/B站/快手/腾讯/小红书/微视线上实测全通；YouTube（PO Token）解析正常

---

## 3. 当前进行到哪一步 + 未完成待办

### 当前状态
- **v1.0.1 稳定版**：10 平台真实实测通过 + 9 轮巡检完成 + 7 处加固全部上线（git tag v1.0.1-stability = 0ba06b9）
- VPS worker 15 平台模块全部健康；B 站 Cookie 收集 passed=True；隧道/daemon 三服务全部 enabled 开机自启
- 线上 Railway（git_sha=0ba06b9）+ VPS 运行正常；前端/后端/隧道/VPS 全链路无遗留实质不稳定点

### 待办清单（⚠️ 全部已关闭，用户 2026-08-24 确认"这些都不要了"，勿主动拾起）
- ❌ 剩余 5 平台真实链接实测（央视频/斗鱼/百视TV/映客/网易CC）
- ❌ netease（网易云MV）提取器真链验证
- ❌ 桌面端优酷引擎本机 Mac 实测
- ❌ /download 安装包直链替换
- ❌ 微信视频号（判定不可行，代码保留）
- ✅ 已闭环：稳定性 7 处加固（ca025fe→0ba06b9 全部上线）

---

## 4. 关键约束、修改要求、注意事项

### 铁律（用户明确）
- **用户不执行任何命令行运维操作**（SSH/systemctl/改文件等）——AI 自己干，绝不让用户操作
- VPS：`ssh root@8.138.223.3` 直连可用（SSH key 已配）
- 部署自查：push web-dev 后轮询 `https://hanyuxz.top/api/version` 的 `git_sha` 确认 Railway 自动部署
- 真实密钥/凭据必须用户**手动复制粘贴文本**，绝不从截图 OCR 提取

### 技术约束
- **Railway 部署分支 = web-dev**（push 自动部署，轮询 /api/version 的 git_sha 确认）
- **VPS daemon 由 systemd `vdl-cookie.service` 守护**（Restart=always + RestartSec=5，崩溃自动拉起）。改 daemon/worker 后：scp 文件 → systemd 会在 2s 内用新文件拉起（`systemctl restart vdl-cookie` 或重启脚本均可）；注意 restart_daemon.sh 与 systemd 抢端口会出现 "Address already in use" 日志——**无害噪音**，systemd 实例已合法接管，用 `ps aux | grep vdl_cookie_daemon` + `grep 新代码特征` 确认实际加载版本
- **VPS 三核心服务全部 enabled 开机自启**：vdl-cookie（daemon）/ cn_proxy / cn_tunnel_client——隧道断连时 Railway 上所有 VPS worker 平台全挂，VPS 重启后务必确认隧道进程自动拉起
- worker 文件改完要 **scp 到 VPS** 才生效（仓库只是版本管理镜像）
- **Railway 持久卷** `RAILWAY_VOLUME_MOUNT_PATH`：cookie 池 + yt-dlp 自动更新目录共用，部署重启不丢
- **yt-dlp 自动更新**：lifespan 启动 60-180s 后台自动更新（7 天冷却）；`/api/ydlp/version` 的 current 读 pip 固定版（看不到持久卷新版），新版经 bootstrap sys.path 优先加载、重启后生效
- 前端 `web/app.js` 交付前必须自查（jsdom/agent-browser 实测）；CSS 移动端媒体查询必须放 styles.css 文件末尾
- **已测 bug 教训**：
  - 快手签名流无 .mp4 后缀（匹配要认 CDN 域 + /ksc2/）
  - 字节系 CDN（红果 qznovel）校验 **Referer 域**，分享页必须带分享域名 referer + Range
  - 爱奇艺短链 = qy.net/iqy.net（302 展开 + 保留 shareId）
  - 小红书短链 = xhslink.cn（302 展开 + 保留 xsec_token，`_expand_generic_302` 域名判断已修正）
  - Playwright `add_cookies` 拒绝 expires=None（会话 cookie 省略 expires 键）
  - ResolveError 归类查 hint 而非 message（message 常是通用"视频解析失败"）

### 平台真相
- **微信视频号：网页版技术不可行**（客户端协议鉴权，非 HTTP Cookie；Mac 微信 v4 移除开发者工具；本地 cookie 私有密钥加密）——替代：微信内转发文件传输助手长按保存/录屏
- 微视：可用的（H5 接口无水印 + Playwright 兜底）
- 部分平台签名直链有时效（快手几分钟、爱奇艺 m3u8 短时），拿到直链尽快下载；VDL 有 90s worker 缓存

### 目录/仓库
- 仓库：`github.com/leydall0999-cell/video-downloader`（web-dev 分支 = 部署线）
- 工作目录：`/Users/suixindelang/WorkBuddy/video-downloader`
- 注意：`commentary-pipeline` 是软链指向真实管线源码（视频解说相关）；`commentary-worker` 是调研误判的副本，别改错
