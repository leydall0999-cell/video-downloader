# video-downloader 项目接续快照

> 生成时间：2026-08-20 22:30 (GMT+8) ｜ 当前分支：`web-dev`（HEAD=`c83ca4b`，**已 push origin/web-dev**）｜ 线上版本：`hanyuxz.top/api/version` git_sha=`c83ca4b`（功能全部部署）
> 用途：复制到新会话，让 AI 直接接着往下开发，无需翻完整聊天记录。

---

## 一、原始需求

- **项目**：自托管「视频下载 / 媒体备份」Web 工具（**FastAPI + yt-dlp + 原生前端**），域名 `hanyuxz.top`（Railway 海外节点，经国内 VPS `8.138.223.3` 回源）。
- **本次会话主线（8/20）**：① 网页版只保留「下载 / 视频格式转换 / 去水印」三大功能，App 专属功能绝不能闪现；② 爱奇艺分享链接（`playShare.html` / `iqy.net` 短链）能解析下载。
- **增值方向**：视频解说自动成片（commentary，代码已落地、默认关闭 `VDL_COMMENTARY_ENABLED=false`）。
- **合规红线**：只下载自己拥有/已授权内容；**不破解付费墙/DRM**。

---

## 二、已完成工作成果（全部线上可用）

### 本次会话（8/20）完成
1. **网页版闪 App 功能修复**（commit `5d6f438`）：`web/index.html` 里 4 个 App 专属 tab（tabLibrary/tabCommentary/tabSubscribe/tabPcs）默认加 `hidden`；App 版不受影响（app.js else 分支按能力显式覆盖）。三层防线：HTML 默认 hidden + setTimeout bootViewSet 守卫 + `/api/nodes` profile 驱动。**网页版刷新不再闪现 App 功能** ✅
2. **爱奇艺分享页 VPS Playwright worker**（`da1436b`→`c83ca4b` 共 15 commits，全部已 push + Railway/VPS 部署）：
   - `vps_worker/iqiyi_resolve.py`：**最终方案 = 浏览器先访问 iqiyi.com 主页建会话 + Playwright 监听网络请求捕获 `meta-cdn.video.iqiyi.com/*.m3u8` 直链**（不自己调 tmts API）+ **stealth 指纹伪装**（webdriver/plugins/chrome 对象/去 --enable-automation，绕过爱奇艺 headless 检测）。
   - `vps_worker/vdl_cookie_daemon.py`：启动自加载 `.cookie_sync.env`（nohup 重启不丢 token）。
   - `server/downloader.py`：`_IQIYI_HOSTS`/`_is_iqiyi_host`/`_iqiyi_info`（m3u8_native + Referer），probe() 与 _run_once 路由；worker 未配置且非 playShare 时回退 yt-dlp。
   - `server/routers/core.py`：iqiyi host resolve 超时 20s→90s。
   - VPS 端到端实测：连续调用返回 `{"ok": true, "title": "活佛济公3", "video_url": "meta-cdn m3u8"}` ✅
   - 配套脚本：`deploy_iqiyi.sh`（一键部署+稳健重启）、`test_iqiyi.sh`（自动取 token 端到端测试）、`debug_iqiyi.py`（页面 dump 诊断）。

### 此前会话已完成（详见旧快照）
- **平台下载**：bilibili（API 兜底）、小红书、抖音（VPS Playwright）、快手、微博、网易云、喜马拉雅（含专辑）、歌单/专辑批量下载。
- **架构**：VPS daemon（18731，`/v1/resolve` 通用端点 + cookie 推送）、公共 Cookie 池（`server/cookie_pool.py`）、B站 API 兜底、Token 轮换、cn_proxy 稳定性修复。
- **桌面 App**：app-dev worktree（`video-downloader-app`），已并入 main。

---

## 三、当前进度与待办

**现状**：网页版三大功能干净展示；爱奇艺 worker 代码链路 100% 完成、stealth 版 VPS 实测成功。**但爱奇艺反爬是动态对抗**——stealth 版大幅提升成功率，爱奇艺升级检测后可能复发。

**待办清单（按优先级）**
1. **爱奇艺长期稳定方案（重要，未做）**：登录 Cookie + yt-dlp IqiyiIE 路径（不走 worker）——用户贴 Cookie 到「高级选项」，v_xxx.html 长链直接 yt-dlp 下载。需要时可给用户写操作指引。
2. **验证用户网页端实际下载**：stealth 版已部署，让用户刷新网页端粘贴 playShare 链接实测（解析 50-70s，等待勿刷新）；若又跳 error.html 说明爱奇艺升级检测，需再调 stealth。
3. **`/api/cookie/dump` debug 端点收敛/移除**（旧待办，排查 B站 用完后可删）。
4. **main ← web-dev 反向合并（高优先级但需仔细）**：web-dev 是精简版（6 router），main 是全功能版（17 router），沿用 app-dev→main 经验做文件级覆盖合并，需用户明确指示时做。
5. **可选优化**：抖音解析 26s 提速；VPS 配住宅代理提升 B站 画质（当前数据中心 IP 限 480P）。
6. **暂缓平台**：腾讯（会员 DRM）、西瓜（内容大量失效）、知乎、爱优芒（DRM）。

---

## 四、关键约束与注意事项

- **分支**：默认只在 `web-dev` 开发；切 app-dev/main 需用户明确指示。桌面构建在 `video-downloader-app` worktree（app-dev）。
- **VPS daemon 重启（踩过的最大的坑）**：`pkill -f vdl_cookie_daemon.py` 会杀掉 ssh 会话自身（命令行含同样字符串）exit 255；孤儿 daemon 占端口会导致新代码起不来（"修了没生效"假象）。**正确流程**：`pgrep -f "[v]dl_cookie_daemon.py" | xargs -r kill -9` 全杀 → `fuser -k -n tcp 18731` 释端口 → `nohup /opt/vdl-worker/.venv/bin/python vdl_cookie_daemon.py >> daemon.log 2>&1 &`。验证：daemon.log 见 `✅ cookie daemon up`，`/v1/resolve` 返回新版本字段。
- **VPS 环境**：daemon 必须用 `/opt/vdl-worker/.venv/bin/python`（系统 python3 **无 playwright**）；VPS 1.6GB 内存，chromium 用完必须清理残留（--single-process + ps 精确杀，不能用 `pkill -f chromium` 会误杀自身）。
- **沙盒可直接连 VPS**：`ssh root@8.138.223.3` / `scp`（无需用户中转），脚本在 `/opt/vdl-worker/`。
- **爱奇艺链接边界（已实测）**：`iqy.net` 短链已被 GoDaddy/Afternic 劫持成停放页，**服务端不可解析，死路**；`iq.com` 国际版不在 yt-dlp `IqiyiIE._VALID_URL`（只认 `iqiyi.com/*.html`）范围内；`playShare.html` 分享页是 JS SPA，必须走 VPS worker（或登录 Cookie）。
- **版本核对**：以 `/api/version` 的 `git_sha` 为准，**勿用 Railway 部署卡片 hex**（那是部署 ID）。
- **Railway 部署**：Redeploy = 用相同代码重建；正确方式 = 推新 commit 到 web-dev 触发构建，或 Settings→Source 切分支。
- **凭据**：绝不从截图 OCR 提取凭据；token 值不明文外传（VPS `.cookie_sync.env` + Railway Variables）。
- **共享重资源必加锁**：定时循环 + HTTP 按需触发共用 Playwright 必须串行化（`_push_lock`），否则并发打挂 VPS。
- **记忆目录**：`.workbuddy/memory/` 刻意不入库；`.workbuddy` 文件夹非缓存勿删。今日详细日志在 `.workbuddy/memory/2026-08-20.md`。
