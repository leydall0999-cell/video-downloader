# 项目搬迁变更摘要（video-downloader）

> 变更日期：2026-08-15
> 关联 commit：`533b207`（本地提交，未推送）
> 文档定位：本摘要与 commit message 互补——commit 记"改了哪些文件"，本文件记"为什么改、影响面、验证方式、回滚方案"。

---

## 1. 背景与动机

长期记忆中记录项目应位于规范路径 `/Users/suixindelang/WorkBuddy/video-downloader`（自 2026-08-04 重命名），但实测仍停留在带日期戳的中间目录 `…/2026-08-04-17-23-10/video-downloader`。

该错位带来两个风险：
1. 历史教训 `2026-08-13` 工作区被整体清空，仅 `.workbuddy` 残留；若实例再次被清，带日期戳的旧目录易丢失，规范路径又不存在。
2. 桌面打包 `VideoDownloader.spec` 与文档硬编码了旧绝对路径，路径错位会导致 PyInstaller 打包失败、文档指引误导。

故执行正式搬迁，把项目落到规范路径并同步所有引用。

---

## 2. 变更清单

| # | 变更项 | 操作 | 说明 |
|---|--------|------|------|
| 1 | 项目根目录 | `mv`（同卷原子移动） | `2026-08-04-17-23-10/video-downloader` → `WorkBuddy/video-downloader`，`.git` 完整保留 |
| 2 | 父级软链 `commentary-pipeline` | 重建 | `…/WorkBuddy/commentary-pipeline` → `问问题/commentary-pipeline`，保持 `$REPO/../commentary-pipeline` 解析等价 |
| 3 | `VideoDownloader.spec` | 编辑 | 替换 6 处硬编码旧路径为规范路径，桌面打包恢复正常 |
| 4 | `项目结构说明.md` | 编辑 | 仓库路径说明同步为规范路径 |
| 5 | `build/` 目录 | 删除 | 属 `.gitignore` 忽略的 PyInstaller 中间产物，含旧路径缓存，可重建 |
| 6 | 启动冒烟 | 验证 | 新位置 5 条路由（`/api/version`、`/api/subscriptions`、`/api/retention/config`、`/api/library`、`/api/nodes`）全部返回 200 |
| 7 | 新建交付物本地提交 | 提交 | `tests/test_merged_modules_verify.py` + `合并后系统核对报告.md` + `项目结构说明.md` 落 `533b207`，防工作区清空丢失 |

---

## 3. 未变更 / 刻意保留项

- **`dist/VideoDownloader.dmg`（283MB 成品）**：独立交付物，不依赖源路径，原样保留。
- **旧 dated 父目录 `2026-08-04-17-23-10/`**：仅剩 `magnet-player` / `.workbuddy` / 旧软链，与本项目无关，未动。
- **`commentary-pipeline` 真实源码**（`问问题/commentary-pipeline`）：始终在外部，未被移动或破坏。
- **`.build_venv/`**：构建 venv 含旧路径缓存但属 gitignored，未清理（不影响运行，重建即新路径）。

---

## 4. 影响面评估

| 维度 | 影响 |
|------|------|
| 桌面打包（`build_mac.sh` / `VideoDownloader.spec`） | ✅ 已修复，spec 路径对齐 |
| 解说管线（`$REPO/../commentary-pipeline`） | ✅ 软链重建，解析等价 |
| 运行/启动 | ✅ 冒烟全 200，未破坏 |
| 远程/双节点部署 | ⚠️ 无影响（部署走相对路径 + 环境变量） |
| Git 历史 | ✅ 完整保留，仅新增 1 个本地 commit |
| 远程仓库 | ⚠️ 未推送，需用户在自有终端用 SSH `git push` |

---

## 5. 验证方法

```bash
# 1) 启动冒烟（新位置）
cd /Users/suixindelang/WorkBuddy/video-downloader
VDL_SUBSCRIPTIONS_ENABLED=1 VDL_LIBRARY_ENABLED=1 VDL_RETENTION_ENABLED=1 \
VDL_AI_DEWATERMARK_MODE=http VDL_AI_DEWATERMARK_ENDPOINT=http://fake.local \
.build_venv/bin/python - <<'PY'
import sys; sys.path.insert(0, "server")
from fastapi.testclient import TestClient
import app as m
c = TestClient(m.app)
for p in ["/api/version","/api/subscriptions","/api/retention/config","/api/library","/api/nodes"]:
    print(p, c.get(p).status_code)
PY

# 2) 残留路径检查（源/配置应为空）
grep -rln "2026-08-04-17-23-10" --include=*.py --include=*.sh --include=*.spec \
  --include=*.md --include=*.json . | grep -v -E "/dist/|.build_venv/|.git/"

# 3) git 状态
git status --short && git log --oneline -3
```

---

## 6. 回滚方案

若需回退到搬迁前状态：

```bash
# 回移目录（同卷原子）
mv /Users/suixindelang/WorkBuddy/video-downloader \
   /Users/suixindelang/WorkBuddy/2026-08-04-17-23-10/video-downloader
# 删除新位置软链（如需）
# rm /Users/suixindelang/WorkBuddy/commentary-pipeline
# 回退 commit（仅本地，未推送）
# git reset --soft HEAD~1
```

> 注：因使用 `mv`（非复制），旧目录已不存在；若需保留旧路径并存，应改用 `cp -a` 而非 `mv`。本次已确认目标不存在、无覆盖风险，故直接 `mv`。

---

## 7. 后续事项

- [ ] 用户在自有终端 `git push`（SSH）将 `533b207` 推到 GitHub，消除"未提交/未推送"丢失风险。
- [ ] 若后续正式弃用旧 dated 目录，可手动删除 `2026-08-04-17-23-10/`（确认无他物后）。
- [ ] 长期记忆中"video-downloader 自 2026-08-04 重命名"现已成事实，无需再改。
