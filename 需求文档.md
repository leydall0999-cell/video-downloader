# 需求文档（后期开发模块）

> 记录**规划中、尚未开发**的模块，供后续排期与开发参考。
> 已上线 / 已规划能力见 `DESKTOP_FEATURES.md`、`COMMENTARY_PLAN.md`，不在本文重复。
> 本文为活文档，新增需求直接追加模块即可。

---

## 模块一：视频上传 + 格式转换

### 目标 / 用户故事
用户本地已有视频，不想走「下载」环节，直接上传到站点后**修改输出格式 / 转码**（如 mp4→mov、视频→音频 mp3、改分辨率/码率、旋转竖屏等），处理完下载或存入媒体库。

### 用户流程
1. 拖拽或选择本地视频上传
2. 选择目标格式、可选参数（分辨率、码率、是否保留音轨、竖屏旋转）
3. 预览参数 → 点击「开始转换」
4. 进度条 → 完成 → 下载结果 / 存入媒体库

### 功能点
- [ ] 本地视频上传（复用现有上传接收能力，当前 `/api/upload` 用于解说，需扩展为通用上传）
- [ ] 目标格式选择（mp4 / mov / mkv / webm / mp3 / m4a / gif 等）
- [ ] 可选参数：分辨率、视频码率、音频开关、竖屏旋转（90°/180°）
- [ ] 转换进度展示（WebSocket 或轮询）
- [ ] 完成后下载 / 存入媒体库

### 技术基础（可复用，避免重复造轮子）
- `server/app.py` 已有 `CONVERT_EXT` 映射与 ffmpeg 转码逻辑（`-c:v libvpx-vp9` 等编解码），格式转换可直接复用。
- ffmpeg 已确认为必需依赖（Dockerfile 已装），无需新增底层能力。

### 待确认
- 上传文件大小 / 时长上限（云端算力与存储约束）
- 结果落云端还是仅本机临时目录
- 是否需要保留原画质（无损 remux vs 重编码）

---

## 模块二：PDF / 图片去水印

### 目标 / 用户故事
去除用户**自有或已授权**文档 / 图片上的水印（demo 水印、自己添加的标记、已购素材的预览水印等），便于二次创作或归档。仅处理用户有权处理的内容。

### 用户流程
1. 上传 PDF 或图片（png/jpg/webp 等）
2. 选择去水印方式：
   - 图片：框选水印区域 → 内容识别填充
   - PDF：自动移除页面水印图层（含注释型水印）；扫描件 PDF 先栅格化再去水印再合成
3. 预览「原图 vs 处理后」对比
4. 导出结果（图片 / 新 PDF）

### 功能点
- [ ] 图片去水印：框选水印区域 + inpainting 修复（OpenCV `inpaint` / 可选 AI 修复模型如 LaMa）
- [ ] 图片批量：多图批量处理
- [ ] PDF 去水印：遍历页面 objects / annotations 定位并删除水印对象
- [ ] 扫描型 PDF：页面栅格化 → 图片去水印 → 重排合成
- [ ] 原图 / 处理后对比预览
- [ ] 导出（图片另存 / 生成新 PDF）

### 技术参考
- 图片：`OpenCV` inpainting（TELEA / NS）做轻量修复；效果要求高可接 `LaMa`（Resolution-aware）等开源去水印模型。
- PDF：`PyMuPDF (fitz)` 遍历页面内容流与注释，定位 watermark 对象删除；扫描件走「栅格化 → 去水印 → 重排」。
- 水印区域自动识别（可选增强）：SAM / 传统轮廓检测，减少手动框选。

### 合规边界（硬约束，与项目红线一致）
- **仅用于用户自有 / 已授权内容的去水印**，禁止用于去除他人版权标识、规避授权。
- UI 需有用途提示；不提供批量去除第三方受版权素材水印的能力。

### 待确认
- 图片去水印用传统 inpaint 还是 AI 模型（体积 / 效果 / 算力权衡）
- 是否支持水印区域自动识别（免框选）
- PDF 水印类型覆盖度（注释型 / 图片型 / 背景型）

---

## 附：已有相关原型（避免重复开发）
- **视频去水印原型**：`问问题/watermark-removal/`（`process.py` / `worker.py`）
  - 能力：E2FGVI（GPU AI 修复）→ OpenCV inpainting（CPU）→ 仅模糊，三级自动降级；框选矩形区域。
  - 注意：这是**视频**去水印，与本文「模块二 PDF/图片去水印」技术路线不同，但同属「去水印工具集」，后期可考虑统一为一套工具。
