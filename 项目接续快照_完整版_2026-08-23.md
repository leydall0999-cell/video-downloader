# 项目接续快照 — video-downloader（VDL）完整版（2026-08-23）

> 生成时间：2026-08-23 11:20（GMT+8）｜整合 8/18→8/23 全部会话成果，版本号/提交/线上状态均已实测核对
> 用法：把本文档整体复制到新会话，AI 即可无缝续接开发。

---

## 0. 当前真实状态（一屏速览，全部实测）

| 项目 | 状态 |
|---|---|
| 本地 web-dev HEAD | `e3fc08c`（= origin/web-dev，已 push） |
| 线上 Railway `git_sha` | `e3fc08c`（push web-dev 即自动部署；轮询 `hanyuxz.top/api/version` 自证） |
| 域名 | `hanyuxz.top` **橙云**（Cloudflare 代理，dig=104.21.77.11/172.67.203.21） |
| Railway 原生域名 | `web-production-b9993.up.railway.app`（绕 CF 用：隧道/双通道上传） |
| VPS | `8.138.223.3`（阿里云，**固定带宽 ~3Mbps**，实测 VPS→Railway 仅 0.39MB/s） |
| 分支 | main=全功能（17 router）、web-dev=`e3fc08c`（网页精简版，**开发主线**）、app-dev（桌面端） |
| 平台 | `/api/platforms` 116 个白名单；直播 3 平台已下线 |
| 今日档案 | 本文件 + `.workbuddy/memory/2026-08-23.md`（当日全量日志） |

---

## 1. 项目原始需求

自托管「视频下载 / 媒体备份」Web 工具（**FastAPI + yt-dlp + 原生前端**），域名 `hanyuxz.top`（Railway 海外节点 + 国内 VPS `8.138.223.3` 回源）。

- 核心功能：50+ 平台下载解析、SSE 实时进度、国内外代理分流、SSRF 防护、视频格式转换、图片/PDF 去水印
- **增值方向**：视频解说自动成片（commentary，代码已落地、默认关闭 `VDL_COMMENTARY_ENABLED=false`）
- **网页版精简定位**：只保留下载 / 格式转换 / 去水印三大功能（web-dev 只挂 core/convert/dewatermark/crypto/fs/cloud 6 router）
- **合规红线**：只下载自己拥有/已授权内容；**不破解付费墙/DRM**；开源 MIT

---

## 2. 已完成工作成果（全量）

### A. 平台支持总表（2026-08-22 成果，未变）

- **VPS Playwright worker（13 resolver，`/opt/vdl-worker/` daemon 18731）**：抖音/快手/微博/喜马拉雅专辑/爱奇艺/央视频/1905/风行/百视TV/红果短剧/好看视频 ✅；斗鱼/映客/网易CC ⏸️ 已下线（DISABLED_PLATFORMS）
- **yt-dlp 原生/API 兜底**：bilibili（412→API 兜底）、小红书、网易云、YouTube、腾讯及全部国际平台
- **国际平台**：白名单 116；可直接解析 ~40；BitChute ✅；Rumble ⚠️ 需住宅代理；Tubi ⏳ 待住宅代理；DAZN/Netflix/Disney+ 等 28 个 DRM 明确提示
- **YouTube 免 Cookie**：解析 tv_embedded + 下载 android client 矩阵降级，43s 完成

### B. 架构与基础设施（2026-08-22，未变）

- **VPS daemon**（18731 `/v1/resolve?platform=&url=` + `/v1/push`）：Playwright 全局串行锁、85s 硬超时、解析前杀 chromium 残留
- **公共 Cookie 池**：`server/cookie_pool.py`（白名单/限频/验真/Fernet 加密），持久卷 `/data`；`/api/cookie/sync`（收）/`sync/from-local`（本机推）/`sync/to-cloud`（云推）
- **隧道**：Railway `wss://web-production-b9993.up.railway.app/ws/cn-tunnel`（原生域名绕 CF）；VPS client 按 Host 分流 daemon(18731)/cn_proxy(18888)；应用层 20s 心跳
- **下载链路**：直链 m3u8 归一化 `m3u8_native` + ext 保持 mp4 + FFmpegVideoRemuxer

### C. 转换模块全链路改造（⭐ 2026-08-23 主力，全部线上已验证）

**格式与容量**
- 上传上限 2GB→**10GB**（`UPLOAD_MAX_BYTES`，`VDL_UPLOAD_MAX_BYTES` 可覆盖）；白名单加 `.hevc/.h265`
- 格式 20→**23 种**：+hevc（H.265/MP4，libx265，`-tag:v hvc1`）+wma+mp2；ffmpeg 编码器探测 `_probe_ffmpeg_encoders()` → 缺库自动从 `/api/nodes` targets 隐藏（本机/线上全齐 23 全显）

**上传链路（提速 + 稳定性，4 轮迭代）**
- 分片上传：32MB/片（>2GB 自动 64MB）× **8 路并发**，单片失败重试 2 次、实时速度/已传量、XHR `timeout=120s` 防后台 tab 限流卡死；文件级并发 2
- **双端点混合上传**（commit 80f1aa8）：CF 免费版对上传 POST 限速 ~5MB/s（实测铁证：沙盒海外 4.96 与用户国内 <5 收敛同一数值）→ 分片在 `hanyuxz.top`（CF）与 `web-production-b9993.up.railway.app`（原生，无 CF 限速层）间分流，两端点同一后端同一分片存储可混合
- **双通道动态选路**（commit 718f1ff）：每文件 `_chStats` 记两通道最近 3 片平均吞吐，`ucPickEndpoint()` 80% 选快通道 + 20% 探索防锁死慢通道，重试固定换端。用户实测：**均值 >7MB/s、峰值 12MB/s**（原 5MB/s）
- 取消/删除：`/api/upload-chunk/abort` + XHR onabort + `_removed` 标记，上传中/待转码均可删

**转码与 UX（状态机 + 进度 + 命名）**
- **状态机**：`pending → uploading → uploaded（已上传·待转码）→ running → completed/failed`（commit 61a3992，后端零改动）
- 上传完**停等确认**：每行独立「开始转码」（用该行最新格式）+「开始批量转换」（各用各格式）；target 下拉 uploading/uploaded 可改、running/completed 锁定；批量参数=新文件默认模板，「应用到未开始」只覆盖 pending
- **转码进度实时可见**（commit e3fc08c，最新）：后端 `_run_convert` 改为 **ffprobe 独立预探测 total_dur + ffmpeg `-nostats -progress pipe:1`**（stdout 每 ~0.5s 一组 `out_time_us=`，比 stderr `time=` 可靠），独立 daemon 线程读 stderr 保留 tail 80 行，progress 只升不降；前端轮询 1.5s，stage 区分「排队中… / 无损直转中… / 转码中 xx%」，进度 30%→100% 映射
- **产物命名**：下载名 = `[格式]原文件名.扩展名`（如 `[MKV]我的视频.mkv`，`ucBuildOutputName`）；媒体库（to_library）同样 `[格式]原名.ext`（job 存 `src_name`）
- **自动无损直转**：ffprobe 探测源 codec 与目标默认编码一致 → 自动 `-c copy` 零重编码（`_probe_src_codecs` + `_TARGET_VIDEO_CODEC/_AUDIO_CODEC`），规避 Railway 容器 libx264 1080p rc=9
- **ffmpeg 容错**：`-err_detect ignore_err -fflags +discardcorrupt -analyzeduration 10M -probesize 10M`；失败时 stderr 尾部 80 行过滤无诊断头部后写入 job.error
- CORS `allow_headers` 补 **`X-Device-Id`**（原生域名跨域收分片必需）

---

## 3. 当前进度与待办

**现状**：转换模块全链路已完成并上线（10GB/23 格式/双通道上传/待转码确认/进度实时/文件名带参）。上传速度均值 >7MB/s（CF 限速 5 + 原生波动贡献），用户已接受现状。

**待办清单（按优先级）**：
1. **【待用户实测确认】转码进度实时性**：commit `e3fc08c` 刚部署，用户尚未复测 705MB 文件的进度条是否实时跳动（每 1-2s 跳一次）——用户确认前不算完
2. **CF Pro 升级（用户已决定"以后"做）**：$20/月解除上传限速，届时单通道即跑满用户宽带上行（>12MB/s 稳定）；升级后把双端点机制平滑切回单通道（前端 `UC_UPLOAD_ENDPOINTS` 相关逻辑可保留不动，天然自适应）
3. **Tubi 打通验证**：用户提供美/加/澳/新/英/墨任一住宅代理 → 复测 `_tubi_info`（代码就绪，`tubitv.com` 白名单已加）
4. **Rumble 打通验证**：需海外住宅代理（视频页 CF 挑战，数据中心 IP 403）
5. **直播模块恢复确认**：每周一 10:00 自动化提醒（automation-1787389098281）；恢复需先解决「直播流下载永不结束」（方案 A resolve 拒直播 / B 录制时长上限）
6. **main ← web-dev 反向合并**（长期挂起）：保留 main 全量 include_router + 补 web-dev 增强；用户明确要求时做
7. **桌面 App（app-dev）**：仅用户显式要求构建时涉及（`video-downloader-app` worktree；去水印多选区仅在 app-dev）

---

## 4. 关键约束 / 修改要求 / 注意事项

### 分支与开发
- **默认只在 `web-dev` 分支开发**，除非用户明确说切换（app-dev/main）
- 桌面构建必须在 `video-downloader-app` 目录（app-dev 分支）跑，否则缺去水印多选区；改 server/web 后必须重跑 `build_mac.sh`（验证：`grep -c dwSelections dist/.../web/app.js` 应为 11）
- 本机 git 2.15.0 老旧：`git merge-tree` 双参数不支持，冲突用 `git merge-file` 三方合并

### 部署与验证
- **Railway 版本自证**：push web-dev 后轮询 `hanyuxz.top/api/version` 的 `git_sha` == 刚 push 的 commit；部署卡片 8 位 hex 是部署 ID **不是** git SHA
- push 即自动部署，无需手动 Redeploy；超时未变才需干预
- **VPS 连接**：`ssh root@8.138.223.3`（SSH key 可用）；VPS daemon 在 `/opt/vdl-worker/`（.venv 有 playwright）
- **daemon 重启安全**：`pgrep -f "[v]dl_cookie_daemon.py" | xargs -r kill -9` + `fuser -k -n tcp 18731` + 用 `.venv/bin/python` 重启；**勿用 `pkill -f`**（会杀 ssh 会话）

### 上传链路（2026-08-23 沉淀，重要）
- **CF 免费版对上传 POST 限速 ~5MB/s 是硬事实**（用户国内链路与沙盒海外链路都收敛 5 左右）；下载不限速走 CDN 反而快 → 下载/轮询/finish 保持同源 hanyuxz.top，只有分片上传走双端点
- **VPS 中转方案已否决**：VPS 固定带宽 ~3Mbps（实测 VPS→Railway 0.39MB/s），中转会把用户上传卡死在 0.4MB/s；除非将来换大带宽 VPS
- 双端点依赖 `UC_UPLOAD_ENDPOINTS`（前端 app.js）+ CORS 已放 `X-Device-Id`；**两端点必须是同一 Railway 部署**（同一 UPLOAD_TMP 分片存储），换部署/加域名要同步改前端常量
- 分片 XHR 必须有 timeout（防 Chrome 后台 tab 限流永久 pending）；finish 是一次性合并操作不可服务端重试，失败只能提示重传

### 平台/网络速查（2026-08-22，未变）
- VPS 中国 IP 不可达：YouTube/Rumble（被墙）、Tubi（302 gdpr）、DAZN（地区锁）、多数欧洲公共台
- 国内站下载必须回源代理（`VDL_PROXY_CN`/18889 隧道），海外直连地理围栏 403
- YouTube（数据中心 IP）：解析 tv_embedded 稳；下载必须 android client
- B站：412 风控走 API 兜底；链接归一化 `www.bilibili.com/video/BVxxx` 长链（勿用 b23.tv 短链喂 extractor）
- 爱奇艺：免费内容走 `*.inter.71edge.com/*.f4v`（FLV 直链）；VIP 返回 cookie_required

### 技术要点
- **凭据绝不从截图 OCR 提取**（AppKey/Secret/Token 必须用户手动复制粘贴文本）
- 共享重资源必加锁（定时循环 + HTTP 按需共用 Playwright 必须串行化）
- web-dev 路由探查：`_IncludedRouter.path` 是 'N/A'，静态遍历会漏；用 TestClient 真实请求或数 `_IncludedRouter` 实例（web-dev=6）
- 转码进度：ffprobe 预探测 total_dur 失败时 progress 保持 0（前端 30%）——属损坏文件场景，可接受；勿回退 stderr `time=` 解析（容器资源紧张时 Duration 行可能不出导致卡死）

### 合规
- 不破解付费墙/DRM；会员/DRM 内容（Netflix/Disney+/DAZN/Channel4 等）明确提示不解析
- 直播流下载永不结束问题未解决（live 会录到硬超时）——直播平台已下线规避；恢复需先处理

---
