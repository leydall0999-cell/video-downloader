# 项目接续快照 — video-downloader（VDL）完整版（2026-08-22）

> 生成时间：2026-08-22 22:02（GMT+8）｜整合 8/18→8/22 全部会话成果，所有版本号/提交/服务状态均已实测核对
> 用法：把本文档整体复制到新会话，AI 即可无缝续接开发。

---

## 0. 当前真实状态（一屏速览，全部实测）

| 项目 | 状态 |
|---|---|
| 本地 web-dev HEAD | `5aa1755`（= origin/web-dev，已 push；干净无未提交改动） |
| 线上 Railway `git_sha` | `5aa1755`（Railway 绑定 GitHub web-dev，push 即自动部署，轮询 /api/version 自证） |
| 平台总数 | `/api/platforms` 返回 **116** 个（白名单识别）；直播 3 平台已下线隐藏 |
| 国际平台矩阵 | `INTERNATIONAL_PLATFORMS.md` 四类分级（可直接解析 ~40 / 需 Cookie ~8 / DRM ~28 / 无 extractor ~35） |
| VPS daemon | 18731 LISTEN（Playwright worker 13 个 resolver），85s 硬超时 + chromium 回收加固 |
| 隧道 | Railway↔VPS 隧道正常（VDL_PROXY_CN 回源国内出口） |
| 分支 | main=`98b38d2`（全功能）、web-dev=`babd6fe`（网页精简版，**开发主线**）、app-dev（桌面端，未动） |
| 今日档案 | `项目接续快照_2026-08-22.md`（当日明细）+ 本文档（全量整合） |

---

## 1. 项目原始需求

自托管「视频下载 / 媒体备份」Web 工具（**FastAPI + yt-dlp + 原生前端**），域名 `hanyuxz.top`（Railway 海外节点，经国内 VPS `8.138.223.3` 回源）。

- 核心功能：50+ 平台下载解析、SSE 实时进度、国内外代理分流、SSRF 防护、视频格式转换、图片/PDF 去水印
- **增值方向**：视频解说自动成片（commentary，代码已落地、默认关闭 `VDL_COMMENTARY_ENABLED=false`）
- **网页版精简定位**：只保留下载 / 格式转换 / 去水印三大功能（web-dev 剥离桌面 App 专属 router）
- **合规红线**：只下载自己拥有/已授权内容；**不破解付费墙/DRM**（Netflix/Disney+/DAZN 等一律明确提示不做）；开源 MIT

---

## 2. 已完成工作成果（全量）

### A. 平台支持总表

**VPS Playwright worker（13 个 resolver，`vps_worker/` + daemon 注册）**
| 平台 | worker | 方式 | 状态 |
|---|---|---|---|
| 抖音 | douyin_resolve.py | Playwright 音视频轨分离，需 ffmpeg 合并 | ✅ |
| 快手 | kuaishou_resolve.py | Playwright 合并 mp4 单文件 | ✅ |
| 微博 | weibo_resolve.py | Playwright 过 Sina Visitor System | ✅ |
| 喜马拉雅专辑 | ximalaya_album_resolve.py | getTracksList 分页 | ✅ |
| 爱奇艺 | iqiyi_resolve.py | Playwright 监听 m3u8/f4v；stealth 伪装；VIP 检测 | ✅ |
| 斗鱼/映客/网易CC | douyu/inke/cc_resolve.py | Playwright/纯 HTTP | ⏸️ 已下线（DISABLED_PLATFORMS） |
| 央视频 | ysp_resolve.py | Playwright JSONP cKey 签名 | ✅ |
| 1905 | m1905_resolve.py | Playwright（详情页反爬） | ✅ |
| 风行 | fun_resolve.py | Playwright 接口签名 | ✅ |
| 百视TV | bestv_resolve.py | wasm makepreviewquery 签名 | ⚠️ 网页端仅试看 9s（平台限制） |
| 红果短剧 | hongguo_resolve.py | Playwright 捕获字节 CDN 流 | ✅ |
| 好看视频 | yt_dlp_plugins/extractor/haokan.py | base64+XOR 解密 | ✅ |

**yt-dlp 原生 / API 兜底**：bilibili（412 风控 → `_bilibili_api_extract` API 兜底）、小红书、网易云、YouTube（见下）、腾讯（部分）、以及全部国际平台（通用兜底）。

**国际平台（2026-08-22 盘点）**：白名单 116；可直接解析约 40（含 FranceTV/Yle 实测、Kick/CHZZK 等）；BitChute ✅ 已打通；Rumble ⚠️ 需住宅代理；Tubi ⏳ 代码就绪待住宅代理；DAZN 🚫 DRM 不可行；Netflix/Disney+ 等 28 个 DRM 平台明确提示。

### B. YouTube 免 Cookie 下载（⭐ 2026-08-22 突破，43s）
- **机制**：解析走 `tv_embedded` client（稳定）；下载 403 自动进入 **player_client × format 矩阵降级**（`_run_once`），`android` client（直连 mp4）对数据中心 IP 放行——实测唯一可行，置首位后 **43s 完成**。
- **Cookie 自动链路**：user 粘贴 > env `VDL_YOUTUBE_COOKIE` > 本机缓存 > 公共池 pool。

### C. 架构与基础设施
- **VPS daemon**（18731，`/v1/resolve?platform=&url=` 通用端点 + `/v1/push`）：Playwright 全局串行锁（`_resolve_lock`/`_push_lock` 独立）、85s 硬超时（ThreadPoolExecutor + `shutdown(wait=False)`）、每次解析前 `_kill_all_chromium()` 清残留。
- **公共 Cookie 池**：`server/cookie_pool.py`（白名单/限频/验真/Fernet 加密），持久卷 `/data`；接收端 `/api/cookie/sync` + 本机 `/api/cookie/sync/from-local` + 推送 `/api/cookie/sync/to-cloud`。
- **国内回源代理**：Railway（海外）经 `VDL_PROXY_CN`（默认 127.0.0.1:18889 隧道）回源国内出口，否则国内站被地理围栏 403。
- **隧道**：Railway `wss://web-production-b9993.up.railway.app/ws/cn-tunnel`（原生域名绕 CF）；VPS client 按 Host 分流 daemon(18731)/cn_proxy(18888)；应用层 20s 心跳。
- **下载链路加固**（8/22）：直链 m3u8 归一化 `m3u8_native` + ext 保持 mp4 + `FFmpegVideoRemuxer`（-c copy 封装 mp4）；daemon 防堆积；bestv 试看/VIP 检测；is_live 透传。

---

## 3. 当前进度与待办

**现状**：国内平台收尾（直播 3 平台下线）；国际平台 116 白名单 + 矩阵文档；下载链路实测健康（抖音/B站/红果/1905/央视频/风行全量下载验证）；YouTube 免 Cookie 可用。

**待办清单（按优先级）**：
1. **Tubi 打通验证**：用户提供美/加/澳/新/英/墨任一住宅代理 → 「高级选项 → 代理」配置后复测 `_tubi_info`（代码已就绪，`tubitv.com` 白名单已加）。
2. **Rumble 打通验证**：需海外住宅代理（视频页 CF 挑战，数据中心 IP 稳定 403）；代码已防御性接入。
3. **直播模块恢复确认**：每周一 10:00 自动化提醒（automation-1787389098281）会询问；恢复需处理「直播流下载永不结束」（方案 A resolve 拒直播下载 / B 录制时长上限）。
4. **免费 AVOD 系列**：Popcornflix/Kanopy 等无 DRM 平台可逐个实测（评估逻辑同 Tubi；VPS 中国 IP 受限）。
5. **国际平台偶发反爬**（Rumble/Pluto/Yle 403/429）：重试或代理。
6. **main ← web-dev 反向合并**（长期挂起，风险高需仔细）：web-dev 精简版 vs main 全功能版，沿用 app-dev→main 经验（保留 main 全量 include_router + 补 web-dev 增强）；用户明确要求时做。
7. **桌面 App（app-dev）**：仅在用户显式要求构建时涉及（`video-downloader-app` worktree，app-dev 分支；去水印多选区仅在 app-dev）。

---

## 4. 关键约束 / 修改要求 / 注意事项

### 分支与开发
- **默认只在 `web-dev` 分支开发**，除非用户明确说切换（app-dev/main）。
- 桌面构建必须在 `video-downloader-app` 目录（app-dev 分支）跑，否则缺去水印多选区；改 server/web 后**必须重跑 build_mac.sh**（验证：`grep -c dwSelections dist/.../web/app.js` 应为 11）。
- 本机 git 2.15.0 老旧：`git merge-tree` 双参数不支持，分支冲突用 `git merge-file` 三方合并。

### 部署与验证
- **Railway 版本自证**：轮询 `hanyuxz.top/api/version` 的 `git_sha` == 刚 push 的 commit；部署卡片上的 8 位 hex 是 Railway 部署 ID，**不是** git SHA，勿比对。
- push web-dev 即自动部署（Railway 绑定 GitHub），无需手动 Redeploy；超时未变才需干预。
- **VPS 连接**：`env -u http_proxy ssh root@8.138.223.3`（沙盒 SSH 走 22 端口）；VPS daemon/脚本在 `/opt/vdl-worker/`（.venv 有 playwright）。
- **daemon 重启安全流程**：`pgrep -f "[v]dl_cookie_daemon.py" | xargs -r kill -9` + `fuser -k -n tcp 18731` + `nohup .venv/bin/python vdl_cookie_daemon.py >> daemon.log 2>&1 &`；**勿用 `pkill -f`**（会杀 ssh 会话 exit 255）。注意多次重启会残留孤儿 daemon（PPID=1）占端口 → 新代码起不来、旧 daemon 还在跑的"修了没生效"假象。

### 平台/网络速查
- **VPS 中国 IP 不可达**：YouTube（被墙）、Rumble（被墙）、Tubi（302 gdpr.tubi.tv）、DAZN（403 地区锁）、欧洲公共台多数（NRK/ARTE/ABC/ITVX/BBC 被墙或地区锁）。
- **Railway 数据中心 IP**：Tubi 返回 2.8KB 无 `window.__data` 反爬壳页；Rumble 视频页 CF 403；Yle 403；FranceTV 偶发 extractor 差异。
- **国内站下载必须回源代理**（`VDL_PROXY_CN`/18889 隧道），海外直连被地理围栏 403。
- **YouTube client 结论**（数据中心 IP）：解析 tv_embedded 稳；下载必须 android（ios=SABR 无格式、tv=页面重载、tv_embedded=下载 403）。
- **欧洲公共台地区锁**：NRK（挪威 IP）、Yle（数据中心 403）；FranceTV/Yle 部分内容中国 IP 反而可解析。

### 技术要点
- **直链 m3u8 下载**：protocol 归一化 `m3u8_native` + ext 保持 mp4（勿改 m3u8 扩展名）+ FFmpegVideoRemuxer remux。
- **无 height format 兜底**（BitChute 等）：`_detect_play_url` 3b 与 `build_watch_options` 已加无 height 直链兜底，勿删。
- **curl_cffi**：Chrome TLS 指纹模拟，requirements 已加（Rumble embedJS 等 CF 站点用）。
- **爱奇艺**：免费内容走 `*.inter.71edge.com/*.f4v`（FLV 直链）；`data/pcw-data.video.iqiyi.com` 是 JSON 代理非真实媒体；VIP 内容返回 cookie_required。
- **B站**：页面 412 风控走 API 兜底（`_bilibili_api_extract`）；链接统一归一化为 `www.bilibili.com/video/BVxxx` 长链（勿用 b23.tv 短链喂 extractor）。
- **凭据**：绝不从截图 OCR 提取凭据（AppKey/Secret/Token 必须用户手动复制粘贴文本）；token 值不再明文外传（VPS `.cookie_sync.env` + Railway Variables）。
- **共享重资源必加锁**：定时循环 + HTTP 按需触发共用 Playwright 必须串行化，否则并发打挂 VPS。
- **web-dev 路由探查**：app.py 的 `_IncludedRouter.path` 是 'N/A'，静态遍历 routes 会漏判；用 TestClient 发真实请求或统计 `_IncludedRouter` 个数（web-dev=6：core/convert/dewatermark/crypto/fs/cloud）。

### 合规
- 不破解付费墙/DRM；会员/DRM 内容（Netflix/Disney+/DAZN/Channel4 等）明确提示不解析。
- 直播流下载永不结束问题未解决（live FLV/m3u8 会录制到硬超时）——直播平台已下线规避；恢复需先处理。

---

## 5. 关键文档
- `INTERNATIONAL_PLATFORMS.md`：国际平台四类矩阵（120+ 平台，2026-08-22 更新）。
- `项目接续快照_2026-08-22.md`：8/22 当日明细（39+ commit 逐条）。
- `CN_PROXY_SETUP.md` / `COOKIE_POOL_SETUP.md` / `DESKTOP_FEATURES.md` / `COMMENTARY_PLAN.md`：各模块手册。
- `.workbuddy/memory/MEMORY.md`：项目长期笔记；`.workbuddy/memory/2026-08-22.md`：今日日志。

---

## 6. 2026-08-23 增量（设备隔离 / 无声修复 / 反馈 / 转换优化）

> 本节为 2026-08-22 完整版快照之后的增量，新会话直接沿用即可，无需读旧日志。

### 本轮成果（全部已部署线上，git_sha 轮询确认）
| 板块 | commit | 内容 |
|---|---|---|
| 下载提速 | `685bcba` | VPS worker 解析结果 90s 短时缓存（解析→下载不再二次 Playwright）+ requests Session 复用；解析后下载 6s 完成 |
| 前端反馈 | `03113f6` | 下载按钮 loading 防连点 + 新任务卡滚动定位 |
| UI 去重 | `63645c3` | 移除输入区重复平台 chips，保留 header 徽章 |
| **任务设备隔离** | `664f9fa`/`03dbbf1`/`997b275` | **每个页面任务独立可见**：前端 sessionStorage device_id + X-Device-Id header；后端 TaskStore.device_id + 全端点归属校验（列表/详情/SSE/文件/retry/pause/resume/delete/cancel-all/convert/cloud-save/upload-convert）；无归属=系统任务全局可见；无 device 请求只返回系统任务（不泄露）。**修复跨设备自动下载 BUG**（syncMissingCards 收养 + paintTask 自动 save.click 的副作用） |
| **下载无声修复** | `93dbcba`/`fe95f0d` | 抖音无声根因：worker 过滤器漏 audio 轨 + muted 播放不加载音频 + 分离轨/混合轨未区分 → worker 三分分类+取消静音+混合轨优先；_douyin_info 混合轨单直链/分离轨双 format 合并；新增 `_ensure_audio_track` 成品音轨校验+两级补救（全平台兜底） |
| 留言反馈 | `7b07024` | 右下角「💬反馈」悬浮按钮+弹窗；POST /api/feedback → downloads/.feedback.json（限流/锁/500条上限） |
| 转换优化 | `e6152f5` | **格式 7→20 种**（+avi/flv/ts/m4v/wmv/mpeg/3gp/ogv/aac/wav/flac/ogg/opus）；上传音频后缀放开；/api/nodes 暴露 max_upload_bytes+targets；前端格式下拉动态化+大小提示；批量上传并发 2→3 |

### 当前线上状态（2026-08-23 00:50）
- 分支：web-dev HEAD=`e6152f5`（origin 已同步）；线上 git_sha=e6152f5
- 平台：116 个识别；VPS daemon 13 worker 在线
- 转换：20 种输出格式；上传上限 2GB（VDL_UPLOAD_MAX_BYTES 可覆盖）；最大转换视频=2GB
- 反馈：downloads/.feedback.json 实时接收用户留言

### 新增关键机制（务必沿用）
- **设备隔离**：任何新前端 XHR（非 request() 封装）必须手动带 `X-Device-Id`（见 upload-convert 先例）；新任务端点必须 `_require_task(task_id, _device_of(request))`；TaskStore.list_all(device) 无归属任务全局可见、device 空只返系统任务。
- **抖音流**：混合轨（v{id}-web.douyinvod.com mp4 自带音轨）单直链；分离轨（media-video-avc1+media-audio）必须双 format 合并；worker 须静音起播 1.5s 后取消静音才能抓到音频轨。
- **音轨防御**：`_ensure_audio_track`（下载完成调用）——有声零开销；无声→独立音频轨重合并→转 mkv→明确提示。纯音频任务跳过。
- **转换格式**：ogv/ogg 用 libvpx+libopus（libtheora/libvorbis 部分 ffmpeg 构建缺失）；新格式需在 CONVERT_TARGETS+CONVERT_EXT+前端 fmtOptions 三处同步。

### 待办（按优先级）
1. 用户反馈处理：定期查 downloads/.feedback.json 回复用户留言
2. Tubi 住宅代理验证（代码就绪，缺网络出口）
3. Rumble 需海外住宅代理/浏览器会话（CF 挑战）
4. 直播恢复确认（每周一自动化提醒中）
5. main↔web-dev 合并同步（web-dev 已领先 main 大量提交）
6. 桌面 App 重构建（app-dev 需同步设备隔离等修复——**注意：设备隔离改动需同步到 app-dev 打包**）

---

## 7. 2026-08-23 增量·续（10GB 上限 / 23 种格式 / 分片并发上传）

> 追加于 2026-08-23 01:10（GMT+8）。用户需求：2GB 太小提 10GB、格式增加常用格式、上传还是慢。commit `5aa1755`（web-dev），已 push + 线上 git_sha 轮询确认部署。

### 本轮成果（全部已部署线上并实测）
| 板块 | 内容 |
|---|---|
| **上传上限** | `UPLOAD_MAX_BYTES` 默认 2GB→**10GB**（`VDL_UPLOAD_MAX_BYTES` 可覆盖）；上传白名单 +`.hevc/.h265` 源文件 |
| **新格式 20→23** | +**hevc**（H.265/MP4，libx265，`-tag:v hvc1`）、+**wma**（wmav2 音频）、+**mp2**（音频）。前端标注：hevc「省空间」、音频「仅音频」 |
| **编码器探测** | `_probe_ffmpeg_encoders()` + `_TARGET_ENCODER_DEPS` → `CONVERT_TARGETS_AVAILABLE`；缺 libx265 等库的 ffmpeg 自动从 /api/nodes targets 隐藏不可用格式（本机与线上 Debian ffmpeg 全齐，23 种全显）。**新格式必须三处同步**：CONVERT_TARGETS + CONVERT_EXT + 前端 fmtOptions（AUDIO_ONLY_FMTS） |
| **分片并发上传**（提速核心） | 前端 32MB/片 × **4 路并发**、单片失败重试 2 次、实时速度/已传量（0.4s 平滑窗口）、文件级并发 3→2（总连接 8）。后端 `POST /api/upload-chunk`（落盘 up_{id}.p{index}，单片上限 64MB、累计超限 413+清全部片）+ `POST /api/upload-chunk/finish`（校验齐全→顺序合并→精确限流→提交 job，沿用设备隔离）。旧 `/api/upload-convert` 保留兼容 |
| **孤儿分片清理** | `_cleanup_orphan_upload_parts()`：>24h 的 up_*.p* 残留定期清理（断传/关页兜底） |

### 验证记录（本轮补齐）
- **新格式真实转码**（本地 ffmpeg 实测）：hevc → 视频流 h265 + 音频 aac；wma → wmav2 音频；mp2 → mp2 音频。全部 completed ✅
- **分片边界**（TestClient）：70MB 随机文件 3 片并发→finish 200 且分片清理 ✅；mock 上限 500KB 累计超限→413+全清理 ✅；单片超 64MB→413+删除 ✅；finish 防御分支 ✅
- **线上全链路**：真实 mp4 切 2 片 → hanyuxz.top 上传 → finish → 转码 completed → 下载产物 h264+aac 完好 ✅
- 回归：test_upload_convert 7 passed（test_merged_modules_verify 4 failed 为改动前既有环境问题：conftest 先 import app 致模块开关 env 失效，与本次无关）
- /api/nodes 线上确认：`max_upload_bytes=10000000000`，targets 23 种含 hevc/wma/mp2

### 当前线上状态（2026-08-23 01:10）
- 分支：web-dev HEAD=`5aa1755`（origin 已同步）；线上 git_sha=5aa1755
- 转换：**23 种**输出格式；上传上限 **10GB**；大文件自动分片 4 路并发上传
- 平台 116、VPS daemon 13 worker、设备隔离/无声修复/反馈 均已生效（见第 6 章）

### 待办（承接第 6 章，未变）
1. 用户反馈处理（downloads/.feedback.json）
2. Tubi / Rumble 住宅代理验证
3. 直播恢复确认（每周一自动化）
4. main↔web-dev 合并同步（web-dev 已领先 main 大量提交）
5. 桌面 App 重构建（app-dev 需同步设备隔离 + 本轮分片上传/新格式改动）
