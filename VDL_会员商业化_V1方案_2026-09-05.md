# VDL 会员商业化 V1 方案（照搬 DataTool.vip 三轨结构）

> 决策：2026-09-05，用户定「直接套用，后面根据测试后针对性修改」。
> 本文件是 V1 的唯一真源。实现与测试见 `server/membership.py`、`server/routers/membership.py`、`server/tests/test_membership.py`。
> 后续任何档位/积分/捆绑调整，先改本文件再改代码。

---

## 0. 为什么照搬 DataTool

DataTool.vip 已被验证的三轨结构，直接映射到 VDL 现有人群（视频下载/去水印/抠图/AI 解说用户）：

| DataTool 产品 | VDL 对应能力 |
|---|---|
| 在线视频解析下载（网页） | 本地 119 平台解析下载（app 核心） |
| 插件批量（评论/数据/字幕） | 视频解说管线 / 字幕提取 / 批量任务 |
| AI 字幕识别/字幕提取/视频总结 | AI 解说 / ASR / 字幕 / 智能抠图引导 |

本方案保留 DataTool 的三个核心机制：
1. **下载会员 = 长周期订阅**（30/180/365 天，年付 50% off）——对应 VDL 下载/解析权益
2. **AI 会员 = 月订阅 + 积分池，且自动捆绑下载会员权益**——对应 VDL 全部 AI 能力
3. **永久积分包 = 纯按次计费，不与订阅绑定**——给低频/白嫖用户兜底

---

## 1. 档位与价格（V1 直接套用 DataTool 数值，测试后调整）

### ① 下载会员 `download_member`

| plan | 价格 | 时长 | 折算月价 | 权益 |
|---|---|---|---|---|
| `download_month` | ¥29.80 | 30 天 | ¥29.8 | 全部下载权益 |
| `download_half_year` | ¥99.90 | 180 天 | ¥16.7（省44%） | 同上 |
| `download_year` | ¥179.00 | 365 天 | ¥14.9（省50%） | 同上，标注「最受双迎」 |

下载权益（套用 DataTool 表，字段化实现，先不接功能门禁）：
- 视频解析配额 / 日；原画解析配额 / 日；批量素材配额 / 日
- 评论 / 数据 / 字幕批量：不限
- 高速通道（本地不限速语义）、优先客服

### ② AI 会员 `ai_member`（**自动含下载会员全部权益**）

| plan | 价格 | 时长 | 一次性到账积分 |
|---|---|---|---|
| `ai_5500` | ¥49.90 | 30 天 | 5500 |
| `ai_15000` | ¥99.90 | 30 天 | 15000 |

- 积分有效期 = 本次 AI 会员结束；不续费则清零
- 无「纯 AI 不含下载」档位（阶梯捆绑，拉高 ARPU）
- AI 会员暂只做月付，不做半年/年（避免年付锁定 AI 成本）

### ③ 永久积分包 `credit_pack`（无订阅）

| pack | 价格 | 积分 | 有效 |
|---|---|---|---|
| `credits_5000` | ¥50.00 | 5000 | 永久 |
| `credits_15000` | ¥99.00 | 15000 | 永久，标注「最受欢迎」 |

消耗顺序：**先扣 AI 订阅积分（快过期），扣完再扣永久积分**。

---

## 2. 数据模型（`~/.video-downloader/membership.json`）

```json
{
  "download_member": {
    "active": true, "plan": "download_half_year", "expire_at": 1789...
  },
  "ai_member": {
    "active": true, "plan": "ai_5500", "expire_at": 1789...,
    "grant_credits": 5500, "credits_left": 5500
  },
  "permanent_credits": {"total": 15000, "packs": [{"pack": "credits_15000", "amount": 15000, "bought_at": 1789...}]},
  "daily_usage": {"date": "2026-09-05", "resolve": 12, "original": 3, "batch_material": 40},
  "meta": {"activated_at": ..., "last_plan": ..., "history": []}
}
```

字段语义：
- `expire_at` = unix 秒。激活时 `max(now, 当前到期) + 时长`（续费顺延，不吞已有天数）
- `daily_usage.date` 非当日时自动重置计数（惰性日切）
- 时间全部注入（`now_fn`），可测

---

## 3. 落地范围与里程碑

### V1（本次实现，代码已提交）
- [x] `server/membership.py`：纯逻辑引擎（套餐表/状态机/激活/积分/配额），无外部依赖
- [x] `server/routers/membership.py`：`/api/member/*` 路由（状态查询/套餐列表/激活/积分花费/配额使用）
- [x] `server/app.py` include_router 挂载
- [x] `server/tests/test_membership.py`：纯单元测试全过（不依赖网络/二进制）
- [x] 本方案文档

### V1 明确不做（等真实测试数据后针对性改）
- ❌ 真实支付渠道（微信/支付宝商户）—— V1 激活走测试激活码接口
- ❌ 任何现有功能的会员门禁（不动下载/去水印/抠图现有行为，避免回归）
- ❌ UI 会员中心面板（等引擎稳定后再加，避免「新增子面板必须同步 index.html+app.js」的连锁改动）
- ❌ 云端 server（web-dev）侧接入

### 后续（测试反馈后再改）
1. 用真实用户样本确定哪些功能真正产生成本/值得门禁（解析？云端 AI？）
2. 定价验证：¥29.8 起点、AI 捆绑、积分包销量
3. 激活码签发与验签（离线 RSA/HMAC）
4. 会员中心 UI + 各功能门禁接入（逐项最小化，先截图对齐）

---

## 4. 测试方法

```bash
cd /Users/suixindelang/WorkBuddy/video-downloader-app
.build_venv/bin/python -m pytest server/tests/test_membership.py -v
```

覆盖：套餐表结构 / 激活顺延 / AI 会员自动含下载权益 / 积分先订阅后永久 / 到期清零 / 日配额惰性重置 / 状态公开 dict。

---

## 5. 待验证假设（写进测试清单）

- [ ] 年付 50% off 是否真能推动转化（DataTool 数据支持，VDL 待测）
- [ ] AI 会员 ¥49.9/5500 积分：单次 AI 调用（ASR/字幕/视觉引导）建议消耗 1-50 积分，具体需成本核算
- [ ] 免费用户是否需要硬配额（当前设计：免费无配额表 → 不设卡，先观察）

---

## 6. 功能接入点清单（会员介入条件）—— 2026-09-05 拟定，待逐项测试

> 原则：**无第三方成本的本地能力不设卡**（卡了伤口碑）；设卡只针对「高成本 / 高级输出 / 批量 / 云端资源」。
> 档位语义：🆓 免费 / 👑 下载会员 / 🤖 AI 会员（含👑）/ 🎁 积分（AI 订阅积分或永久积分均可扣）。
> 消耗点默认走 `use_daily(resource)`（👑 日配额）或 `spend_credits(n)`（🤖/🎁 AI 积分）。

### 6.1 下载 / 解析（core 域）
| 接入点 | 档位 | 触发条件 / 门槛 | 配额 |
|---|---|---|---|
| 常规平台解析下载（本地 yt-dlp） | 🆓 | 免费日次数上限内 | resolve 免费 10/日（2026-09-05 定稿） |
| 免费超限后的继续解析 | 👑 | 达到免费上限 → 会员墙 | resolve 1000/日 |
| 原画 / 4K / 高码率直链 | 👑 | 清晰度 >1080P 判定 | original 100/日 |
| VPS worker 平台（14 平台需转发） | 👑 | 请求落到 VPS 转发 | 并入 resolve |
| 批量解析 / 批量下载并发 | 👑 | 链接数 >5 或并发 >2 | batch_material 1000/日 |
| 批量导出 评论/数据/字幕 | 👑 | 导出按钮 | 不限 |
| 共享 Cookie 池优先通道 | 👑 | 命中需登录平台 | 不限 |
| 高速代理链路 | 👑 | 开启加速 | 不限 |

### 6.2 去水印（dewatermark 域）
> ⚠️ 语义澄清（2026-09-05 修正）：视频去水印的 `resolution` 档（720/1080/480）**只控制 AI 推理计算量**
> （先缩到该短边推理、再上采样回原尺寸合成，输出恒为原分辨率、非水印区零改动，720P 推理→1080P 输出实测像素差 ~1）。
> 因此**不存在"免费给 720P 输出"这堵墙**——本地推理不设清晰度墙，设卡只针对「输入复杂度/资源占用」：
> 4K/超高清输入、超长视频、并发占用。图片 LaMa 同理：>2048px 是输入复杂度门槛（推理显存/时间），不是输出降级。
| 接入点 | 档位 | 触发条件 | 消耗 |
|---|---|---|---|
| 图片去水印（本地 LaMa） | 🆓 | 输入短边 ≤2048px | 免费 |
| 图片去水印 超大输入（>2048px / 4K 源图） | 👑 | 输入尺寸判定 | 不限 |
| PDF 去水印 | 🆓 | 常规页数（≤20 页免费） | 免费 |
| PDF 批量页数超限 | 👑 | 页数 >20 | 不限 |
| 视频去水印（输入 ≤1080P 且 ≤5 分钟） | 🆓 | 输入分辨率与时长双判定 | 免费 |
| 视频去水印 4K 输入 / 长视频 / 高并发 | 👑 | 输入 >1080P 或时长 >5min 或队列并发超限 | 不限 |

### 6.3 一键抠图（matting 域）
| 接入点 | 档位 | 触发条件 | 消耗 |
|---|---|---|---|
| 本地 BiRefNet 基础抠图 | 🆓 | 常规分辨率 | 免费 |
| 本地高清 / 原尺寸导出 | 🆓 | 本地算力，不设卡 | 免费 |
| 云端高质抠图（MediaKit 官方额度） | 🎁 | 选择「云端模式」且未带自配 Key | ~3 积分/次（按 0.01~0.03 元成本核算） |
| 带自配 Key 的云端抠图 | 🆓 | 使用用户自带 Key | Key 成本归用户 |
| Real-ESRGAN 超分 x2 输出 | 🎁 | 勾选「超分输出」 | ~8 积分/张（建议） |
| AI 视觉定位（qwen-vl 引导） | 🎁 | 开启「AI 视觉定位」且未带自配 Key | ~2 积分/次 |
| closed-form matting 精修 | 🆓 | 本地后处理 | 免费 |

### 6.4 视频解说 / 字幕（commentary + subtitles 域）
| 接入点 | 档位 | 触发条件 | 消耗 |
|---|---|---|---|
| 本地音频转写（无 Key 离线档） | 🆓 | 未配置模型 Key | 免费 |
| AI 解说脚本生成（官方额度） | 🤖 | 生成分镜/脚本 | 按成片计（建议 50~200 积分/条，含多步） |
| AI 配音（官方 TTS） | 🤖 | 选择云端配音引擎 | 按音频时长计（建议 10 积分/分钟） |
| 片头片尾 / 集数卡视觉检测 | 🤖 | 触发 VLM | 并入成片包 |
| 字幕提取 | 🆓 | 常规视频 | 免费 |
| 字幕翻译 | 🎁 | 翻译按钮 | 1~3 积分/条（建议） |
| AI 字幕识别（ASR） | 🎁 | 识别按钮 | 2~5 积分/分钟（建议） |
| 字幕烧录 / 封装 | 🆓 | 本地 ffmpeg | 免费 |
| 解说历史保留（云端） | 🤖 | 超过免费保留条数 | 不限 |

### 6.5 条件判定清单（后端统一做）
1. 任一接口先查 `member.status()`：无任何会员 → 走免费档判断；有 👑/🤖 → 走配额或免卡。
2. 配额类：`quota_state(resource)` 先判免费额 vs 会员额；`use_daily` 扣减；超限返回业务码 `MEMBER_QUOTA`。
3. 消耗类（AI 积分）：`spend_credits(n)` 前置校验，不足返回 `MEMBER_CREDITS`。
4. 前端：收到 `MEMBER_QUOTA` / `MEMBER_CREDITS` → 打开会员中心对应 tab 并定位到相关套餐（自动引导）。
5. 用户自带 Key 的云端调用一律视为「自带料加工」，**不扣会员额度**（代码不变、用户自负 Key 成本）——这是与 DataTool 最大的差异点（DataTool 无自带 Key 概念）。

### 6.6 待拍板 / 待测（写进测试计划）
- [ ] 免费解析 10/日、图片 ≤2048px、PDF ≤20 页、视频 ≤1080P&≤5min 这些**免费门槛数值**是否合理（先用较宽值观察付费点）
- [ ] MediaKit/超分/VLM 的单次积分定价（按真实成本 ×3 毛利测算）
- [ ] 「自带 Key 不扣费」是否会造成会员无人买（若测出问题 → 改为自带 Key 也按调用抽成积分，或官方额度独享高级模型）
- [ ] 解说成片包 50~200 积分是否覆盖住 ASR+脚本+配音 的多次云端调用成本

### 6.7 修订（2026-09-06 用户拍板：配额墙从解析挪到下载）
- **解析（/api/resolve）不再设配额墙**：可随意解析预览、查看全清晰度。
- **下载（/api/download、/api/batch）设墙**：免费 10 次/日（发起下载任务计 1 次），
  下载/AI 会员 1000 次/日；任务成功创建才计费，失败不烧；超限 402 + `MEMBER_QUOTA|`，
  前端在「点击清晰度下载」处弹会员中心引导（不再在解析时打断）。
- 引擎配额键：`resolve` → `download`（daily_usage 相应迁移；老状态文件惰性兼容）。
- 下载权益文案统一为「下载任务 1000 次/日」。原画/4K 与批量配额键保留未接入（后续逐项）。
