# app 端三大功能实测与稳定性分析报告

> 2026-08-25 05:30 ｜ app-dev @77ee20c（三大功能同步后）｜ 测试环境：macOS 沙盒 + 源码 uvicorn + TestClient + VPS 线上链路

## 一、实测结果

### 1. 下载功能（9 平台抽样）
| 平台 | 链路 | 结果 | 耗时 |
|---|---|---|---|
| 抖音 | VPS 转发 | ✅ Douyin 直链 | ~20s |
| B站 | yt-dlp | ✅ BiliBili 多清晰度 | 快 |
| 快手 | VPS 转发 | ✅ Kuaishou 直链 | ~20-40s |
| 微视 | VPS 转发 | ✅ WeiShi 直链 | ~15s |
| 爱奇艺 | VPS 转发 | ✅ Iqiyi m3u8 | ~17s |
| 红果 | VPS 转发 | ✅ 直链（重试后成功） | **17.6s** |
| 小红书 | yt-dlp | ✅ XiaoHongShu 多清晰度 | 4.5s |
| YouTube | yt-dlp+PO | ⚠️ 沙盒 SIGKILL（环境限制；web 线上已验证 OK） | — |
| 优酷 | 本地/浏览器 | ⚠️ 沙盒未测成（web 线上 UPS 通道已验证） | — |

**结论**：核心下载链路健康，VPS 转发链路（app→线上 /v1/resolve→隧道→VPS daemon）7 平台全通。

### 2. 视频处理
| 项 | 结果 |
|---|---|
| 转码（upload-convert） | ✅ status=completed，6s（2s 测试视频） |
| concat 无损拼接 | ✅ 22003 字节 = 2×11402，时长 3.86s 正确 |
| ffmpeg | ✅ 8.1.2 可用 |
| 分片路由 | ✅ /api/upload-chunk 三件套已注册（422 校验通过） |

### 3. 去水印
| 项 | 结果 |
|---|---|
| 掩码顺序无关 | ✅ subtract 先于 add 仍正确挖洞（本次修复生效） |
| 图片 inpainting | ✅ telea 填充水印区（mean=246，文字清除） |
| 依赖 | ✅ build venv 含 cv2 + pymupdf（打包环境完整）；默认 venv 无 cv2（仅测试环境） |

## 二、不稳定因素清单与解决方案

### 🔴 P0-1：VPS 配置一次性注入，更新不生效
- **现象**：`_ensure_vps_env()` 只在 app 启动时注入 env（app.py:2934）。运行中若 cloud_sync.json 更新（token 轮换/URL 变更），**运行中的 app 不会热更新**，VPS 链路持续 403 直到重启 app。
- **实锤**：本次实测期间更新 cloud_sync.json token 后，运行中的打包 app（8321）微视/红果仍走 yt-dlp 兜底（旧 env），源码新进程正常。
- **方案**：`_call_vps_worker` 遇 401/403 时重读 cloud_sync.json 更新 token 重试一次（自动自愈）；env 注入加 5 分钟 TTL 重读。

### 🔴 P0-2：慢 VPS 请求占满 uvicorn 线程池
- **现象**：红果 17.6s、快手 20-40s 的慢解析占线程；实测批量请求时后续请求排队/超时（115s 无响应），uvicorn 线程池（默认 40）耗尽后新请求 502。
- **方案**：a) 同平台解析串行化（进程内锁，复用 VPS daemon 的 8s 锁语义）；b) resolve 端点加硬超时（如 100s，超时返回可读错误）；c) 前端已有 120s fetch 超时兜底（web 已加，app 前端同代码已带）。

### 🟡 P1-1：web /v1/resolve 转发端点无限流
- **现象**：只有 token 校验，无频率限制——token 泄漏或被滥用时可任意消耗 VPS worker 资源。
- **方案**：加按 IP 的简单限流（如 60 次/小时，超过 429）。

### 🟡 P1-2：3 个已知测试失败（web/app 共有 mock 缺陷）
- **现象**：test_download_phases ×2、test_friendly_error_403 ×1——`_FakeProc` 缺 stdout、`_worker_http` session 未 mock，非功能问题但 CI 不干净。
- **方案**：补 mock 修复测试，恢复全绿。

### 🟡 P1-3：cookie 池空 WARNING 日志噪音
- **现象**：每次 resolve 打印 10+ 条"公共 Cookie 池已空" WARNING，淹没真实错误。
- **方案**：降为 DEBUG 或合并摘要（每 10 分钟一条）。

### 🟢 P2-1：打包 app 端口占用
- **现象**：dist app 固定 8321；与源码 server 冲突（本次实测撞端口）。
- **方案**：确认 app 端口顺延逻辑生效（build 脚本声称支持）。

### 🟢 P2-2：YouTube/优酷重资源解析
- **现象**：沙盒 SIGKILL（资源限制）；低配 Mac 上 yt-dlp YouTube 解析可能吃内存。
- **方案**：真实 GUI 环境无沙盒限制，低优先；可在设置加"国际平台解析降级"开关（可选）。

## 三、建议实施顺序
1. P0-1 env 热更新自愈（app-dev，本次即可做）
2. P0-2 resolve 硬超时 + 同平台串行（app-dev）
3. P1-1 转发端点限流（web-dev，需你确认动 web）
4. P1-2 测试修复（app-dev）
5. P1-3 日志降噪（app-dev）
