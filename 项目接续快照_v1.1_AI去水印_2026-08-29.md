# VideoDownloader (VDL) 项目接续快照 v1.1 · AI 去水印桌面端

> 基线：v1.0.1-stability（2026-08-24，commit 0ba06b9，Railway git_sha=0ba06b9）
> 本轮更新：2026-08-28 ~ 08-29（AI 去水印桌面端移植 + frozen 死局修复 + 7 个连锁 bug 修复）
> 当前 HEAD：
> - **app-dev = bd56616**（桌面构建线，本次工作重点）
> - **web-dev = 6a7d088**（线上部署线，Railway hanyuxz.top）

---

## 1. 当前项目原始需求

**总目标**：做一个跨平台视频下载 + 处理工具（App 桌面端 + Web 网页版），两端代码各自维护、功能上 App 覆盖 Web。

- **下载**：粘贴链接 → 解析真实直链/多清晰度 → 下载。支持抖音/B站/腾讯/微视/快手/爱奇艺/1905/小红书/红果/微博等国内平台 + YouTube/TikTok 等（119 平台清单）。
- **Web 端固定 4 大 tab**：下载 / 视频处理 / 图片·PDF去水印 / 更多功能介绍（不可改）。
- **桌面端保留 9 tab**：下载 / 媒体库 / 视频解说 / 视频格式转换 / 去水印 / 订阅追更 / 种子 / 百度网盘 / 百度下载（桌面独有能力不丢）。
- **跨平台 Cookie 共享池**：用户自愿贡献登录态到公共池，访客复用（仅登录态不含密码）。
- **VPS Playwright worker 体系**：国内反爬平台由国内 VPS 真实浏览器解析，Railway 经反向隧道调用。
- **AI 图片去水印（本轮重点）**：上传图片 → 框选水印区域 → LaMa ONNX 模型无痕修复 → 输出去水印图。Web 端已上线，桌面端 8/28~29 完成移植并修通 7 个连锁 bug。

---

## 2. 已经完成的全部工作成果

### 2.1 v1.0.1 稳定性基线（2026-08-24，已全部上线，详见旧快照）
- 10 平台真实直链实测通过；9 轮巡检 + 7 处加固（cookie 推送重试 / 前端 fetch 超时 / VPS profile 清理 / daemon 锁超时 / yt-dlp 自动更新+持久卷 / VPS 隧道开机自启）已上线；git tag v1.0.1-stability=0ba06b9。

### 2.2 Web 端 AI 去水印上线（web-dev，2026-08 早些时候）
- `9fa367d` feat：图片去水印接入 LaMa AI 无痕修复（onnxruntime，免 torch）。
- `b4ac826`/`a31f0e2`/`843de1f`/`6d525e1`：内存护栏（Railway 免费实例 OOM 防护）+ 子进程隔离 + 模型完整性校验。
- `6a7d088`：frozen 桌面端同进程推理分支 + 模型下载加超时（与 app-dev 同源改法）。

### 2.3 桌面端 AI 去水印移植（app-dev，本轮）
- `4e37dc6` feat：桌面端图片 AI 无痕去水印（LaMa ONNX）移植。
- `398c8f7` fix：桌面端 frozen 同进程推理 + 模型下载加超时（修复"AI 去水印未产出有效文件"死局——原 subprocess 在 PyInstaller frozen 下起不来）。

### 2.4 桌面端重启验证中连锁修复的 7 个 bug（app-dev，全部已 commit+push+重打包+ditto 覆盖 /Applications）
| # | 现象 | 根因 | 修复 commit |
|---|---|---|---|
| 1 | AI 去水印结果整图纯白 (255,255,255) | LaMa `lama_fp32.onnx` 输出范围 **[0,255]**，代码误当 [0,1] 执行 clip(0,1) 把 230 压成纯白 | `a9a4ad9` 改为 `clip(out/255.0)` |
| 2 | 首次跑 AI 报 `<urlopen error timed out>` | 模型首次下载 `socket.setdefaulttimeout(60)` 不够拉完 107~198MB | `8b91b26` 超时 60s→**300s + 重试一次** |
| 3 | 去水印完成但前端「看不到成品」 | `.dw-result` 在 `.dw-layout` grid 之外、落在视口外，完成只 hidden=false 没人滚动 | `ad83ad7` 完成后 `scrollIntoView({block:'center'})` |
| 4 | 「🔍 放大编辑」点不了 | `.dw-modal` 灯箱**完全缺 CSS 样式**（无 position:fixed 覆盖层）→ 弹窗在文档流外不可见；且打开前置校验过严 | `e17639f` 补全套 `.dw-modal` 样式 + 放宽 `hasImg` 校验 |
| 5 | 处理后对比图只显示上半截 | `.dw-compare img { height:260px }` 强制固定高度 + object-fit contain 居中，大图只露上半 | `f68df74` 改 `width:100%; max-height:60vh; height:auto` |
| 6 | 灯箱放大只看到左半图，右半空白 | `.dw-preview-wrap { max-width:100% }` 截断 wrap + flex-shrink 把超出的右半裁掉 | `78c5d85` `.dw-modal-body .dw-preview-wrap { max-width:none; flex-shrink:0 }` |
| 7 | 「加选」模式拖动 = 移动图片而非画框 | ① 原生 img draggable 触发 native drag ② mousedown pan 逻辑反直觉（命中已有选区即 pan） | `bd56616` `draggable=false`+`dragstart` 拦截 + 仅显式「移动」模式 pan |

- **模型预下载**：`~/.vdl_models/lama_fp32.onnx`（198MB，经 hf-mirror.com 下载，≥100MB 校验），首次 AI 跑直接复用秒出。
- **验证**：红矩形 (200,30,30)→(226,226,226) 修复，背景 (230)→(230) 保持，`AI DEWATERMARK OK=True`；灯箱/对比/放大/加选均修复。
- **环境**：每次修复均 `bash desktop/build_mac.sh`（COMMENTARY_PIPELINE_DIR="/Users/suixindelang/WorkBuddy/问问题/commentary-pipeline"）→ `.build_venv`(PyInstaller 6.22.2) → `ditto dist/VideoDownloader.app /Applications/VideoDownloader.app` → 重启。

---

## 3. 当前进行到哪一步 + 未完成待办

### 当前状态
- **app-dev bd56616 运行中**（端口 8321，原生窗口正常），全部 7 个修复已生效，用户可正常走「上传图→框选→AI 去水印→放大加选→下载」。
- 桌面端 AI 去水印已端到端可交付。

### 待办清单
- ⏸ **[待用户拍板] web-dev 同步 `/255` 修复**：web-dev 的 `dewatermark_ai.py` 有与 app-dev 完全相同的 `[0,255]→[0,1]` bug（同代码；Railway OOM 降级一直没触发 AI 推理故未暴露）。按铁律动 web 端需先问用户是否同步。→ 若同步：改 web-dev `dewatermark_ai.py::_infer_tile` 归一化 + commit + push + 轮询 Railway `git_sha`。
- ⏸ **[可选] web-dev 模型下载超时** 仍 60s（app-dev 已改 300s，见 8b91b26）。如需 web 端首跑也稳，一并同步。
- ⚠ **launcher 偶发卡死**：重打包后进程写锁 + 60s 后端未就绪时 launcher 偶发卡死，沙盒内 `pkill -9` 杀不净。**兜底：让用户 Force Quit（Cmd+Opt+Esc）再 open 更稳**。非代码 bug，属已知环境现象。
- ❌ 微信视频号（判定不可行，代码保留不维护）；优酷本地引擎已下架（app-dev de220ee）；其余 v1.0.1 已关闭待办勿主动拾起。

---

## 4. 关键约束、修改要求、注意事项

### 铁律（用户明确，务必遵守）
- **工作重心默认 app 端（app-dev）**；只有用户明确说「干 web 端的活」才转 web-dev；特殊情况需转 web 端**必须先询问用户**获得同意。
- **两条支线独立，绝不 merge**（曾误 merge 被纠正回滚）。功能覆盖靠按需 cherry-pick / 手工移植，绝不双端全量同步。
- **App 禁止任何自动跳浏览器行为**；唯一允许打开浏览器：百度网盘 OAuth 授权页（用户主动点登录）。原生窗口打不开只写日志+系统弹窗，绝不自动跳浏览器。
- **用户不执行任何命令行运维**（SSH/systemctl/改文件等）——AI 自己端到端干（commit/push/部署轮询/ssh VPS 全由 AI 执行）。
- **真实密钥/凭据必须用户手动复制粘贴文本**，绝不从截图 OCR（I/l、O/0 易抄错）。
- 部署自查：push web-dev 后轮询 `https://hanyuxz.top/api/version` 的 `git_sha` 确认 Railway 自动部署生效，不等用户确认。

### 技术约束
- **仓库/工作树布局**：
  - 主仓库 `video-downloader`（= web-dev 分支）：`/Users/suixindelang/WorkBuddy/video-downloader`
  - 桌面工作树 `video-downloader-app`（= app-dev 分支）：`/Users/suixindelang/WorkBuddy/video-downloader-app`
  - **桌面构建必须在 app-dev 目录跑**，否则打出来的 app 缺去水印多选区等仅 app-dev 改动。
  - `commentary-pipeline` 是软链指向真实管线源码（`问问题/commentary-pipeline`）；`commentary-worker` 是调研误判副本，别改错。
- **PyInstaller frozen 模式**：`sys.executable` 是 launcher 二进制；`web/` `server/` 冻结进 bundle。改前端/后端**必须重打包**才进二进制；验证：`grep -c dwSelections dist/VideoDownloader.app/Contents/Resources/web/app.js`（多选区应为 ≥11）。
- **构建命令**：`COMMENTARY_PIPELINE_DIR="/Users/suixindelang/WorkBuddy/问问题/commentary-pipeline" bash desktop/build_mac.sh`；`.build_venv` 需 PyInstaller 6.22.2（曾损坏，force-reinstall 修复）。
- **安装/重启**：`ditto dist/VideoDownloader.app /Applications/VideoDownloader.app`；`pkill -9 -f "VideoDownloader/Contents/MacOS/VideoDownloader"` + `rm -f ~/.vdl_instance.lock` + `open`；端口 8321。launcher 偶发卡死→用户 Force Quit。
- **模型**：`~/.vdl_models/lama_fp32.onnx`（198MB，hf-mirror.com 下载，≥100MB 校验）。LaMa 输出 **[0,255]**，归一化必须 `/255.0`（这是本轮踩过的最深坑）。
- **AI 去水印前端文件**：`web/app.js`（dwOpenModal/dwApplyZoom/dwDrawMode/dwSelections/dwModalZoom）、`web/styles.css`（.dw-layout/.dw-result/.dw-modal/.dw-compare/.dw-preview-wrap）、`web/index.html`。后端：`server/dewatermark_ai.py`（_infer_tile / _ensure_model）。
- **VPS**：`ssh root@8.138.223.3` 直连；daemon 由 systemd `vdl-cookie`(Restart=always) 守护；三服务 vdl-cookie/cn_proxy/cn_tunnel_client 全 enabled；worker 改完 scp 才生效。
- **Railway 持久卷** `RAILWAY_VOLUME_MOUNT_PATH`：cookie 池 + yt-dlp 自动更新共用。

### 已踩坑·经验（防再犯）
- **LaMa 输出范围 [0,255]**（不是 [0,1]）——任何归一化都要 `/255.0`，否则整图纯白。
- **灯箱/弹窗 CSS 缺失**会导致「按钮点不了」假象——先查该 class 是否有样式，别只盯 JS。
- **固定 height 的 img** 配 object-fit contain 会裁掉大图下半——用 `max-height + height:auto`。
- **flex 子项默认 flex-shrink:1** 会在主轴被收缩裁图——放大场景加 `flex-shrink:0; max-width:none`。
- **原生 img draggable=true** 会触发 native drag 抢走 mousedown——需 `draggable=false` + 拦截 `dragstart`。
- **Pydantic v2 + PEP 563（from __future__ import annotations）**：`Any` 字段会被当字符串，TypeAdapter 报错——用具体 Union 而非 Any。
- **faster-whisper VAD 过激**会判整段非语音→转写 0 段→"转写稿为空"；`min_silence_duration_ms` 500→1500 + VAD 0 段 fallback 禁用重试 + `WHISPER_VAD=0` 可关。

### 平台真相
- **微信视频号：网页版技术不可行**（客户端协议鉴权非 HTTP Cookie；Mac 微信 v4 移除开发者工具；本地 cookie 私有密钥加密）。替代：微信内转发文件传输助手长按保存/录屏。
- 微视可用（H5 无水印直链 + Playwright 兜底）；微信视频号字段保留在 cookie_pool 但不再维护。
- 部分平台直链有时效（快手几分钟、爱奇艺 m3u8 短时），拿到尽快下载。

### 目录/仓库
- 远程：`github.com/leydall0999-cell/video-downloader`（web-dev=部署线，app-dev=桌面线）
- 工作目录：`/Users/suixindelang/WorkBuddy/video-downloader`（web-dev）/ `video-downloader-app`（app-dev）
- 历史快照存档：`/Users/suixindelang/WorkBuddy/video-downloader/项目接续快照_*.md`；完整 v1.0.1 见 `项目接续快照_v1.0_2026-08-24.md`。
