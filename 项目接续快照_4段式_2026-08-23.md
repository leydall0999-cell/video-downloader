# VDL 续接快照（4 段式，可直接复制到新会话）— 2026-08-23 21:22 实测核对

> 用法：整段复制到新会话，AI 即可无缝续接。当前分支 `web-dev`（网页精简版），域 `hanyuxz.top`（Railway 自动部署）。

---

## 1. 原始需求
自托管视频下载/媒体备份工具（FastAPI + yt-dlp + 原生前端）。**本次核心需求（用户原话）**：「App 及网页端能够互相作用，把 Cookie 推到云端共享池，解析时自动复用，过程不用问我，解决完告诉我」。即：**桌面 App 与网页端互相把登录态 Cookie 推到同一个云端共享池，解析时两端都能自动复用，不需手动粘贴。**

## 2. 已完成成果（线上已部署，git_sha=74cc95a）
- **去水印 UI 全套打磨**：已 commit（`37f54bb`，灯箱拖动平移/居中/选区并集等）；线上 HEAD 现已被 Cookie 修复覆盖为 `74cc95a`。
- **Cookie 共享池根因修复（已 commit + push + 部署，`74cc95a`）**：优酷/腾讯视频等站 Cookie 在 `/api/cookie/sync`、`/api/cookie/contribute` 入池前被 `verify_cookie` 用 `_verify_generic`（yt-dlp 试解析会员内容报 -3007）拒收。修复点：
  - `server/cookie_pool.py`：新增 `_STRUCTURAL_FIELDS`（youku.com→`P__yk__uck,yktk,cna,x5sec,sess_vkey,unb`；v.qq.com→`vus_session,video_platform,uid_tt,login_ecookie`）+ `_structural_check`；`verify_cookie` 改为「仅明确 False 才拒，None 放行」。
  - `server/app.py`：两入池端点拒绝文案细化（提示需含 `P__yk__uck`/`vus_session`），逻辑保持 `ok is False` 才拒。
  - `web/app.js`（`contributeCookie`）：fetch + 状态分流（200/400/429/其他），不再静默 catch。
- **本地验证已确认**：优酷结构 Cookie → `/api/cookie/contribute` 返回 200 + `added=True`；二次贡献 429 限频；`get_cookie('youku.com')`=HIT 且落盘；临时脚本 `verify_youku_pool.py` 已清理。线上 Cookie 池现含 `bilibili.com.json`。

## 3. 当前进度 + 待办
- **`[5]` 根因已查清（2026-08-23 21:4x）**：**不是代码 bug，是上次验证方法写错**——上次测 `_base_options("https://v.youku.com/...", ...)` 把整条 URL 当成了第 1 个位置参数 `retries`，导致 `host=""`，`get_cookie("")` 自然为空。真实调用方（如 `downloader.py:2820/3018/3405`）都是 `_base_options(PROBE_RETRIES, host, cookie=..., proxy=...)`，`host` 作为第 2 位正确传入。已用正确签名 + 隔离池复现：**`host=v.youku.com` 时 Cookie 头正确注入**（`get_cookie("v.youku.com")` 候选含 `youku.com` → 命中 `youku.com.json`）；纯池路径（模拟 Railway 无浏览器缓存）`Cookie == pool header: True`。
- **新增诊断端点 `/api/cookie/inject-diag?host=v.youku.com`**（已 commit+push+部署 `1ba440c`）：按 `_base_options` 同口径报告会被注入的 Cookie 来源（cache/pool/miss）+ 脱敏长度。线上实测：bilibili → source=pool,len=1067（链路通）；youku → source=miss（因池里尚无优酷 Cookie，符合预期）。
- **✅ 用户已于 2026-08-23 21:53 提供真实优酷 Cookie（纯文本粘贴），我代贡献成功**（POST `/api/cookie/contribute` 返回 `{"ok":true,"added":true,"verified":true}`）。**`inject-diag?host=v.youku.com` 实测 `resolved.source=pool, len=1206`**（候选 `v.youku.com→youku.com` 命中 `youku.com.json`）——`[5]` 的 **Cookie 注入链路已 verified ready**。
- **✅ 优酷解析器已打通（2026-08-23 21:5x）**：进一步调研发现 yt-dlp 2026.7.4 **自带 YoukuIE**（非「未实现」），根因是其 `_VALID_URL` 仅匹配 `v/play/player.youku.com`，而当前优酷链接全是 `www.youku.com` → 落 generic → `pending_extractor`。修复：`probe` 里对 `www.youku.com`→`v.youku.com` 归一化（`server/downloader.py`，commit `d79b4b2`+`26afff4` 修正判断，`26afff4` 已 push+部署，线上 `git_sha` 一致）。线上实测：优酷 URL 从 `pending_extractor` 变为 `[youku] 接管`（测试 vid 返回 -1002「视频已被屏蔽」，证明确实走真实解析流程，非解析器缺失）。`[5]` 的 Cookie 注入经 `http_headers` 已对接 yt-dlp 请求栈，YoukuIE 调 `ups.youku.com` 自动带用户登录态。
- **⚠️ 端到端实测结果（2026-08-23 22:2x，用户给真实 URL `v.youku.com/v_show/id_XMjQ0ODk0Njg0`）**：线上 `POST /api/resolve`（cookie 空→走池注入用户 youku Cookie）仍返回 **`[youku] XMjQ0ODk0Njg0: Youku server reported error -3007: 观看此节目，请先登录！`**。`inject-diag?host=v.youku.com` 复检 `resolved.source=pool,len=1206`（Cookie 确实会被注入）。**本地 yt-dlp 对照实验：带/不带用户这条 Cookie 解析该 vid，结果完全一致（都 -3007）** → 证明：解析器接管 ✅、Cookie 注入 ✅，但**用户贡献的这条优酷 Cookie 未通过优酷登录校验**（直连 `ups.get.json` 返回 `user.login=None,data.stream=False` 空壳）。`contribute` 时 `verified=true` 仅验格式/字段，不保证登录态真有效。
- **当前唯一阻塞 = 缺一份优酷服务端认可的有效登录 Cookie**：用户这条 Cookie 含 `juid`/`P_pck_rm`/`_m_h5_tk` 但**缺核心登录字段 `P__yk__uck`**，疑似复制自"半登录/游客态"标签页或漏了 session 字段。
- **下一步（需用户操作）**：在优酷网页**确认已真正登录**（右上角有头像/昵称）→ 用浏览器插件（Cookie-Editor / EditThisCookie）在 `youku.com` 页面「导出全部 Cookie」复制**完整字符串**（应比现这条更长、含 `P__yk__uck` 等）→ 重新发给我（或网页端重新贡献，会覆盖 `youku.com.json`）。届时我直接 `curl /api/resolve` 验证 -3007 消失。
- **建议**：当前这条无效 `youku.com.json` 在池里共享给别人也会 -3007（误导），要不要我先从池撤掉？告诉我即可（破坏性操作，先确认）。
- **与本次无关的长期待办（勿混）**：会员制限流、CF Pro 升级、App 完美拼接、百度网盘端点、直播恢复。

## 4. 关键约束 / 代码位置速查 / 接手命令
- **用户不碰命令行**：AI 自运维（VPS `ssh root@8.138.223.3` 改文件/重启、push 后轮询 `/api/version` 自证部署、线上 curl 自查）。**只在 `web-dev` 开发**；**关键凭据绝不从截图 OCR**（须用户粘贴文本）。
- **部署铁律**：push web-dev 即 Railway 自动部署，无需 Redeploy、别问「部署好了吗」；唯一权威 = `/api/version` 的 `git_sha` == 刚 push 的 commit。
- **Cookie 池安全边界（不可破坏）**：仅白名单域（chrqj/bilibili/youku + hardened 派生 + env `VDL_COOKIE_POOL_DOMAINS`）；入池仍走白名单+限频+验真/结构校验+`VDL_COOKIE_ENC_KEY` 加密+TTL 30 天+后台 `verify_and_prune`；双份物理隔离（Railway 卷 `/data/cookie_pool` vs App 本机 `~/.videodownloader/cookie_pool`）；App→Railway 单向推送维持。
- **代码位置速查**（行号随提交漂移，用 grep 端点路径定位）：
  - 公共池 `server/cookie_pool.py`：`_BASE_DOMAINS` / `verify_cookie` / `_structural_check` / `get_cookie` / `add_cookie`
  - 端点 `server/app.py`：`/api/cookie/sync`、`/api/cookie/contribute`、`/api/cookie/sync/to-cloud`、`/api/cookie/pull-diag`
  - 自动取池 `server/downloader.py` 的 `_base_options`（用户未贴 Cookie 时 `get_cookie` 注入分支）
  - App 推送 `web/js/desktop-app.js` 的 `syncToCloud` → POST `/api/cookie/sync/to-cloud`
  - 网页贡献 `web/app.js` 的 `contributeCookie`
  - 强反爬域 `server/downloader.py` 的 `_COOKIE_HARDENED_DOMAINS`（含 youku.com/v.qq.com）
- **接手命令**（仅当用户已贡献真实优酷 Cookie 后验证用）：
  ```bash
  cd /Users/suixindelang/WorkBuddy/video-downloader
  git branch --show-current        # 应 = web-dev
  # [5] 已查清：非代码 bug，无需改 downloader.py。验证链路：
  curl "https://hanyuxz.top/api/cookie/inject-diag?host=v.youku.com"
  # 期望 resolved.source=pool, len>0；再解析优酷受限视频确认 -3007 消失
  # 部署自证：
  for i in $(seq 1 20); do sleep 10; curl -s https://hanyuxz.top/api/version | grep -o '"git_sha":"[^"]*"'; done
  ```
