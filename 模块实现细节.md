# 模块实现细节（AI 去水印 / 订阅监控 / 双节点分流 / 时效清理）

> 本文档对四个模块的实现机制做逐层拆解，含关键函数、参数、门控与兜底策略。
> 代码基准：`/Users/suixindelang/WorkBuddy/video-downloader`（commit `533b207`）。

---

## 1. AI 去水印调度（`ai_dewatermark`）

### 1.1 入口与职责边界
- 它是**加工队列**的一个 `op`，走 `/api/process/run` → `_process_run` → 后台线程 `_run_process`。
- 在 `_run_process`（`server/app.py:4303`）里，普通 op（audio/gif/trim/compress/upscale/frame/sheet/ringtone/dewatermark/frames）都是**同步 ffmpeg** 并在本函数内收尾；唯独两类例外：
  - `ai_dewatermark`（`:4398`）→ 调 `_run_ai_dewatermark(job_id, src, p)` 后 **`return`**，由 worker 异步收尾，本函数不写 job 终态。
  - `frames`（`:4401`）→ 产物是目录，单独收尾。
- 关键区别：**`dewatermark`（ffmpeg `delogo`）是固定矩形打码框**；**`ai_dewatermark` 是 E2FGVI 修复**，二者并存、互不复用。

### 1.2 调度函数 `_run_ai_dewatermark`（`server/app.py:4183`）
参数取自 `params`：`x/y/w/h/band`（区域与修复带宽）、`mode`（默认 `auto`）。

**两种运行模式**（由 `VDL_AI_DEWATERMARK_MODE` 决定，默认 `local`）：

| 模式 | 行为 | 收尾 |
|------|------|------|
| `http` | `POST {AI_DEWATERMARK_ENDPOINT}/render`，body `{video,x,y,w,h,band,mode}`，带 `X-Worker-Token`（若设了 `VDL_AI_DEWATERMARK_TOKEN`）；拿到 `worker_job` 后每 5s 轮询 `/status/{worker_job}`，超时 `AI_DEWATERMARK_TIMEOUT`（默认 3600s）；完成后 `GET /file/{worker_job}` 下载成片 | 成片写入 `{stem}_AI去水印.mp4`，写侧车、登记媒体库 `lib_id` |
| `local`（默认） | `subprocess` 调 `VDL_AI_DEWATERMARK_DIR/process.py`（默认 `watermark-removal/process.py`），传 `--x --y --w --h --band --mode`，env 注入 `E2FGVI_BASE` | 同步完成，直接走收尾 |

HTTP 模式轮询终止条件：
- `status == "completed"` → 跳出下载成片；
- `status == "failed"` → `raise RuntimeError(st.error)`；
- 超 `deadline` 仍未完成 → `raise RuntimeError("AI 去水印超时")`。

异常统一在 `except`（`app.py:4266`）写 `job["status"]="failed"` + `error[:400]`，不会卡死队列。

### 1.3 门控与前端契约
- `AI_DEWATERMARK_ENABLED`（`app.py:271`）= `sys.frozen`（桌面版）**或** `VDL_AI_DEWATERMARK_ENABLED`。
- `AI_GPU_AVAILABLE`（`app.py:293`）= 仅当 enabled 时跑 `_detect_gpu()`（探测 `torch.cuda` / `torch.backends.mps`）。
- `/api/nodes`（`app.py:1464`）暴露 `ai_dewatermark: {enabled, gpu}`，前端据此显隐入口。
- **已知缺口**（前次核对发现，非阻断）：前端 `resetProcessPanel` 仅按 `kind` 过滤、该选项始终出现，未用 `node.ai_dewatermark.enabled` 门控；未配置 worker 时提交才失败。建议前端按 `enabled` 过滤或后端 `process_run` 提前 4xx。

---

## 2. 订阅监控（`subscriptions`）

### 2.1 持久化模型
- 仓库：`SubscriptionStore`（`server/subscriptions.py:39`），落盘 `<DOWNLOAD_DIR>/.subscriptions.json`。
- 线程安全：`RLock` + 写时 `tmp.replace()` 原子替换（损坏文件以空仓库启动，下次写入覆盖）。
- 数据类 `Subscription`（`subscriptions.py:20`）：`id/url/name/platform/quality_key/quality_label/cookie/proxy/auto_check/last_video_ids/last_checked/created_at`。

> 设计取舍：任务状态只存内存、重启即丢；但"我关注的 UP 主"是长期配置，必须落盘。探查用 `extract_flat`（只拿元数据、不下载），与下载内核解耦；新建下载由 `app.py` 走 `store.create + scheduler.submit`，**不经过免费配额**。

### 2.2 探查 `probe_channel`（`subscriptions.py:110`）
- yt-dlp `extract_flat=True` + `simulate=True` + `ignoreerrors=True` + `socket_timeout=30` + `retries=2` + `playlistend=limit`。
- 延迟导入 yt_dlp（避免无依赖时模块加载失败）；整体失败返回 `[]`，不抛异常（避免 watchdog 卡死）。
- cookie 处理：以 `cookie:` 前缀的去掉前缀取纯值，注入 `http_headers["Cookie"]`；支持 proxy。
- 返回每项 `{id, title, url(优先 webpage_url), uploader}`，按发布倒序最多 `limit` 条。

### 2.3 基线去重策略 `_run_subscription_check`（`app.py:4443`）
1. `probe_channel` 取最新视频；
2. 过滤 `id` 已在 `last_video_ids`（已知基线）内的项；
3. 新视频逐个 `parse_source` 校验平台 → `store.create` + `scheduler.submit` 加入下载队列；
4. **仅把"成功提交任务"的 id 并入基线**（最多保留 200 条）；解析失败的 id 不计入（下次 check 可重试）；
5. 更新 `last_checked`。

> 关键：基线只认"真提交成功"的视频，解析/下载失败的留待重试，避免漏更。

### 2.4 路由与门控
- `GET /api/subscriptions` → `{subscriptions, enabled}`（`app.py:4488`）；`to_public_dict` 原样回传（含 `cookie`/`proxy`，见前次核对"敏感字段回传"观察）。
- `POST /api/subscriptions`（`app.py:4493`）：`SUB_ENABLED` 否则 403；校验 quality；`parse_source` 校验为已知公开平台。
- `POST /api/subscriptions/check`、`DELETE /api/subscriptions/{id}`。
- 门控：`SUB_ENABLED`（开关策略同媒体库：桌面版/显式开启）；`SUBSCRIBE_PROBE_LIMIT=100`、`SUB_CHECK_INTERVAL=1800`（30 分钟，auto_check 用）。

---

## 3. 双节点分流（dual-node）

### 3.1 架构原则
- 国内 + 海外各部署一份实例，**各自直连目标站点、不跨境**；按域名分流，避免把请求绕到对端带来的延迟与合规风险。
- 后端 `/api/nodes`（`app.py:1405`）向前端广播分流所需三要素：
  - `region`：`VDL_REGION`（默认 `global`，国内节点设 `cn`）；
  - `peer`：`VDL_PEER_ENDPOINT`（对端地址，空则退化为单节点）；
  - `china_domains`：`CHINA_DOMAINS` 列表。
- 前端行为（见 `node_info` docstring）：粘贴链接时判断 host 是否国内站 → 国内站把请求发到 **cn 节点**，海外站发到 **global 节点**；`peer` 为空则退化单节点。

### 3.2 国内站点判定
- `CHINA_DOMAINS`（`server/platforms.py:120`）：bilibili/douyin/kuaishou/xiaohongshu/weibo/iqiyi/v.qq.com/youku/mgtv/zhihu/ximalaya/haokan/pearvideo/huya/douyu/yangshipin/cntv/cctv 等 **40+ 域名**。
- `is_china_host`（`platforms.py:146`）：命中 `CHINA_DOMAINS` 或后缀 `.cn` / `.com.cn` 即判国内（国内站直连更快、走海外代理反而超时）。

### 3.3 国内回源代理 `cn_proxy.py`（零依赖标准库）
- 用途：实例部署在海外（Railway）时，国内站被地理围栏拦截；把本代理跑在**物理位于国内**的机器上，公网暴露后，海外节点设 `VDL_PROXY_CN=http://user:pass@<公网>:端口`，yt-dlp 解析/下载国内站即经此回源。
- 实现：`ThreadingHTTPServer` + `BaseHTTPRequestHandler`：
  - `do_CONNECT`：HTTPS 隧道，建立 TCP 双向转发（非阻塞 + 发送缓冲，`BlockingIOError` 不当致命，否则大文件被掐断）；
  - `do_GET/POST/...`：HTTP 正向代理，代为请求并透传响应；
  - 逐跳头（`Connection/Proxy-Authorization` 等）按 `_HOP_BY_HOP` 剥离；
  - 可选 `CN_PROXY_AUTH=user:pass`：所有请求（含 CONNECT）须带 `Proxy-Authorization: Basic`，否则 **407**。
- 注意（用户实践）：阿里云机房 IP 会被 B站等站点封禁（412/截断），需**住宅 IP 兜底**——`cn_proxy` 应跑在住宅网络而非云机房。

---

## 4. 时效清理回收站兜底（`retention`）

### 4.1 五档策略（`scan` dry-run，`server/retention.py:414`）
单遍 `rglob` 分拣，跳过 `PROTECTED_NAMES`，`_safe_inside` 防越界：

| 档 | 识别 | 开关字段 | 条件 |
|----|------|----------|------|
| `temp` 临时碎片 | `_is_temp`（.part/.ytdl 等） | `temp_enabled`/`temp_days` | `age >= temp_days` |
| `frames` 抽帧目录 | 目录名含 `FRAMES_DIR_MARK` | `frames_enabled`/`frames_days` | 目录 mtime `age >= frames_days` |
| `thumbs` 缩略图缓存 | `THUMB_DIR_NAME` 下 | `thumbs_enabled`/`thumbs_days` | `age >= thumbs_days` |
| `media` 媒体本体 | `MEDIA_EXTS` | `media_enabled`/`media_days` | `age >= media_days`（伴生 sidecar 计入总大小） |
| `quota` 容量上限 | 按 `quota_gb` 阈值 | `quota_enabled`/`quota_gb` | 超过阈值时按**最旧优先**删，降到阈值内（不重复计已在 media 的项） |

返回含 `labels`（= `CATEGORY_LABELS`）、`total_files`、`total_size`、`trash_available`、`scanned_at`。

### 4.2 执行 `run`（`retention.py:523`）与回收站兜底
- `need_trash = {"media", "quota"}`：这两档**强制走系统回收站**（删除有回收价值，可找回）。
- 判定：`use_trash = cfg.media_use_trash and cat in need_trash`。
- **兜底红线**（`retention.py:536`）：
  ```python
  if use_trash and not trash_available():
      errors.append(f"{label}：系统回收站不可用，已跳过 N 项（拒绝硬删）")
      continue   # 宁可跳过，绝不硬删
  ```
- 删除时 `victims = [target] + _sidecars_of(target)`（媒体/配额档连同侧车一起进回收站）；`move_to_trash(v)` 为跨平台多级兜底（macOS Finder / Windows 回收站 / Linux trash-cli / 兜底 `shutil`）。
- 非 `need_trash` 档（temp/frames/thumbs）直接 `shutil.rmtree/unlink` 硬删（可重建、无回收价值）。
- 收尾 `_prune_empty_frame_dirs`：清掉清空后的空抽帧目录。

### 4.3 前端契约与门控
- `GET /api/retention/config` 取设（12 字段 `RetentionConfigRequest`）；`paintUsage` 读 `dir_size`/`disk_free`/`path` 展示决策依据。
- 前端 `CLEAN_LABELS`/`DANGER_CATS` 与后端 `CATEGORY_LABELS`/`need_trash` 对齐（前次核对已确认一致）。
- 门控：`RETENTION_ENABLED`（同媒体库开关）；前端按 `trash_available` 决定媒体档能否开启。

---

## 附：四模块门控速查

| 模块 | 启用条件 | 关键环境变量 |
|------|----------|--------------|
| AI 去水印 | 桌面版(frozen) 或 `VDL_AI_DEWATERMARK_ENABLED` | `VDL_AI_DEWATERMARK_MODE/DIR/ENDPOINT/TOKEN/TIMEOUT` |
| 订阅监控 | 桌面版 或 `VDL_LIBRARY_ENABLED`（`SUB_ENABLED`） | `VDL_SUBSCRIBE_PROBE_LIMIT` / `VDL_SUB_CHECK_INTERVAL` |
| 双节点 | 任意实例设 `VDL_REGION` + 对端 `VDL_PEER_ENDPOINT` | `VDL_PROXY_CN`（国内回源）、`CN_PROXY_AUTH` |
| 时效清理 | 桌面版 或 `VDL_LIBRARY_ENABLED`（`RETENTION_ENABLED`） | 配置走 `/api/retention/config` |
