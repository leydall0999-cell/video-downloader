# video-downloader 项目接续快照（2026-08-22）

> 生成时间：2026-08-22 22:00 (GMT+8) ｜ 当前分支：`web-dev`（HEAD=`cd96349`，**已 push origin/web-dev**）｜ 线上版本：`hanyuxz.top/api/version` git_sha=`cd9634980`（全部部署）
> 用途：复制到新会话，让 AI 直接接着往下开发，无需翻完整聊天记录。

---

## 一、原始需求与今日主线

- **项目**：自托管「视频下载 / 媒体备份」Web 工具（**FastAPI + yt-dlp + 原生前端**），域名 `hanyuxz.top`（Railway 海外节点，经国内 VPS `8.138.223.3` 回源）。
- **合规红线**：只下载自己拥有/已授权内容；**不破解付费墙/DRM**（Netflix/Disney+/DAZN 等一律不碰）。
- **今日（8/22）主线**：① 30 国内平台逐个实测打通（含直播三平台后下线）；② 用户列 120+ 国际平台批量盘点与接入；③ 用户两轮反馈"很多平台下载不了" → 全平台下载链路排查修复；④ YouTube 免 Cookie 下载突破。

---

## 二、今日完成工作成果（全部线上可用）

### 1️⃣ 国内平台 VPS worker 接入（9 个新平台，此前会话开始、今日收尾部署）
| 平台 | worker | 方式 | commit |
|---|---|---|---|
| 斗鱼 | douyu_resolve.py | Playwright 监听流 | `c6a0dd2`/`8c85252` |
| 央视频 | ysp_resolve.py | Playwright JSONP cKey 签名 | `dd17ed1`/`ff77e4b` |
| 好看视频 | yt_dlp_plugins/extractor/haokan.py | base64+XOR 解密 | `7b586c6`/`21157f5`/`6048bc2` |
| 1905 | m1905_resolve.py | Playwright（详情页反爬） | `8db1f6e` |
| 风行 | fun_resolve.py | Playwright 接口签名 | `33a618a` |
| 百视TV | bestv_resolve.py | wasm makepreviewquery 签名 | `a7d7af8` |
| 红果短剧 | hongguo_resolve.py | Playwright 点播放捕获字节 CDN 流 | `79f5233` |
| 映客 | inke_resolve.py | 纯 HTTP live_share_pc API（无需浏览器） | `927b807` |
| 网易CC | cc_resolve.py | 纯 HTTP vapi 公开接口（签名 FLV 直链） | `5f8f5fd` |

### 2️⃣ 直播模块下线（用户要求，`18c238e`）
- **斗鱼/映客/网易CC 三个直播平台整体下线**：`server/platforms.py` 新增 `DISABLED_PLATFORMS={douyu,inke,neteasecc}` 过滤平台列表；`downloader.py` probe()/_run_once 统一拦截返回「该直播平台已暂时下线」。
- **已创建每周一 10:00 自动化提醒**（id: automation-1787389098281）：「直播模块下线状态提醒」——确认是否恢复 + 是否处理遗留「直播流下载永不结束（硬超时报下载超时）」问题。
- VPS 上三个 worker 保留未删（恢复时改回 DISABLED_PLATFORMS 即可）。

### 3️⃣ 下载链路修复（8/22 上段，用户首轮报"还有问题"）
- **爱奇艺免费内容 F4V 直链**（`7090ab8`）：播放器改版后免费内容不发 m3u8，改走 `*.inter.71edge.com/*.f4v` 完整 FLV 直链；worker 增加 f4v 捕获 + `_iqiyi_info` 按 URL 自适应 ext/protocol + direct 透传。
- **直链 m3u8 下载协议归一化**（`7090ab8`）：worker 返回 protocol=https+m3u8 会被 yt-dlp HttpFD 下成播放清单文本（本地 A/B 实测 38952B 文本 vs 真实拉流）→ 统一归一化 `m3u8_native`。
- **daemon 加固**（`e7225ae`）：85s 硬超时 + `shutdown(wait=False)` + 每次解析前 `_kill_all_chromium()` 清残留（孤儿 chrome 占 500MB+ 拖垮 1.6GB VPS，导致线上 504）。
- **bestv 试看/VIP 检测**（`30eb219`）：bestv preview.m3u8 只有 9s 试看分片 → worker 统计 EXTINF <60s 判试看返回提示；iqiyi 捕获 playervideoinfo 提取 vipType + f4v HEAD 大小校验（<50MB=广告）→ VIP 返回 cookie_required 提示。
- **is_live 透传**（`c474e8e`）：_inke_info/_cc_info 透传 worker is_live + summarize 新增 is_live 字段。

### 4️⃣ 国际平台 120+ 盘点（用户列 120+ 平台）
- **静态探测**：yt-dlp 2026.7.4 对 102 平台 extractor 覆盖（`_VALID_URL` 匹配，⚠️ 域名点需转义 `\.`）。
- **白名单扩列 46 → 总数 110**（`d78bce9`）：真实 extractor（kick/curiositystream/vevo/snapchat/bigo/pluto/cbcgem/itv/arte/francetv/rai/svt/nrk/yle/rtve/tver/chzzk/sonyliv/hotstar/iflix/viu/mewatch/ivi/kinopoisk/abciview/sbs/nebula）+ DRM 识别（disneyplus/primevideo/appletv/paramount/hulu/peacock/crunchyroll/mubi/viki/crackle/rtlplus/unext/fod/zee5/tvnz/ctv）。
- **复核修正**（`a668149`，110→**114**）：AfreecaTV/RedBullTV/F1TV/PeerTube/Niconico/SonyLIV/iflix/meWATCH/KinoPoisk/CHZZK 其实都有 extractor → 补 4 白名单；Channel4/Criterion/Acorn 归 DRM 类。
- **产出 `INTERNATIONAL_PLATFORMS.md`** 四类矩阵：①可直接解析 ~40 ②需 Cookie/地区 ~8 ③DRM 合规不做 ~28 ④无 extractor ~35。
- **关键认知**：VDL 通用兜底 = 任何 yt-dlp 有 extractor 的站都能解析（白名单只影响识别/显示）；Railway 海外机房直连国际站。

### 5️⃣ 反爬平台攻坚（Rumble / BitChute / DAZN / Tubi）
- **BitChute ✅ 打通**（`f62bba7`）：yt-dlp BitChuteIE 元数据 OK 但直链 null——根因 `_check_format` 的 format 只有 url+filesize 无 height，`_detect_play_url` 3b 与 `build_watch_options` 只收有 height 直链 → 加无 height 兜底（prog_unknown 桶）。线上 play_url=zbbb278hfll091.bitchute.com/...mp4 ✅。惠及所有无 height format 提取器。
- **Rumble ⚠️ 部分**（`5c5839e`→`4291283`）：新增依赖 `curl_cffi`（Chrome TLS 指纹）；embedJS 对无会话请求返回**随机/推荐视频**（同一 URL 三次三个视频！）→ 页面 embed id 提取失败/sys.msg 拦截时明确报错，绝不退回短 id。视频页 CF 挑战数据中心 IP 稳定 403 → 需海外住宅代理。
- **DAZN 🚫 判定不可行**（`3a09736`）：认证墙（authentication-prod.ar.indazn.com/v1/authenticate）+ DASH(.mpd) + **Widevine DRM**；VPS 中国 IP 403、Railway 数据中心 IP 403（仅住宅 IP 可达）。白名单识别 + 明确提示。同类付费 DRM（Showmax/beIN/Turkcell/Movistar+）同构判定。
- **Tubi ⏳ 代码就绪待代理**（`946b4bc`→`cd4aeef`）：免费 AVOD 无 DRM，`_tubi_info` 完整解析（页面 `window.__data` 括号匹配 → dash/hlsv3/hlsv6 manifest，优先 HLS 直链；js_to_json 简化；oz API 兜底；**proxy 参数支持**）。VPS=GDPR 302 墙、Railway=2.8KB 反爬壳页 → 配美/加/澳/新/英/墨住宅代理即可打通。白名单 115→116。

### 6️⃣ 全平台下载链路排查与强化（用户二轮报"很多平台下载不了"）
- **实测方法**：VPS 真机（daemon 新鲜直链 + yt-dlp process_info 同款配置全量下载）+ 线上真实 /api/download 任务轮询 + /api/tasks/{id}/file 文件获取。
- **实测结果**：抖音 757KB ✅、B站（412 走 API 兜底）✅、红果 36MB ✅、1905 82MB ✅、央视频 453MB ✅、风行 68MB ✅、文件获取 200 video/mp4 ✅；腾讯测试链接内容不可用（非 bug）；YouTube ❌（见下）。
- **修复（`ac340c9`）**：① m3u8 直链 ext 归一化 bug——原把 ext 改 m3u8 产出 .m3u8 文件（内容 TS）→ 改保持 mp4 + `_download_options` 视频分支加 FFmpegVideoRemuxer（-c copy 统一封装 mp4）；② YouTube 下载阶段注入 visitor_data + fetch_pot。

### 7️⃣ YouTube 免 Cookie 下载突破（⭐ 今日高光）
- **问题**：无 Cookie + 数据中心 IP，tv_embedded 解析 OK 但下载分片 403；4 条 format 链降级全挂。
- **方案**：`_run_once` 403 降级升级为 **player_client × format 矩阵**（`aa7d423`）→ 顺序优化（`cd8209b`）→ **android 首位**（`cd96349`）。
- **task logs 实证**各 client：ios="Requested format is not available"（SABR）、tv="The page needs to be reloaded"、**android=直连 mp4 对云 IP 放行 ✅ 唯一可行**、tv_embedded=下载 403。
- **实测**：YouTube 无 Cookie 下载**总耗时 43s**（原 3-4 分钟），文件 11.8MB video/mp4 完整高清。
- **现有 YouTube 双保险**：①无 Cookie → tv_embedded 解析 + android client 降级（43s）；②有 Cookie → 自动链路（user > env VDL_YOUTUBE_COOKIE > cache > 公共池 pool）。

---

## 三、当前进度与待办

**现状**：国内 30 平台收尾（直播 3 平台下线）；国际平台 116 白名单 + 矩阵文档；下载链路实测健康；YouTube 免 Cookie 可用（43s）。

**待办清单（按优先级）**：
1. **Tubi 打通验证**：用户提供美/加/澳/新/英/墨任一住宅代理 → 配 VDL_PROXY（高级选项 → 代理）→ 复测 `_tubi_info` HLS 直链（代码已就绪）。
2. **Rumble 打通验证**：需海外住宅代理（视频页 CF 挑战），有代理后可复测。
3. **直播模块恢复确认**：每周一 10:00 自动化提醒会问；恢复需处理「直播流下载永不结束」问题（方案 A resolve 拒绝直播下载 / B 录制时长上限）。
4. **免费 AVOD 系列**：Popcornflix/Kanopy 等无 DRM 平台可逐个实测（评估逻辑同 Tubi，VPS 中国 IP 受限）。
5. **YouTube 下载耗时观察**：android client 若偶发失效（YouTube 改版），矩阵其余 client 兜底；有 Cookie 后走 Cookie 路径更稳。
6. **国际平台偶发反爬**（Rumble/Pluto/Yle 403/429）：重试或代理。

---

## 四、关键机制速查（防重复踩坑）

- **平台下线机制**：`platforms.py` `DISABLED_PLATFORMS` + downloader probe/_run_once 拦截。
- **直链 m3u8 下载**：归一化 `m3u8_native` + ext 保持 mp4 + FFmpegVideoRemuxer remux（勿再改成 m3u8 扩展名）。
- **YouTube client 选择**（数据中心 IP）：解析 tv_embedded 稳；下载 403 降级矩阵 android 首位（唯一可行）。
- **curl_cffi**：Chrome TLS 指纹模拟，已入 requirements（Rumble embedJS 等 CF 站点）。
- **daemon 重启安全流程**：`pgrep -f "[v]dl_cookie_daemon.py" | xargs -r kill -9` + `fuser -k -n tcp 18731` + `nohup .venv/bin/python ... &`（勿用 pkill -f，会杀 ssh 会话）。
- **Railway 部署自证**：轮询 `hanyuxz.top/api/version` 的 `git_sha` == 目标 commit（部署卡片上的 hex 是 Railway 自己的 ID，不是 git SHA）。
- **VPS 无法访问的站**：YouTube（被墙）、Rumble（被墙）、Tubi（GDPR 302）、DAZN（403 地区锁）——这些只能走 Railway（海外）或用户代理。
- **欧洲公共台**：多为地区锁（NRK 挪威 IP、Yle 数据中心 403）；FranceTV/Yle 部分内容中国 IP 反而可解析。

---

## 五、关键文档
- `INTERNATIONAL_PLATFORMS.md`：国际平台四类矩阵（120+ 平台分级，2026-08-22 全面更新）。
- `CN_PROXY_SETUP.md` / `COOKIE_POOL_SETUP.md`：国内回源代理 / Cookie 池手册。
- `.workbuddy/memory/2026-08-22.md`：今日详细日志（各平台结论、踩坑、修复细节）。
