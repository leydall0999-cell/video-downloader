fastapi==0.141.1
uvicorn[standard]==0.52.1
yt-dlp==2026.7.4
requests==2.32.3
cryptography==50.0.0
python-multipart==0.0.20

# 本地 Apple Vision OCR（片头视觉检测降级路径）：macOS 原生、免 key、离线。
# 仅 macOS 桌面构建需要；pyobjc 只有 macOS wheel，用 sys_platform 标记避免其它平台 pip 失败。
pyobjc-framework-Quartz; sys_platform == "darwin"

# YouTube bot 检测绕过（PO Token provider）：yt-dlp 插件，配合 Docker 内置的
# bgutil PO token server（/opt/bgutil/server/build/main.js，监听 127.0.0.1:4416）
# 自动为 YouTube 请求生成 proof-of-origin token，解决数据中心 IP 被
# "Sign in to confirm you're not a bot" 拦截的问题。
bgutil-ytdlp-pot-provider>=1.3.2
# 种子下载（桌面版可选）：libtorrent 为可选依赖，未安装时功能自动关闭（import 守护）。
# 官方 wheel 现状：2.0.13 仅 macOS 14/15；2.0.11 额外提供 macOS 13 的 x86_64 wheel。
# 为避免在 macOS 13 / Windows 等无对应 wheel 的环境让 `pip install -r requirements.txt` 失败，
# 这里仅对 Linux 自动安装；macOS / Windows 桌面版请手动安装（见 DESKTOP_FEATURES.md §2.10）：
#   pip install "libtorrent==2.0.11"
libtorrent==2.0.11; sys_platform == "linux"

# PDF / 图片去水印（需求文档模块二）：OpenCV inpainting + PyMuPDF。
# 两者均有全平台 wheel；缺失时去水印路由自动返回 503（import 守护，不影响其余功能）。
opencv-python-headless>=4.8.0
pymupdf>=1.23.0

# 图片 AI 无痕去水印（LaMa ONNX，~107MB 权重首次惰性下载到 ~/.vdl_models）：onnxruntime 免 torch，CPU 即可跑。
# 缺失时图片去水印自动回退 OpenCV（import 守护，不影响其余功能）。
onnxruntime>=1.17.0
# INT8 动态量化（VDL_DW_INT8=1，onnxruntime.quantization.quantize_dynamic 依赖此独立 onnx 包）：
# 用于把 LaMa fp32 权重动态量化为 int8，CPU 上约 1.5-2× 提速。缺失时 INT8 分支静默回退 fp32。
onnx>=1.15.0

# 一键抠图（图片去背景，「编辑」下新增子标签）：无需新增依赖。
# server/matting_ai.py 直接用上面的 onnxruntime + numpy + Pillow 跑 BiRefNet ONNX
# （刻意不引入 rembg：它会连带 scipy/scikit-image/numba/pooch，让安装包暴涨数百 MB）。
# ONNX 权重在首次使用时惰性下载到 ~/.vdl_models（默认轻量版 213MB，完整版 927MB）。

# Rumble 反爬绕过：curl_cffi 提供 Chrome TLS 指纹模拟（impersonate），
# 用于 embedJS/u3 接口解析（urllib/requests 默认指纹被 Cloudflare 403）。
curl_cffi>=0.7.0

# 图片高效压缩（AVIF 输出，比 WebP 再小约 20-50%）：pillow-avif-plugin 提供 AVIF 编解码。
# 缺失时压缩功能的「AVIF 输出」选项自动返回提示（compress.py import 守护），不影响其余功能。
# 注意：Pillow 由 build_mac.sh 单独安装，这里仅列插件即可。
pillow-avif-plugin>=1.6.0

# 图片 AI 增强去水印（扩散模型 inpainting：SD 1.5 / SDXL-Inpainting）：
# 【默认不打包】——torch+diffusers 栈约 2~3GB，会直接击穿 DMG <500MB 目标；
# 且 8GB 机型受 available() 内存闸门限制根本用不了，给所有人装是纯死重。
# 设计：扩散档为「高配机（16GB+）按需启用」的可选引擎，运行库由用户另行安装，
# 缺失时 /api/dw/capability 把该档显示为不可用、其余功能不受影响（与 cv2/onnxruntime 降级一致）。
# 需要启用的高配用户，在 .build_venv 里手动装：
#   pip install "torch>=2.2.0" "diffusers>=0.27.0" "transformers>=4.38.0" \
#               "accelerate>=0.27.0" "safetensors>=0.4.0" "huggingface-hub>=0.23.0"
# （macOS Apple 芯片会自动选 arm64+MPS wheel；权重首次用时惰性下载到 ~/.vdl_models）
# torch>=2.2.0
# diffusers>=0.27.0
# transformers>=4.38.0
# accelerate>=0.27.0
# safetensors>=0.4.0
# huggingface-hub>=0.23.0
