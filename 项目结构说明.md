# VideoDownloader 项目结构说明

> 生成日期：2026-08-15
> 仓库路径：`/Users/suixindelang/WorkBuddy/video-downloader`
> 规模：后端约 20 个 Python 模块（~20k 行）+ 前端 5.5k 行原生 JS + 独立解说 Worker + 桌面打包 + 自带 yt-dlp 插件 + 22 个测试
> 说明：本文档基于对全部源码的一次通读，用于快速建立项目全景认知。

---

## 一、项目定位

自托管「视频下载 + 媒体库管理 + 增值能力」工具。架构为 **FastAPI 后端 + 零依赖原生 HTML/CSS/JS 前端**。

核心原则：
- **个人媒体备份**：下载、整理、归档自己的媒体。
- **合规红线**：不破解付费墙 / 不绕过 DRM / 不下载付费 VOD（Netflix / iQiyi VIP / Plex VOD 等）。
- **服务端轻算力**：解说、AI 去水印等重算力甩给独立 worker，主服务只做编排与桥接（避免挤占下载 CPU）。

---

## 二、技术栈与部署形态

| 维度 | 选型 |
|---|---|
| 后端框架 | FastAPI（0.141） |
| 下载内核 | yt-dlp（钉版 2026.7.4）+ aria2c 加速 |
| 本地加工 | ffmpeg（提取音频/GIF/裁剪/压缩/放大/抽帧/封面/铃声/去水印） |
| 前端 | 单页原生 `index.html` + `app.js` + `styles.css`（无构建步骤） |
| 解说管线 | 独立 HTTP Worker（8100 端口）+ Whisper / edge-tts / 可选 LLM |
| 桌面版 | PyInstaller 打包 macOS DMG / Windows EXE（体积目标 < 500MB） |
| 云部署 | Railway（海外）+ 阿里云 ECS（国内）双节点，按域名分流、各自直连不跨境 |
| 特性开关 | 环境变量门控（`VDL_*`） |

---

## 三、目录结构树

```
video-downloader/
├── README.md                    # 功能/平台白名单/双节点部署/合规变现/API 表
├── DESKTOP_FEATURES.md          # 桌面版功能全清单（最长文档）
├── COMMENTARY_PLAN.md           # 解说管线规划
├── 需求文档.md                  # 产品需求
├── requirements.txt             # 钉版依赖（fastapi/yt-dlp/cryptography…）
├── run.sh / start.py            # 启动脚本（Railway / 本机）
├── Dockerfile / Procfile / runtime.txt
├── VideoDownloader.spec         # PyInstaller 打包 spec
├── cn_proxy.py / CN_PROXY_SETUP.md   # 零依赖国内回源代理（海外部署用）
├── proxy_scout.py / setup_vps.sh / e2e_tencent_test.sh  # 代理探测/VPS一键部署/端到端测试
├── server/                      # 后端（FastAPI）
│   ├── app.py                   # 主路由 + 生命周期 + 中间件 + 特性开关（最大，4627 行）
│   ├── platforms.py             # 链接校验 / 50+ 平台白名单 / 国内外代理分流
│   ├── downloader.py            # yt-dlp 封装：解析/下载/进度/看门狗/续传/Cookie 注入
│   ├── tasks.py                 # 下载任务内存仓库（状态机/步骤/日志/TTL/孤儿回收）
│   ├── batch.py                 # 批量下载并发调度
│   ├── ffmpeg_tools.py          # 11 种本地加工（含去水印 delogo）
│   ├── library.py               # 媒体库扫描（base64 id/目录穿越防护/缩略图懒生成）
│   ├── extract_text.py          # 提取文案（字幕→回退 Whisper→发布简介）
│   ├── subtitles.py             # 字幕抽取 / 硬烧录 / 可选 LLM 翻译
│   ├── process_queue.py         # 加工队列并发控制
│   ├── retention.py             # 时效自动清理（5 档，媒体删除走回收站）
│   ├── archive.py               # 一键归档网盘（去重/凭据脱敏）
│   ├── clouddrive.py            # WebDAV + 百度网盘 OAuth2
│   ├── crypto_vault.py          # 库内保险箱 AES-256-GCM 加密
│   ├── subscriptions.py         # 订阅监控（本地 JSON + yt-dlp 探查频道新视频）
│   ├── torrent.py               # 种子下载（libtorrent + aria2 双后端）
│   ├── llm_config.py            # 统一 LLM 配置（多厂商 + 环境变量覆盖）
│   ├── cookie_cache.py          # 本机浏览器 Cookie 缓存（chmod 600，30 天 TTL）
│   ├── ydlp_update.py           # yt-dlp 解析器自动更新（sha256 + Zip Slip 防护）
│   └── commentary_locate.py     # 解说管线三处路径定位
├── web/                         # 前端（零依赖）
│   ├── index.html               # 单页结构（含 #adsSlot 广告位容器 + 各 Tab）
│   ├── app.js                   # 全部交互逻辑（最大前端文件，5492 行）
│   ├── styles.css               # 响应式样式
│   └── robots.txt               # 禁止索引
├── commentary-worker/           # 独立解说管线（不挤占下载 CPU）
│   ├── commentary_worker.py     # 独立 HTTP 服务（8100）
│   ├── process.py               # 一键管线编排（转写→配音→出片）
│   ├── scripts/                 # edit/llm_script/transcribe/config/voice_preview 等
│   ├── verify_quality.py / Dockerfile / requirements.txt / deploy.sh / start_worker.sh
├── desktop/                     # 桌面打包
│   ├── build_mac.sh / build_win.sh   # PyInstaller
│   ├── bundle_aria2.py          # 自包含 aria2c
│   ├── desktop_launcher.py
│   └── deploy_mac.sh
├── yt_dlp_plugins/extractor/    # 自写提取器覆盖（优先级高于内置）
│   ├── vqq_patch.py             # 腾讯视频新版 pinia 数据修复（标题/时长）
│   ├── chrqj.py
│   └── kuaishou.py
└── tests/                       # 22 个测试（路由冒烟 + 单测）
    ├── test_merged_modules_verify.py   # 4 模块合并后核对（本任务产出）
    ├── test_commentary_routes.py
    ├── test_torrent_*.py / test_archive_*.py / test_crypto_*.py
    ├── test_clouddrive_download.py / test_ydlp_update.py / test_ffmpeg_tools_unit.py
    └── …
```

---

## 四、后端模块详解（`server/`）

### 4.1 核心下载链路
- **`app.py`（4627 行，编排层）**
  - 全部 REST 路由 + WebSocket（进度 SSE 替代）
  - 中间件：CORS / no-cache / API Token 鉴权
  - 生命周期 `lifespan`：初始化各模块、启动后台清理任务
  - 全部特性开关集中在此（`SUB_ENABLED` / `RETENTION_ENABLED` / `LIBRARY_ENABLED` / `COMMENTARY_ENABLED` / `AI_DEWATERMARK_*`）
  - 关键调度函数：`_run_process`（加工 op 分发，含 `ai_dewatermark`）、`_run_subscription_check`、`_process_status`
- **`platforms.py`** — 链接校验、50+ 平台白名单、国内外代理分流、URL 规范化、非视频站拦截
- **`downloader.py`（1189）** — yt-dlp 封装：解析 / 清晰度选项 / 下载 / 进度聚合 / **停滞看门狗+硬超时** / 断点续传 / 浏览器 Cookie 注入 / aria2c 加速 / 在线观看地址探测
- **`tasks.py`** — 下载任务内存仓库：状态机、步骤、日志、TTL 清理、孤儿目录回收
- **`batch.py`** — 批量下载并发调度器（动态并发上限）

### 4.2 本地媒体加工
- **`ffmpeg_tools.py`（419）** — 提取音频 / GIF / 裁剪 / 压缩 / 放大 / 抽帧 / 封面 / 铃声 / **去水印(delogo)** 共 11 种
- **`library.py`** — 媒体库扫描：base64 id 可逆、目录穿越防护、缩略图懒生成、保险箱文件识别
- **`extract_text.py`** — 提取文案（内嵌/在线字幕 → 回退 Whisper 转写；发布简介）
- **`subtitles.py`** — 字幕（在线/内嵌抽取、硬字幕烧录、可选 LLM 翻译）
- **`process_queue.py`** — 加工队列并发控制（Condition+计数，默认 2 / 硬顶 4）
- **`retention.py`（618）** — **时效自动清理**：分 5 档（碎片/抽帧/缩略图/媒体/容量上限），媒体删除强制走系统回收站（跨平台多级兜底）
- **`archive.py`（438）** — **一键归档网盘**：选什么/传哪/指纹去重/凭据脱敏

### 4.3 云盘 / 加密 / 订阅 / 种子
- **`clouddrive.py`（687）** — WebDAV（零依赖）+ 百度网盘 OAuth2（分片上传/分享转存/官方合规下载）
- **`crypto_vault.py`** — 库内保险箱 AES-256-GCM 加密（PBKDF2 派生、分块 nonce、密钥不落盘）
- **`subscriptions.py`** — **订阅监控**：本地 JSON 持久化 + yt-dlp 探查频道新视频
- **`torrent.py`（918）** — **种子下载**：libtorrent + aria2 双后端自动选择、SSRF 防护、路径穿越拦截、完成写侧车
- **`llm_config.py`** — 统一 LLM 配置（OpenAI/DeepSeek/通义/智谱/Moonshot/Ollama/自定义；持久化 JSON + 环境变量覆盖）

### 4.4 支撑模块
- **`cookie_cache.py`** — 本机浏览器 Cookie 缓存（~/.videodownloader，chmod 600，30 天 TTL）
- **`ydlp_update.py`** — yt-dlp 解析器自动更新（PyPI sha256 校验 + Zip Slip 防护 + 原子替换）
- **`commentary_locate.py`** — 解说管线三处路径定位（包内捆绑 / 环境变量 / 外部扫描）

---

## 五、前端（`web/`）

- **`index.html`（1144）** — 单页结构（含广告位容器 `#adsSlot`、各 Tab）
- **`app.js`（5492，最大前端文件）** — 全部交互：解析 / 下载 / 进度 SSE / 在线观看 / 媒体库 / 订阅 / 时效清理 / 云盘 / 保险箱 / 解说 / AI 去水印 / 转码 / 种子
- **`styles.css`（1581）** — 响应式样式
- **`robots.txt`** — 禁止索引

---

## 六、解说 Worker（`commentary-worker/`）

独立 HTTP / 子进程管线（不挤占下载 CPU）：
- **`commentary_worker.py`** — 独立 HTTP 服务（8100 端口）
- **`process.py`（374）** — 一键管线编排（转写 → 配音 → 出片）
- **`scripts/`** — `edit.py` / `edit_ffmpeg.py`（切条+字幕+旁白）、`llm_script.py`（解说词生成）、`transcribe.py`（Whisper）、`config.py`、`commentary_options.py`、`voice_preview.py`、`ffmpeg_path.py`
- **`verify_quality.py` / `Dockerfile` / `requirements.txt` / `deploy.sh` / `start_worker.sh`**

---

## 七、桌面打包（`desktop/`）与 yt-dlp 插件

- **`desktop/`** — `build_mac.sh` / `build_win.sh`（PyInstaller）、`bundle_aria2.py`（自包含 aria2c）、`desktop_launcher.py`、`deploy_mac.sh`
- **`yt_dlp_plugins/extractor/`** — **自写提取器覆盖**：`vqq_patch.py`（腾讯视频新版 pinia 数据修复）、`chrqj.py`、`kuaishou.py`
  - 覆盖机制：同名类（`__all__` 同名）被 yt-dlp `load_plugins` 前置到提取器链，优先级高于内置。

---

## 八、测试（`tests/`，22 个）

覆盖路由冒烟 + 单测：
- `test_merged_modules_verify.py` — 之前产出的 4 模块合并后核对（AI 去水印 / 订阅监控 / 时效清理 / 媒体库清理）
- `test_commentary_routes` / `test_torrent_*` / `test_archive_*` / `test_crypto_*` / `test_clouddrive_download` / `test_ydlp_update` / `test_ffmpeg_tools_unit` 等

复跑示例：
```bash
cd video-downloader
.build_venv/bin/python -m pytest tests/test_merged_modules_verify.py -q
```

---

## 九、关键架构特征

1. **状态全内存态**：任务 / 种子 / 加工作业重启即丢；订阅 / 清理 / 归档 / 保险箱配置落盘 JSON。
2. **特性开关驱动**：`VDL_SUBSCRIPTIONS_ENABLED` / `VDL_LIBRARY_ENABLED` / `VDL_COMMENTARY_ENABLED` / `VDL_AI_DEWATERMARK_*` 分别门控对应模块；桌面版（frozen）自动探测。
3. **合规与变现**：订阅墙（转换/下载/云盘共用一把 `VDL_CONVERT_SUB_KEY`，按 IP 每日限次）、广告位默认关、SSRF 全链路防护、不做付费墙破解。
4. **双节点部署**：国内外各一份，按域名分流、各自直连、不跨境。
5. **安全底线**：媒体删除走回收站、凭据 chmod 600、Cookie 仅本机、密钥不落盘。

---

## 十、特性开关一览

| 开关 | 门控模块 |
|---|---|
| `VDL_SUBSCRIPTIONS_ENABLED` | 订阅监控 |
| `VDL_LIBRARY_ENABLED` | 媒体库 + 时效清理 + 加工队列 + AI 去水印 |
| `VDL_RETENTION_ENABLED` | 时效清理（部分受 LIBRARY 控制） |
| `VDL_COMMENTARY_ENABLED` | 解说管线 |
| `VDL_AI_DEWATERMARK_MODE` / `VDL_AI_DEWATERMARK_ENDPOINT` | AI 去水印（http/local 模式） |
| `VDL_CONVERT_SUB_KEY` | 订阅墙统一密钥 |
| `VDL_ADS_ENABLED` | 广告位开关 |

---

## 十一、合规与变现红线（必读）

- **不破解付费墙 / 不绕过 DRM / 不下载付费 VOD**（Netflix、iQiyi VIP、Plex VOD 等）。
- 变现模型：订阅优先（开源 + 增值）+ 合规广告为辅（`VDL_ADS_ENABLED` 开关控制），排斥灰色/诱导广告。
- 平台白名单仅放行已知合规站点，非视频站一律拦截。
