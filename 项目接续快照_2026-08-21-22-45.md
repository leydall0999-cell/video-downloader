# 项目接续快照 — video-downloader（VDL）2026-08-21 深夜版

> 生成时间：2026-08-21 22:45（GMT+8）｜本快照所有版本号、提交、服务状态均已实测核对
> 用法：把本文档整体复制到新会话，AI 即可无缝续接开发。

---

## 0. 当前真实状态（一屏速览，全部实测）

| 项目 | 状态 |
|---|---|
| 本地 web-dev HEAD | `514786b`（= origin/web-dev，干净无未提交改动） |
| 线上 Railway `git_sha` | `514786b`（Railway 绑定 GitHub web-dev，push 即自动部署） |
| 隧道 | `tunnel_ready=true`（tunnel-test elapsed≈0.5s 回源 200） |
| Cookie 公共池 | `pool_available_after=true`（pull-diag 全绿） |
| VPS 服务 | `vdl-cookie.service` active + `cn_tunnel_client.service` active（心跳 x156，50+ 分钟零断连） |
| VPS daemon | 18731 LISTEN（PID 356350，systemd Restart=always 托管） |
| 爱奇艺多形式链接 | 全部走 VPS worker，公开视频免 Cookie ✅ |
| 分支 | main=`98b38d2`（全功能）、web-dev=`514786b`（网页精简版，开发主线）、app-dev=`5f9097c`（桌面端，未动） |

---

## 1. 项目原始需求

自托管「视频下载 / 媒体备份」Web 工具（FastAPI + yt-dlp + 原生前端），并规划「视频解说自动成片」增值方向。

- 核心功能：50+ 平台下载解析、SSE 实时进度、国内外代理分流、SSRF 防护、格式转换、去水印
- **合规红线**：只下载自己拥有/已授权内容，不破解付费墙/DRM；开源 MIT，订阅墙仅围绕合法媒体管理增值
- **网页版精简定位**：只保留下载 / 格式转换 / 去水印三大功能（web-dev 分支剥离了桌面 App 专属 router）

### 本次会话主线（2026-08-21 当日）
让**爱奇艺各种形式链接**（playShare 分享页、v_xxx 普通页、a_xxx 专辑页、iqy.net/qy.net 短链）在网页版**都能解析下载**，公开视频免 Cookie；顺带把 **Cookie 池黄灯** 和 **隧道稳定性** 一并治掉。

---

## 2. 已完成工作成果（2026-08-21 全量）

### A. 爱奇艺多形式链接解析（主线，全链路闭环 ✅）

| 提交 | 内容 |
|---|---|
| `036cdfc` | `_strip_tracking_params` 新增 `keep` 白名单；iqy.net/qy.net 展开成 playShare 后保留 `shareId`/`positiveId` |
| `e73e10a` | 重写 `_iqiyi_info` 分发：**所有爱奇艺链接优先走 VPS worker**（playShare/普通页/专辑页），公开视频无需 Cookie；贴 Cookie 且非分享页走 yt-dlp 直下兜底；重写 test_iqiyi_worker.py（11 passed） |
| `cf6f16c` | 修 `_normalize_share_url` 漏分支：直接粘贴 `iqiyi.com/playShare.html?shareId=...` 也保留 shareId/positiveId（此前只修了短链分支） |
| `4f5707d` | 支持 `qy.net` 短链展开（此前仅认 iqy.net），HEAD 跟随重定向到最终 iqiyi.com 链接 |
| `8f223b7` | 隧道/VPS 故障不再伪装成「需要 Cookie」，错误分类透明化（仅真 407 才提示贴 Cookie） |

**实测通过**：playShare（活佛济公3）、qy.net/afIXo3Z-6e（倚天屠龙记，返回 meta-cdn m3u8 直链）均成功。

### B. VPS worker / daemon 稳定性（✅）

| 提交 | 内容 |
|---|---|
| `ddf0bf6` | 拆锁：`_resolve_lock`（resolve 串行 Chromium）与 `_push_lock`（push）独立；push 加 20s 超时兜底——修掉「push 网络抖动占锁饿死 resolve」 |
| 60bedc3 | 反转推送方向：VPS 主动推改 **Railway 经隧道主动拉**（绕开 VPS→Railway 跨境抖动） |
| `f53b3ec` | 两端复用共享 `VDL_COOKIE_SYNC_TOKEN` 鉴权（免另配密钥） |

### C. 隧道架构修复（✅，本次最大坑）

| 提交/动作 | 内容 |
|---|---|
| 灰云方案 | 用户 Cloudflare 改 DNS only，VPS 直连 Railway 源站绕 CF 封禁（CF 封 VPS 阿里云 IP，全部 CF 前端站点超时） |
| VPS 服务文件 | 重写 `cn_tunnel_client.service`：WS URL 改 `wss://web-production-b9993.up.railway.app/ws/cn-tunnel`（Railway 原生域名，非 CF） |
| 隧道 client 按 Host 路由 | `cn_tunnel_client.py` `handle_open` 按首笔请求 Host 分流：`127.0.0.1:18731` → daemon，其余 → cn_proxy 18888（dbb2649 配套） |
| `dbb2649` | Railway `_call_vps_worker` 自动纠正旧配置：指向 cn_proxy(18888) → 经隧道直连 daemon(18731) |
| `1367e2d` | **Railway 环境代理误伤修复**：`_call_vps_worker` 改用 requests + 显式 `proxies`（VDL_COOKIE_PULL_PROXY 默认 127.0.0.1:18889）覆盖 Railway 注入的 http_proxy/https_proxy（此前 407 被误判为「需要 Cookie」）；顺手修 b23.tv/BVxxx 丢失 p/t |
| 心跳修复（VPS 直接部署，未走 git） | **移除 WS 层 `ping_interval/ping_timeout`（FastAPI/Starlette 不响应 ping → 10s 等不到 pong 主动断开）+ 新增应用层 type=3 心跳协程（每 20s 一帧）**——修掉高频断连（此前每 30-120s 断一次，`no close frame received or sent`） |

### D. Cookie 池黄灯根治（✅）

| 提交 | 内容 |
|---|---|
| `d3947f4`/`7c38093` | pull-diag 加细粒度诊断（pool_dir/mount/cipher/candidates/逐条 entries） |
| `85c7135` | **根因修复**：`cookie_pool._save` 全量重写时把已有加密记录（仅 header_enc 键）重写成空明文条目 → 密文丢失、去重永不命中、池无限膨胀、available 闪烁。修复：有密文无明文原样保留、双空条目丢弃、无 cipher 解密回明文兜底；新增 test_cookie_pool_save.py（3 用例） |

**线上验证**：pull-diag 连续 5 次 `pool_available_after=true` 稳定，池文件 n=2 收敛；B站 resolve 实测返回真实视频 + m4s 直链（公共池 Cookie + 隧道回源闭环）。

---

## 3. 当前进度与待办清单

### ✅ 已闭环
1. 爱奇艺全部链接形式 → VPS worker → m3u8 直链，公开视频免 Cookie
2. 隧道稳定性（心跳修复后 50+ 分钟零断连）
3. Cookie 池黄灯转绿
4. B站（公共池路径）解析闭环

### ⏳ 待办（按优先级）

- [ ] **P0｜代码固化心跳修复**：心跳增强目前只直接部署在 VPS `/opt/vdl-tunnel/cn_tunnel_client.py`，**仓库 `vps_worker/cn_tunnel_client.py`（若存在）尚未同步该改动**。下次大改前应把 VPS 当前版本复制回仓库并提交，防止丢失。
- [ ] **P1｜清理诊断代码**：pull-diag 已含大量细粒度诊断字段（原注释「排查完成后移除」），建议收敛为简洁版或保留为运维诊断端点（决策后处理）。
- [ ] **P1｜main←web-dev 反向合并**：web-dev 领先 main 80+ 提交但删除了 11 个 router，**不能直接 git merge**；沿用「文件级覆盖 + app.py 增量补丁」策略。需用户明确指示才动手。
- [ ] **P2｜隧道断连窗口的 3s 重试**：`9b7489d` 已给 probe 加「隧道重连窗口 3s 全新实例重试一次」，可实测确认生效。
- [ ] **P2｜VIP/付费视频路径**：worker 407 → 用户贴 Cookie → yt-dlp 直下，链路已就绪但**未经真机 VIP 账号实测**。
- [ ] **P3｜抖音提速 / VPS 住宅代理提 B站画质**（长期优化，用户之前提过可选）。

---

## 4. 关键约束、修改要求、注意事项（重要，务必遵守）

### ⚠️ 工作方式铁律（用户明确，2026-08-21 写入长期记忆）
1. **凡是 AI 能自己完成的操作，绝不让用户做**。包括：VPS 运维（SSH root@8.138.223.3 改文件/重启服务/看日志，SSH key 沙盒可直连）、代码→测试→commit→push→轮询 /api/version 确认部署、线上验证（curl /api/resolve、/api/cookie/pull-diag）。
2. **仅在以下情况才问用户**（且优先找绕过方案）：
   - 必须登录用户私人后台（Cloudflare Dashboard、Railway 手动 Redeploy 当自动部署失效时）→ 优先绕过（如隧道直连 `*.up.railway.app` 原生域名绕 CF）
   - 真实密钥/凭据需用户粘贴（**绝不从截图 OCR**）
3. **Railway 部署自查**：push web-dev 后轮询 `https://hanyuxz.top/api/version` 的 `git_sha`，相等即部署成功，不要问用户「部署好了吗」。

### ⚠️ 技术约束与坑（实测教训）
1. **VPS daemon 重启正确流程**：`pgrep -f "[v]dl_cookie_daemon.py" | xargs -r kill -9` + `fuser -k -n tcp 18731` + 用 `/opt/vdl-worker/.venv/bin/python` 启动（系统 python3 无 playwright）。**勿用 pkill -f 会杀 ssh 自身**。daemon 由 systemd `vdl-cookie.service`（Restart=always）托管，kill 后会自动拉起。
2. **隧道**：VPS client `cn_tunnel_client.service`（systemd）连 `wss://web-production-b9993.up.railway.app/ws/cn-tunnel?token=...`；**不要用 WS 层 ping（FastAPI 不响应）**，靠应用层 type=3 心跳（每 20s）保活。
3. **Railway 环境变量代理**：容器内有 http_proxy/https_proxy，任何访问 127.0.0.1/内网隧道的请求必须显式 `proxies={"http":..., "https":...}` 覆盖，不能依赖默认 opener。
4. **CF 封 VPS IP**：VPS（8.138.223.3）访问任何 Cloudflare 前端站点超时；隧道/回源必须走 Railway 原生域名或灰云 DNS only。
5. **版本核对**：以 `/api/version` 的 `git_sha` 为准（Railway 部署卡片里的 hex 是 build ID 不是 git SHA）；本地以 `git rev-parse HEAD` 为准。
6. **分支纪律**：默认只在 `web-dev` 开发，除非用户明确要求切换；app-dev（桌面打包）在 `video-downloader-app` worktree，桌面构建必须在那边跑。
7. **公共 Cookie 池**：`server/cookie_pool.py` `_save` 全量重写必须做字段级往返保真（保留 header_enc，不重写为空）；vps-pull 每次写入同一 Cookie，靠 `add_cookie` 去重（`_decrypt_item(c)==header`）防膨胀。

### 关键配置/域名备忘
- 线上主域：`https://hanyuxz.top`（灰云 DNS only，直连 Railway）
- Railway 原生域名：`web-production-b9993.up.railway.app`（隧道/回源直连用，绕 CF）
- VPS：`root@8.138.223.3`（阿里云）；隧道 token `Hy7FSFe1GpU49LP_lTsPpnLFA4d6fFRLhZc_8AKUu4w`；daemon token 共享 `VDL_COOKIE_SYNC_TOKEN`
- 诊断端点：`/api/version`、`/api/cookie/pull-diag`、`/api/tunnel-test?mode=http|connect|handlers|probe`
- VPS 工作目录：`/opt/vdl-worker`（daemon+worker）、`/opt/vdl-tunnel`（隧道客户端）

---

## 5. 本次会话全部提交（web-dev，514786b 即最新）

```
036cdfc → e73e10a → 1367e2d → 8f223b7 → dbb2649 → cf6f16c → d3947f4 → 7c38093 → 85c7135 → 514786b
```

（另有 VPS 直接部署未入库的改动：隧道心跳增强 — 见待办 P0）
