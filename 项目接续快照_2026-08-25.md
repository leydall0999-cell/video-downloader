# 项目接续快照 — video-downloader（VDL）2026-08-25 20:01

> 由 AI 生成，可直接复制到新会话让 AI 继续开发。两条支线独立：**默认 app 端（app-dev 分支）**，转 web 必须先询问。

---

## 1. 原始需求（项目总目标）

跨平台视频下载工具（**App 桌面端 + Web 端**）：
- 多平台视频解析下载（抖音/B站/腾讯/快手/爱奇艺/微视/1905/小红书/红果/微博等），跨平台 **Cookie 共享池**（含 VPS Playwright worker）
- 附带：视频解说（AI 解说管线 commentary-pipeline）、视频格式转换、去水印（AI+图片/PDF）、订阅追更、种子、百度网盘
- 桌面 App 用 PyInstaller 打包（macOS .app/.dmg），Web 端部署 Railway（hanyuxz.top）
- **近期核心打磨点**：App 桌面端「视频解说」从本地上传视频 → 生成解说成片，成片命名、稳定性、UI 全链路

## 2. 已完成的全部工作成果

### 2.1 代码状态（全部已 commit + push，双仓库同步）
- **app 端** `video-downloader-app`（worktree，app-dev 分支，HEAD `052a57b`，origin 同步）：
  - `3893891` 下载任务 title 真正传入并流入解说成片名
  - `3171bbf` 本地上传解说增加片名输入框（后被撤）
  - `42a9e61` 撤回输入框，本地上传成片名直接用上传文件名(去扩展名)当前缀
  - `2b41605` 根治双击打不开/闪退（单实例锁改端口判定 + webview 异常非致命化）
  - `3a610cf`/`8d45b76`/`6e4a073` 重复双击提窗改 AppKit NSRunningApplication（无需辅助功能权限）
  - `0e76b31` **彻底移除自动跳浏览器**（用户愤怒明确：App 就是 App；原生窗口失败仅弹窗+日志）+ `2ad32e5` 文案
  - `f8a3738` 本地上传片名兜底改进（ffprobe 读 metadata title + 短码）
  - `6b78cee` 测试覆盖 upload.mp4 → 短码兜底
  - `052a57b` 片名优先级改为**上传前的名字优先**（`_meaningful_stem` 判定），上传《少帅.mp4》→ 片名「少帅」
- **commentary-pipeline**（`问问题/commentary-pipeline` 真实源码，master 分支，HEAD `391dc09`，origin 同步）：
  - `c2bcedc` 成片命名改为 `<原片名>-解说完成<YYYYMMDDHHMM>.mp4`（**横竖屏统一不带 `_竖屏`/`_原速` 后缀**）
  - `391dc09` **VAD 放宽 + 0 段自动 fallback 禁用 VAD 重试**（根治「转写稿为空」）

### 2.2 关键功能验证结果（实测）
- 成片命名：上传《少帅.mp4》→ `少帅-解说完成202608251917.mp4`（单元测试实测 title='少帅' ✓）
- upload.mp4 无 metadata → `v<6hex>` 短码（不会退回 upload）✓
- VAD 修复：用户《两只老虎》视频，原 0 段 → 修后 **31 段**（VAD 误判 → fallback 救回）✓
- 启动自查：新包隔离端口 7s 就绪、原生窗口正常、第二实例正确激活退出、不跳浏览器 ✓
- app 全量测试 **228 passed**（无回归）；pipeline 测试：命名 5 过
- **预先存在的失败（与本次改动无关）**：pipeline `test_llm_script.py` 5 failed（mock lambda 签名不匹配 `_call_llm` 参数）、`test_edit_ffmpeg_render.py` 9 failed（模块依赖问题）—— stash 前后一致

### 2.3 交付物
- **最新 DMG**：`video-downloader-app/dist/VideoDownloader.dmg`（19:57 重建，382,437,496 字节，**含 VAD 修复 + 新命名规则**）
- 注意：`/api/version` 指纹仍显示 `构建 052a57b`（build 脚本 git_sha 只取 app 仓库 SHA，不反映 pipeline 改动；bundle 内实际有最新 pipeline 代码，可用 `grep "VAD 模式下返回 0 段" dist/.../commentary/scripts/transcribe.py` 确认）

## 3. 当前进度 + 待办

### 当前状态：用户正在验收新包（19:57 DMG，含命名规则+VAD修复）
- 已交付给用户安装验证；**尚未收到用户确认**新包能否正常跑通（上传 → 转写 → 解说 → 成片 `少帅-解说完成<时间>.mp4`）

### 待办清单（按优先级）
1. **[进行中] 用户验收**：等用户装 19:57 新 DMG 后确认「转写稿为空」已消失、成片名格式正确；若仍有问题按以下排查
2. **（建议）修 build 指纹漏洞**：`/api/version` 的 git_sha 应同时反映 pipeline 仓库 SHA（build_mac.sh 注入 `COMMENTARY_PIPELINE_DIR` 的 git rev-parse HEAD 到 build_version.txt），避免「改了 pipeline 但指纹没变」误导排查
3. **（建议）修预存测试失败**：`test_llm_script.py` mock 更新为接受 `reasoning_effort`/`temperature` 参数；`test_edit_ffmpeg_render.py` 依赖问题
4. **（建议）VAD 相关**：若 base 模型对某些视频识别质量差，可考虑提示用户/文档里说明可用 `WHISPER_VAD=0` 或升级模型
5. **（用户已明确关闭，不要再做）**：剩余 5 平台（央视频/斗鱼/百视TV/映客/网易CC）实测、netease 网易云 MV 验证、桌面端优酷引擎本机实测、/download 安装包直链替换、微信视频号下载（技术不可行已定论）、共享池→weixin.txt 自动同步

## 4. 关键约束、修改要求、注意事项

### ⚠️ 铁律（用户明确，违反会被骂）
1. **app 端禁止任何「自动跳浏览器」行为**（2026-08-25 用户原话：「app就是app打开网页干什么?我什么时候让你这么干了??」）。原生窗口失败只允许：写 `~/.vdl_launch.log` + 系统弹窗；**唯一例外**：百度网盘 OAuth 授权页（用户主动点登录才触发）
2. **默认做 app 端（app-dev）任务**；转 web 端（web-dev）必须先询问用户；两条支线独立绝不 merge，功能覆盖走按需 cherry-pick
3. **成片命名规则（最终版）**：`上传前的名字 + -解说完成 + YYYYMMDDHHMM + .mp4`；固定词「解说完成」；**横竖屏统一不带后缀**；界面文案「解说成片」保持不动（用户：只改文件名）
4. **给用户做"兜底方案"前必须问用户**（自作主张加"跳浏览器"兜底的教训）
5. 关键凭据绝不从截图 OCR；用户不会/不愿执行命令行运维（AI 自运维：SSH/VPS/Railway 全自己来）

### 工程约束
- **桌面构建必须在 `video-downloader-app` 目录（app-dev）跑**：`COMMENTARY_PIPELINE_DIR=/Users/suixindelang/WorkBuddy/问问题/commentary-pipeline bash desktop/build_mac.sh`（约 3 分钟）
- "改了代码但 app 没更新"根因=构建时间早于提交时间；改 server/web/pipeline 后必须重跑 build_mac.sh 才进二进制
- `commentary-pipeline` 是软链指向 `问问题/commentary-pipeline`（真实源码），**不是** `commentary-worker`（那是误判副本）
- 每次关键修复要 commit（防工作区清空丢失教训）；改完 pipeline 记得在 `问问题/commentary-pipeline` 里也 commit+push
- 机器上共存多份 VideoDownloader.app 副本（video-downloader/dist、video-downloader-app-OLD-DO-NOT-USE 等），用户易误点旧包——引导从 /Applications 打开
- VPS 三服务（vdl-cookie/cn_proxy/cn_tunnel_client）全 enabled；cookie_pool 公共池部分域为空是预存状态（非故障）

### 本次会话新增的路径/位置速查
- 成片命名：pipeline `scripts/config.py` `final_output_name()`（固定词+去后缀）；app `server/routers/commentary.py` 两处 upload（title 优先级）；`server/app.py` `_meaningful_stem`/`_probe_video_title`
- VAD：pipeline `scripts/transcribe.py` `_transcribe_whisper_faster()`（`min_silence_duration_ms=1500` + 0 段 fallback + `WHISPER_VAD=0`）
- 启动日志：`~/.vdl_launch.log`；单实例锁：`~/.vdl_instance.lock`（PID 端口 build）
- 测试：`video-downloader-app/tests/test_upload_title_fallback.py`（2 过）、`问问题/commentary-pipeline/tests/test_final_output_name.py`（5 过）
