# 合并后系统测试报告（2026-08-15）

> 目标：0804 精简版与 0809 功能版合并（`-X ours`，以 0809 为主）后，系统排查残留问题。
> 测试对象：`video-downloader` 0809 工作区（HEAD `f7a4461`，已 push main）。

## 一、测试方法
1. **后端路由冒烟**：用 managed venv 起 `TestClient`，遍历全部 **107 个路由**。
   - GET 路由全量请求（跳过 SSE/文件流等会挂起的端点）。
   - 任务操作类 POST/DELETE：`/api/tasks/{id}/pause|resume|retry`、`DELETE /api/tasks/{id}`、`/api/tasks/cancel-all` 等（用占位 task_id，预期 404，绝不该 500）。
   - 21 个 POST 验证路径：`/api/resolve`、`/api/convert`、`/api/commentary/*`、`/api/subtitles/*`、`/api/llm/config`、`/api/subscriptions` 等（仅验证入口，不真正触发下载/转写）。
2. **前端静态交叉比对**（合并残留 bug 的高危区）：
   - `el.X` 全部引用 vs `el = {}` 定义集合。
   - `$('id')` / `getElementById('id')` 全部引用（313 个）vs `index.html` 实际 id。
   - 任务卡片 `refs` 查询 vs 卡片模板 `data-*` 属性。

## 二、后端结论：干净，无合并断裂
- **0 个真实 500 内部错误**。所有路由对占位/异常输入均返回 400/404/422/403 或正常 200。
- 唯一非 2xx 但属**正常降级**：`/api/commentary/script-only` → 503「该实例未启用解说功能」（解说默认关闭）。
- 任务按钮端点验证：pause/resume/retry/delete/cancel-all 对不存在的任务干净返回 404，**无内部异常**。
- 确认 `DELETE /api/tasks/{id}` 后端调用 `store.request_cancel()` **真正中止**下载（非仅删记录）。

## 三、前端结论：全局绑定干净，无「绑定不存在元素」类 bug
- `el.X` 引用与 `el={}` 定义完全对应（cryptoSetMsg/cryptoUnlockMsg 经二次确认**已定义**，初判为误报）。
- 313 个 `$('id')` 引用中只有 5 个不在静态 HTML（`vdl-toast`/`vdl-confirm-overlay`/`bootErr`/`baiduAuthDialog`/`comScriptRetryLoad`），均为 **JS 运行时动态创建**，非 bug。
- 任务卡片 `refs` 与模板 `data-*` **一一对应**，所有按钮（暂停/继续/取消/删除/重试/在线观看/转换）均能正确绑定事件。

## 四、发现的真实问题（已修复）
| 问题 | 严重度 | 状态 |
|---|---|---|
| 任务卡片 `refs` 漏查 `data-convert-quota`：`updateConvertQuota()` 在**订阅墙开启**时访问 `refs.convertQuota` 会抛 `TypeError`，导致转换额度显示崩溃 | 潜在（仅订阅墙开时触发） | ✅ 已补 `convertQuota` 查询 |
| 上一轮为诊断「按钮没反应」临时加的 16 处调试 toast 残留 | 噪音 | ✅ 已全部移除 |

> 「部分按钮没反应」的真实根因是**上一轮已修复**的 `renderSlowWarning` 崩溃：它在 `paintTask` 中对每张卡片抛错，使整张卡片的事件绑定失败→所有按钮失效。移除该调用（`fdc382b`）后卡片正常渲染，故现报「目前没问题」。

## 五、已落地动作
- 修复代码写入 `web/app.js`，提交 `f7a4461` 并 push main。
- 热更新运行中的 `/Applications/VideoDownloader.app/Contents/Resources/web/app.js`（前端改动免重打包，重启 app 即生效）。
- 临时测试脚本已清理。

## 六、结论
合并收口后**后端与前端全局绑定均无遗留崩溃点**；唯一潜在 bug（订阅墙下的转换额度显示）已修复并清理调试残留。建议：从「访达→应用程序」完全退出旧进程后重开 app，即可获得干净版本。

---

## 七、三模块深入测试（解说 / 媒体库 / 字幕）

应要求对上一轮未深入跑的三个功能模块做同样的静态 + 接口核对。

### 测试方法补充
1. **全量 `data-*` 选择器交叉比对**：抽取 `app.js` 全部 `querySelector('[data-x]')` 与 `index.html` 的 `data-*` 比对（区分静态 HTML 与任务卡片 JS 模板里的动态属性）。
2. **前后端字段契约核对**：逐接口比对前端 `fetch`/`request` 的 body 字段与 `Form`/`JSON` 格式 vs 后端 Pydantic 模型 / `Form(...)` 参数。
3. **模块函数齐全性核验**：确认 `library_mod`/`subtitles_mod` 被路由调用的函数均已定义；解说桥接调用的 `commentary-pipeline/process.py` CLI 参数齐全。
4. **运行期 TestClient 冒烟**：library / subtitles / commentary 共 15 个端点，抓真实 500 / 422 / 未捕获异常。

### 媒体库（library）
- 4 个路由（`GET /api/library`、`/file/{id}`、`/thumb/{id}`、`DELETE /api/library/{id}`、`/encfile/{id}`）前端调用与后端一一对应。
- `library_mod` 全部被调函数（`scan_library` / `_load_sidecar` / `get_thumbnail` / `delete_item` / `_resolve_safe` / `encode_id`）均已定义。
- 冒烟：`GET /api/library` → 200（目录可扫），不存在 id → 404（无 500）。
- **结论：无合并残留 bug。**

### 字幕（subtitles）
- 4 个接口（`list` / `extract` / `burn` / `translate`）前端 body 字段**完全包含于**后端模型（`SubListRequest` / `SubExtractRequest` / `SubBurnRequest` / `SubTranslateRequest`），无 422 风险。
- `subtitles_mod` 全部被调函数（`list_online_subs` / `extract_online_sub` / `extract_embedded_subs` / `burn_subtitle` / `_write_subtitle_sidecar` / `translate_srt`）均已定义。
- 冒烟：4 接口对不存在 `lib_id` 干净返回 404（验证 `_resolve_lib_video` 路径安全校验生效），无 500。
- **已知限制（非合并 bug，环境相关）**：字幕烧录 `burn_subtitle` 依赖 ffmpeg 的 libass；本机 ffmpeg 无 libass 时烧录会失败返回 500（已在 memory 记录，属诚实降级），需真实媒体文件才能验证烧录效果。

### 解说（commentary）
- 17 个路由全部存在且前端调用均在列。关键交互契约核对：
  - `POST /api/commentary/script-only`：前端 body `{task_id|file_id, vertical, trim_start, trim_end, ...comGetOptions()}` ⊆ 后端 `CommentaryRequest`，字段完全对齐。
  - `PUT /api/commentary/script/{id}`：前端发 `{segments, voice}` 匹配 `ScriptUpdateRequest{title?, voice, segments}`。
  - `POST /api/commentary/render/{id}`、`/voice-preview`、`/preview/{id}`：前端均用 `FormData` 匹配后端 `Form(...)` 参数（**无 422**）。
- `_resolve_source()` 同时支持 `task_id`（走 `_require_task`）与 `file_id`（走 `library_mod._resolve_safe`），前端两来源都发，逻辑完整。
- 桥接调用的 `commentary-pipeline/process.py` 支持全部传入参数（`--auto` / `--edit-only` / `--script-only` / `--voice` / `--title` / `--vertical` 及剪辑选项），契约自洽。
- 冒烟：未启用（默认）→ 全部 503 正常降级；**0 真实异常**。
- **已知限制（非合并 bug）**：解说默认关闭（`VDL_COMMENTARY_ENABLED=false`），需配置 `COMMENTARY_DIR` 指向真实 pipeline 才可用；`process.py` 的 `--mode` choices 为 `["highlights","highlights_intro","full_web"]`（不含 "full"），当前前端不传 mode 故安全，若未来启用 mode 字段需注意对齐。

### 本轮结论
**三模块均未发现合并残留的真实 bug**：前后端字段契约完全对齐，模块函数齐全，运行期冒烟 0 异常。合并以 0809 为主保留了三模块的完整功能，无 0804 精简版造成的断裂。

## 八、云盘 / 保险箱 / 种子模块验证（2026-08-15 补充，按用户指定顺序）

> 方法升级：在 TestClient 中设 `VDL_LIBRARY_ENABLED=1`（解锁 crypto/archive/torrent 路由体），真正跑通 POST 路由体（而非仅触达 `_require_*` 守卫），专抓合并残留的 `NameError` / `AttributeError` / 422。

### 云盘（归档 archive + 存盘 cloud/save + 百度网盘 baidu）
- 前端覆盖完整：`/api/archive/config`(GET/POST)、`/api/archive/scan`、`/api/archive/run`、`/api/archive/status`、`/api/archive/cancel`、`/api/archive/forget`；`/api/cloud/save`、`/api/cloud/status`；百度 `auth_url`/`list`/`download`/`task/{tid}`/`share/list`/`share/download`/`token`。
- 后端全部路由存在，依赖齐全：`archive_store` / `archive_mod` / `library_mod.scan_library` / `_assert_archive_url`（SSRF 校验）/ `_archive_provider` / `cloud_executor` / `BAIDU_ENABLED` 均在。
- 冒烟（启用 library）：`archive/config`(GET/POST)→200、`scan`→200、`run`→200（无待归档 409）、`forget`→200、`cloud/providers`→200、`cloud/save`(占位 task_id)→404（任务不存在，预期）、`cloud/baidu/auth_url`→503（**未配置百度凭据，属预期降级，非 bug**）。**0 真实 500**。
- 前端契约：`startCloudSave` body `{task_id, provider, dest_path, webdav:{url,user,pass}, baidu:{token}}` 完全匹配 `CloudSaveRequest`；百度浏览/下载/分享均用正确的 query/body 形态。**无 422**。

### 加密保险箱（crypto_vault）
- 8 个路由全部存在，依赖齐全：`crypto_mod`（`new_vault`/`verify_passphrase`/`unlock_key`/`encrypt_file`/`decrypt_file`/`read_header` 均已定义）、`library_mod.{AUDIO_EXTS,IMAGE_EXTS,ENCRYPTED_EXT,_resolve_safe}`、`retention_mod.move_to_trash`、`VAULT_KEY`/`CRYPTO_LOCK`/`CRYPTO_EXECUTOR`。
- 冒烟：`crypto/status`→200、`set-pass`{passwd,confirm}→200（设完即解锁）、`encrypt`/`decrypt`{lib_ids}→200（后台线程跑，即时返回 job_id）、`lock`/`unlock`→200、`job/{id}`→200。`_run_crypto_job` 对不存在文件返回 job 内 errors 而不崩。**0 真实 500**。
- 前端契约：`set-pass`/{passwd,confirm}、`unlock`/{passwd}、`encrypt`/`decrypt`/{lib_ids} 与模型字段一致。**无 422**。
- 安全点：vault.json 只存 salt+verify，内存密钥 `VAULT_KEY`，锁定/重启即清空；解密成功才删 .vdlenc（与 memory 记录一致）。

### 种子下载（torrent / libtorrent）
- **关键事实：managed venv 中 `libtorrent` 实际可用**（`torrent_mod.available()==True`），故 `TORRENT_ENABLED` 为真、功能完整启用（桌面版同此路径）。
- 前端覆盖：`/api/torrents`(list)、`/api/torrents/add`、`/api/torrents/add-file`(FormData)、`/{tid}`(detail)、`/{tid}/pause|resume|remove`、`/{tid}/files`(优先级)。
- 后端路由体简单且自洽，依赖 `torrent_manager`（`get/pause/resume/remove/set_file_priorities`，`_require_torrent` 守卫）。
- 冒烟：真实添加 magnet → `add`→200（返回 `id`）、`detail`→200、`resume`→200、`pause`→200、`files`→404（沙盒无 peer 故无元数据，预期）、`remove`→200、不存在 tid→404。**0 真实 500**。
- 前端契约：卡片与所有操作均用后端返回的 `id` 字段（无 `task_id`/`id` 歧义）；`add-file` 发 `FormData{torrent, save_path}` 匹配后端 `UploadFile`+`Form(...)`。**无 422**。

### 关键守卫端点 `/api/nodes`（决定整块 UI 是否启用）
- 这是 archive/crypto/torrent/baidu/cloud 等界面「是否显示」的总开关。逐一核对：返回 `torrent.available` / `archive.enabled` / `archive.baidu_available` / `crypto.enabled` / `cloud.baidu_available` / `cloud.providers` 等**前端依赖的全部字段**，与前端 `loadConfig` 解构一一对应。
- **结论：合并未破坏该端点**——否则整块桌面功能会在合并后静默灰掉。当前端点完好，桌面 app 下各模块均会正确暴露。

### 本轮结论
**云盘 / 保险箱 / 种子三模块均未发现合并残留的真实 bug**：前后端字段契约对齐、模块函数齐全、运行期冒烟 0 异常、功能开关端点完好。合并以 0809 为主保留了三模块完整功能，无 0804 精简版造成的断裂。

## 九、最终结论
合并收口后**全局（下载主流程 + 解说 / 媒体库 / 字幕 + 云盘 / 保险箱 / 种子）后端与前端均无遗留崩溃点**。已修复的合并残留仅限下载主流程的前端未定义引用（cookieDetectBtn / selectedFormatId / renderSlowWarning / sources）与订阅墙下转换额度显示。建议：完全退出旧 app 后从「访达→应用程序」重开，获得干净版本；如需验证解说 / 字幕烧录 / 百度实际上传等需真实管线、媒体文件或外部凭据的功能，请在本地启用对应开关并准备素材后单独验证。
