# 项目接续快照 — video-downloader（VDL）2026-08-26 13:45

> 由 AI 生成，可直接复制到新会话让 AI 继续开发。两条支线独立：**默认 app 端（app-dev 分支）**，转 web 必须先询问。

---

## 1. 原始需求（项目总目标）

跨平台视频下载工具（**App 桌面端 + Web 端**）：
- 多平台视频解析下载（抖音/B站/腾讯/快手/爱奇艺/微视/1905/小红书/红果/微博等），跨平台 **Cookie 共享池**（含 VPS Playwright worker）
- 附带：视频解说（AI 解说管线 commentary-pipeline）、视频格式转换、去水印、订阅追更、种子、百度网盘
- 桌面 App 用 PyInstaller 打包（macOS .app/.dmg），Web 端部署 Railway（hanyuxz.top）
- **近期主线**：App 桌面端「视频解说」全链路打磨（命名/转写/响度/UI）+ 整体稳定性与健康维护

## 2. 已完成的全部工作成果

### 2.1 代码状态（三仓库全部 commit + push 同步）
- **app-dev**（`video-downloader-app` worktree，HEAD **`ea1f242`**，origin 同步）：
  - 解说链路：`3893891` title 流入成片名 → `42a9e61` 撤回输入框用文件名 → `f8a3738`+`6b78cee` ffprobe/短码兜底 → `052a57b` 上传前名字优先（`_meaningful_stem`）
  - 启动稳定性：`2b41605` 双击打不开根治 → `0e76b31` 彻底移除自动跳浏览器（铁律）→ `ea1f242` 启动等待 6s→20s
  - UI（08-26 上午一批）：`d2263d7` tab 逐字换行修复 → `313b864` 左侧垂直侧栏 → `de220ee` 优酷本地引擎下架 → `43da7f9~0a0dee5` 宽度三轮调整 → `b465ef3` 拆 main/.wrap → `fca1c69` 徽章显隐 → `41e1cd8` 留白 → `7d60bf9`/`e0939d9`/`db82da0` 去水印三修
  - 测试：`1c6bcf2` 修 voice_preview 3 失败；`ea1f242` 新增 work 清理 + 测试
- **commentary-pipeline**（`问问题/commentary-pipeline`，master，HEAD **`d780728`**，origin 同步）：
  - `c2bcedc` 成片命名 `<名字>-解说完成<YYYYMMDDHHMM>.mp4`（横竖屏无后缀）
  - `391dc09` VAD 放宽 + 0 段 fallback（根治转写稿为空）
  - `69e01df` LLM 多端点 failover；`b3b5be2` 旁白响度标准化（-14 LUFS）
  - `d780728` 测试断言同步 + requirements 补 Pillow
- **web-dev**（`video-downloader` 主仓库，HEAD **`b7b9820`**，已部署 Railway）：
  - `315782c` deepseek-v4-flash 预设（线上在跑）
  - `aa96184` 优酷残留清理 + 共享池工具提交（今天）
  - `b7b9820` 本接续快照文档提交（无功能改动）

### 2.2 关键功能与修复（实测验证）
- 成片命名：上传《少帅.mp4》→ `少帅-解说完成202608251917.mp4` ✓；upload.mp4 → v<hex> 短码 ✓
- VAD：0 段 → 31 段 fallback 救回（《两只老虎》实测）✓
- **解说 work 自动清理**：21GB → 5.5GB（启动清 54 个过期目录，保留 7 天可配 VDL_COMMENTARY_WORK_RETENTION_DAYS）✓
- 启动：新包 7-10s 就绪、双实例正常、不跳浏览器、20s 等待窗口 ✓
- 线上：Railway 部署 `aa96184` 确认；`pull-diag` 全绿（token/tunnel/vps/cookie_len=1067/cipher 全 true）✓
- 测试：**app 329 passed**（+4 新用例）、**pipeline 29 passed**，零失败零回归

### 2.3 交付物
- **最新 DMG**：`video-downloader-app/dist/VideoDownloader.dmg`（13:00 构建，指纹 `构建 ea1f242 (app) / d780728 (pipe) @ 08-26 12:58`）
- **/Applications 安装版已更新**为同一版本（ditto 覆盖 + 去 quarantine）
- 装机方式：Cmd+Q 退出 → 拖 DMG 内 .app 覆盖 /Applications（本次已由 AI 直接装好）

## 3. 当前进度 + 待办

### 当前状态：全部闭环，系统健康
- 08-26 巡检 5 项问题全部解决：安装版更新 ✓、work 清理 ✓、启动超时 ✓、web-dev 残留 ✓、MEMORY.md 过期 ✓（pull-diag 偶发超时已确认恢复）
- 三仓库全绿、线上健康、无未提交/未推送

### 待办清单（按优先级）
1. **（可选）用户体验验收**：用户可用 /Applications 新版跑一次完整解说流程（上传→转写→解说→成片）确认端到端
2. **（建议）web 端同步解说命名/UI**：web-dev 未同步 app 端近两天的解说命名规则与 UI 改动（pipeline 共享所以命名自动生效，但 web 前端 UI 侧栏/去水印等是 app 独有）；按铁律动 web 需用户明确要求
3. **（建议）VPS/cookie 池持续维护**：当前健康；VPS daemon systemd 守护，改 cookie 收集逻辑后 scp 自动拉起
4. **（用户已明确关闭，不要再做）**：剩余 5 平台实测、netease 网易云 MV 验证、桌面端优酷引擎本机实测、/download 直链替换、微信视频号下载（技术不可行）、共享池→weixin.txt 自动同步

## 4. 关键约束、修改要求、注意事项

### ⚠️ 铁律（用户明确，违反会被骂）
1. **app 端禁止任何「自动跳浏览器」行为**（用户原话：「app就是app打开网页干什么?我什么时候让你这么干了??」）。原生窗口失败只允许：写 `~/.vdl_launch.log` + 系统弹窗；唯一例外：百度网盘 OAuth 授权页（用户主动点登录才触发）
2. **默认做 app 端（app-dev）任务**；转 web 端（web-dev）必须先询问；两条支线独立绝不 merge，功能覆盖走按需 cherry-pick
3. **成片命名规则（最终版）**：`上传前的名字 + -解说完成 + YYYYMMDDHHMM + .mp4`；固定词「解说完成」；横竖屏统一不带后缀；界面文案「解说成片」保持不动
4. **给用户做"兜底方案"前必须问用户**；关键凭据绝不从截图 OCR；用户不执行命令行运维（AI 自运维：SSH/VPS/Railway 全自己来）
5. **优酷**：app 端本地引擎已下架（de220ee），web 端对应死代码已清理（aa96184）；粘贴优酷链接仍走 yt-dlp/公共池

### 工程约束
- **桌面构建必须在 `video-downloader-app` 目录（app-dev）跑**：`COMMENTARY_PIPELINE_DIR=/Users/suixindelang/WorkBuddy/问问题/commentary-pipeline bash desktop/build_mac.sh`（约 3 分钟）
- "改了代码但 app 没更新"根因=构建时间早于提交时间；改 server/web/pipeline 后必须重跑 build_mac.sh
- `commentary-pipeline` 是软链指向 `问问题/commentary-pipeline`（真实源码），不是 `commentary-worker`
- build 指纹已含双 SHA：`构建 <app> (app) / <pipe> (pipe) @ <时间>`（e7d1210 修复），/api/version 读 Resources/build_version.txt
- 每次关键修复要 commit（防工作区清空丢失）；改 pipeline 记得在 `问问题/commentary-pipeline` 里 commit+push；push web-dev 后 curl /api/version 轮询 git_sha 确认部署
- 测试环境依赖：Pillow 需装（清华镜像）；faster_whisper 在沙盒 venv

### 路径/位置速查
- 成片命名：pipeline `scripts/config.py` `final_output_name()`；app `server/routers/commentary.py` 两处 upload；`server/app.py` `_meaningful_stem`/`_probe_video_title`
- VAD：pipeline `scripts/transcribe.py` `_transcribe_whisper_faster()`（min_silence_duration_ms=1500 + 0 段 fallback + WHISPER_VAD=0）
- work 清理：app `server/app.py` `_purge_commentary_work()`（保留 7 天可配 VDL_COMMENTARY_WORK_RETENTION_DAYS）
- 启动等待：`desktop/desktop_launcher.py` 等就绪 100×0.2s=20s
- 启动日志：`~/.vdl_launch.log`；单实例锁：`~/.vdl_instance.lock`；解说 work：`~/.video-downloader/commentary_out/work/`
- 测试：`video-downloader-app/tests/`（329）、`问问题/commentary-pipeline/tests/`（29）
