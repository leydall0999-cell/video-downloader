# VDL 合并后系统核对报告（AI 去水印 / 订阅监控 / 时效清理 / 媒体库清理）

- **核对日期**：2026-08-15
- **核对对象**：`video-downloader`（合并后 HEAD `22919c6`，工作树 clean）
- **方法**：同历史核对 —— **TestClient 冒烟**（真实打各模块路由，断言状态码与响应骨架）+ **前后端字段契约交叉比对**（后端 JSON key 集合 vs `web/app.js` 实际读取字段）。
- **隔离说明**：外部重算力（yt-dlp 探查 / AI 去水印 worker / ffmpeg）一律打桩，只验证桥接、路由、状态机与字段契约，不依赖网络与二进制。
- **开关**：冒烟通过 `VDL_SUBSCRIPTIONS_ENABLED` / `VDL_LIBRARY_ENABLED`（覆盖 retention + process + library）/ `VDL_RETENTION_ENABLED` / `VDL_AI_DEWATERMARK_MODE=http` 打开四个模块。

## 总览

| 模块 | 路由冒烟 | 字段契约 | 结论 |
|---|---|---|---|
| 订阅监控 | ✅ 4 路由 PASS | ✅ 对齐 | 通过 |
| 时效清理 | ✅ 4 路由 PASS | ✅ 对齐 | 通过 |
| 媒体库清理 | ✅ 列表/删除/404 PASS | ✅ 对齐 | 通过 |
| AI 去水印 | ✅ 提交+状态机 PASS | ✅ 对齐 | 通过 |

> 复跑：`cd video-downloader && .build_venv/bin/python -m pytest tests/test_merged_modules_verify.py -q`

---

## 模块一：订阅监控（`/api/subscriptions*`）

**冒烟**
- `GET /api/subscriptions` → 200，`{subscriptions, enabled}`；`enabled=true`。
- `POST /api/subscriptions` → 200，返回 `to_public_dict()`，落库成功（桩：假平台 + `probe_channel=[]`，不触网）。
- `POST /api/subscriptions/{id}/check` → 200，`{sub_id, checked, new_videos, task_ids, failed}`。
- `DELETE /api/subscriptions/{id}` → 200 `{deleted:true}`；不存在 → 404（list/check 同理 404）。

**字段契约**
- 前端 card/modal 读取：`id, name, platform, last_checked, last_video_ids` —— 后端 `to_public_dict()` 全部提供 ✅
- 前端 check 读取：`new_videos, task_ids` —— 后端提供 ✅
- 后端多返回未用字段：`auto_check, cookie, created_at, proxy, quality_key, quality_label, url`（见下方「观察项 1」）。

## 模块二：时效清理（`/api/retention*`）

**冒烟**
- `GET /api/retention/config` → 200，`{config, labels, trash_available, usage}`。
- `POST /api/retention/config`（改 `interval_hours=12`）→ 200，落盘生效。
- `POST /api/retention/scan` → 200，dry-run 不删文件；`temp` 类命中 1 条；每档被归一为 `{count, size, items[:50]}`。
- `POST /api/retention/run`（`categories:["temp"]`）→ 200，`removed>=1`，文件被真实清理。

**字段契约**
- `CLEAN_LABELS`（前端）与后端 `CATEGORY_LABELS` 五类完全一致 ✅
- `DANGER_CATS=['media','quota']` 与后端 `need_trash={'media','quota'}` 语义一致 ✅
- `fillCleanForm` 读取的 12 个字段 ⊂ 后端 `RetentionConfig` ✅
- `collectCleanConfig` 发送的 12 个字段 == 后端 `RetentionConfigRequest` 接受集合（未发 `media_use_trash`，后端按「缺省不改动」处理，符合预期）✅
- `paintUsage` 读取 `usage.dir_size/disk_free/path` == 后端 `disk_usage()` 返回 ✅
- `scan` 返回 `labels` 前端未消费（前端用自带 `CLEAN_LABELS`，已对齐，非问题）。

## 模块三：媒体库清理（`/api/library*`）

**冒烟**
- `GET /api/library` → 200，`{items, total}`，新写入文件出现在列表。
- `DELETE /api/library/{id}` → 200 `{deleted:true}`；删除后列表不再出现该 id。
- 不存在 id → 404。

**字段契约**
- 前端 card/modal 读取：`id, title, name, platform, uploader, duration, size, mtime, kind, encrypted`（+ `ext, thumbnail, source_url` 备用）—— 后端 `scan_library()` 全部提供，无缺失 ✅
- 后端字段与前端读取完全一一对应，无冗余、无缺失。

## 模块四：AI 去水印（`ai_dewatermark`，process 管线 op）

**冒烟**
- `GET /api/nodes` → 200，含 `ai_dewatermark: {enabled, gpu}`（前端 `app.js:5430/5466` 据此显隐与 GPU 标签）。
- `POST /api/process/run`（`op=ai_dewatermark`，合法 `lib_id` + `params{x,y,w,h,band}`）→ 200，返回 `{job_id, status:"running"}`。
- `GET /api/process/{job_id}` 轮询 → 终态 `completed`（worker 桩已镜像真实收尾写入 `lib_id/name`）。

**字段契约**
- 前端 `pollProcess` 读取：`status, is_dir, count, name`；`renderProcQueue` 读取：`status, steps, name, lib_id, error` —— 后端 `process_status` 返回 `{status, error, lib_id, name, count, is_dir, steps, logs}` 全覆盖 ✅
- 前端入参 `PROCESS_OPS.ai_dewatermark.params`：`x, y, w, h, band` ⊂ 后端 `_run_ai_dewatermark` 接受集合 `{x,y,w,h,band,mode}` ✅
- 任务态额外字段（`out_path` 存于 job 但不经 `process_status` 返回）：前端用 `name` + `loadLibrary()` 刷新定位新文件，不依赖 `out_path`，已确认不阻断 ✅

---

## 跨模块一致性结论

四个模块路由全部可通、状态机正确、前后端字段契约**完全对齐，无阻断性不匹配**。

## 观察项（非阻断，建议后续优化）

1. **订阅列表回传敏感字段（低）**：`GET /api/subscriptions` 的 `to_public_dict()` 把 `cookie`、`proxy` 一并回传前端。本地桌面场景 JSON 不外泄，前端也未渲染，无实际泄露；但若未来走远程/网页版，建议在列表接口对 `cookie/proxy` 做脱敏（仅返回占位或长度）。
2. **AI 去水印开关未前端门控（低）**：后端 `/api/nodes` 已正确返回 `ai_dewatermark.enabled`，但前端 `resetProcessPanel` 仅按 `kind` 过滤、`PROCESS_OPS.ai_dewatermark` 始终出现在加工下拉框（仅按 `.gpu` 改标签）。未配置 worker（`AI_DEWATERMARK_ENABLED=False`）的部署下该选项仍可选，提交后才会失败。建议：前端按 `node.ai_dewatermark.enabled` 过滤该 op，或在 `process_run` 对 `ai_dewatermark` 校验 `AI_DEWATERMARK_ENABLED` 提前 4xx。

## 交付物

- 核对测试套件：`tests/test_merged_modules_verify.py`（4 模块 × 冒烟+契约，全部 PASS）
- 本报告：`合并后系统核对报告.md`
