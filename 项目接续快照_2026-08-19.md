# video-downloader 项目接续快照

> 生成时间：2026-08-20 01:30 (GMT+8) ｜ 当前分支：`web-dev`（HEAD=`1076b1d`，**未 push**，origin 停在 `44989ae`）｜ 线上版本：`hanyuxz.top/api/version` git_sha=`44989aef`（最新功能已全部部署）
> 用途：让任何人（包括未来的自己）能快速接手本项目当前状态，无需翻完整聊天记录。

---

## 一、原始需求

- **项目**：自托管「视频下载 / 媒体备份」Web 工具（**FastAPI + yt-dlp + 原生前端**），域名 `hanyuxz.top`（Railway 海外 ams 节点，经国内 VPS `8.138.223.3` 的 18888 代理回源）。
- **本期主线（8/18–8/20）**：① 治好 B站/抖音/快手/微博等平台的下载（各平台反爬升级）；② 公共 Cookie 池（网页版免手动贴 Cookie）；③ 歌单/专辑整批下载。
- **增值方向**：视频解说自动成片（commentary，代码已落地、默认关闭 `VDL_COMMENTARY_ENABLED=false`）。
- **合规红线**：只下载自己拥有/已授权内容；**不破解付费墙/DRM**（会员内容明确标注并跳过）。

---

## 二、已完成工作成果（全部线上可用）

### 平台下载（8 家 + 整批）
| 平台 | 方案 | 状态 |
|---|---|---|
| **bilibili** | **B站 页面 412 风控（2026-08 升级，urllib3/requests 非浏览器 TLS 栈被拒）→ probe/下载阶段自动走 `_bilibili_api_extract` API 兜底**（view+playurl，带 pool cookie；requested_formats 模拟已选流；数值字段 _to_int） | ✅ 解析+下载闭环（线上 best=480P 为数据中心 IP 限制，非 bug） |
| 小红书 | yt-dlp 原生 | ✅ |
| **抖音** | VPS Playwright `douyin_resolve.py`（游客态拦截真实音视频轨 URL，~26s；音视频分离需 ffmpeg 合并） | ✅ |
| **快手** | VPS Playwright `kuaishou_resolve.py`（合并 mp4 单文件，6.4s；标题剥离话题标签+作者+封面） | ✅ |
| **微博** | VPS Playwright `weibo_resolve.py`（过 Sina Visitor System；CDN 需 Referer） | ✅ |
| **网易云** | yt-dlp 原生（部分歌受 IDC IP 风控，住宅代理才可绕） | ✅ |
| **喜马拉雅** | yt-dlp 原生（单集）；专辑走 VPS Playwright `ximalaya_album_resolve.py`（getTracksList 分页，234 集 12s） | ✅ |
| **歌单/专辑批量** | `/api/playlist` 返回曲目列表 + 前端列表渲染 + 单曲下载 + 批量下载（付费项跳过） | ✅ |

### 架构（复用泛化）
- **VPS daemon**（`vdl-cookie.service`，18731 端口）：`/v1/resolve?platform=<douyin|kuaishou|weibo|ximalaya_album>` 通用端点（`_RESOLVERS` 注册即用）+ `/v1/push` 秒级补推 + 120s 定时推送；Playwright 全局串行锁。
- **Railway `_call_vps_worker(platform, url)`**：新增平台只需写 `_xxx_info()` + `_is_xxx_host()` + probe/_run_once 两处分支。
- **公共 Cookie 池**：`server/cookie_pool.py`（白名单/限频/验真/Fernet 加密/原子写），持久卷 `/data`；`/api/cookie/dump`（**临时 debug 端点，排查 B站 用，可后续收敛**）。
- **B站 API 兜底**：`_bilibili_api_extract`（页面 412/403/网络错误时自动切 API）+ `_rebuild_requested_formats`（按清晰度重建选流）。

### 前端
- 歌单/专辑模式（`isPlaylistUrl` 识别含 hash 的网易云链接）+ 列表渲染 + 单曲下载 + 批量下载（150ms 间隔、付费跳过、失败标红）。
- 短链归一化 `_normalize_url`（抖音 `/Vlp/` 分享尾巴清理、快手 f/ 链接、t.cn）。

### 安全/稳定性
- Token 轮换（`VDL_COOKIE_SYNC_TOKEN` / `VDL_COOKIE_REFILL_TOKEN` 已换新）；废弃 8888 开放代理已停用；cn_proxy `_tunnel` 背压 + EOF flush 修复（大文件下载稳定）。

---

## 三、当前进度与待办

**现状**：8 家平台 + 歌单整批全部可用；B站 修复 6 commits 全部上线验证通过。

**待办清单（按优先级）**
1. **push `1076b1d`（web-dev merge main 空合并，记录汇合点）**——当前本地领先 origin 1 commit，下次 push 功能时会带上去；或现在手动 push（会触发 Railway 空构建，无害）。
2. **main ← web-dev 反向合并（风险高，需仔细）**：web-dev 是精简版（app.py 只 include 6 router：core/convert/dewatermark/crypto/fs/cloud），main 是全功能版（17 router）。反向合并需沿用 app-dev→main 经验（保留 main 全量 include_router + 补 web-dev 增强），白天/用户明确要求时做。
3. **`/api/cookie/dump` debug 端点收敛/移除**（排查 B站 用，功能完成可删）。
4. **可选优化**：抖音解析 26s 提速（移动端页面/常驻浏览器）；抖音加作者/封面；VPS 配住宅代理提升 B站 画质（当前数据中心 IP 限 480P）。
5. **暂缓平台**（各有硬约束）：腾讯（cover 合集+会员 DRM 不可下）、西瓜（网页版内容大量失效）、知乎（页面复杂+内容墙）、爱优芒（DRM）。

---

## 四、关键约束与注意事项

- **分支**：默认只在 `web-dev` 开发；切 app-dev/main 需用户明确指示。桌面构建在 `video-downloader-app` worktree（app-dev 分支）。
- **合规**：不破解付费墙/DRM；会员内容标「会员」并禁用下载；隐私上不静默收集共享第三方凭证（访客必须 opt-in）。
- **版本核对**：以 `/api/version` 的 `git_sha` 为准，**勿用 Railway 部署卡片 hex**（那是部署 ID）。
- **Railway 部署**：Redeploy = 用相同代码重建（不拉新 commit）；正确方式 = Settings→Source 切分支（web-dev）触发新构建，或 Deploy 按钮。
- **Railway 改 Variables 坑**：部署 ID 变了 ≠ env 已更新；用「新 token+非白名单域名」探针验证（403=未生效，400=已生效）。
- **VPS 连接**：`env -u http_proxy ssh root@8.138.223.3`（沙盒 SSH 走 22 端口，勿经沙盒代理）。VPS daemon/脚本在 `/opt/vdl-worker/`，cn_proxy 在 `/opt/vdl-proxy/`。
- **凭据**：绝不从截图 OCR 提取凭据；token 值不再明文外传（在 VPS `.cookie_sync.env` + Railway Variables）。
- **共享重资源必加锁**：定时循环 + HTTP 按需触发共用 Playwright 必须串行化（`_push_lock`），否则并发打挂 VPS。
- **B站 调试方法论（reusable）**：平台解析失败排查顺序——版本核对 → diag（cookie_source/proxy_used）→ 外部 curl 逐段隔离（隧道/代理/cookie/TLS）→ **本地跑项目代码完整复现**（本地=海外出口+18888 隧道+pool cookie，与 Railway 等价）→ 定位差异对症修。curl 全通、yt-dlp/requests 全挂 = TLS 栈风控特征。
- **yt-dlp 构造 info dict 的坑（reusable）**：自定义 info 需 `url`/`requested_formats`（process_info 直接吃 info 不经 process_ie_result）；音视频分离走「requested_formats + 不设顶层 url + format 级 protocol」避免 HttpFD 合并分支丢音频；数值字段必须 int（B站 API 返回字符串 duration）。
- **记忆目录**：`.workbuddy/memory/` 刻意不入库；`.workbuddy` 文件夹非缓存勿删。
