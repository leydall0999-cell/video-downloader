# VDL 项目完整接续快照（2026-08-23 21:18）

> 复制本文件到新会话，AI 可直接接着开发。当前分支 `web-dev`（网页版精简分支）。

---

## 1. 当前项目原始需求

VDL（Video Downloader）自托管视频下载/媒体备份 Web 工具，FastAPI + yt-dlp + 原生前端。三分支发布模型：`main`（全功能）、`web-dev`（轻量 Web 版，仅下载/转码/去水印/加密/fs/cloud 6 路由）、`app-dev`（桌面端打包）。

**本次新增核心需求（用户原话）**：
> "我需要 app 及网页端能够互相作用，把信息推到云端共享池，你想办法直接帮我解决问题就行，过程不需要问我，解决完再告诉我"

即：**桌面 App 与网页端能互相把登录态 Cookie 推到同一个云端共享池，解析时两端都能自动复用，不需手动粘贴。**

---

## 2. 已完成的全部工作成果

### A. 去水印 UI 全套打磨（今日早些，已 commit，线上 HEAD=37f54bb）
- 加选区重叠保留并集、减选区挖洞（SVG mask）
- 选区坐标偏移修复、严格限制在图片内、超细绿虚线（加/减同色）
- 灯箱弹窗收窄 760px 居中、图片居中、放大后鼠标长按拖动平移（✋移动工具）
- 页面整体收窄 720px 居中

### B. Cookie 双向共享池改造（**进行中，代码已改未 commit**）
根因：优酷 Cookie 在 `/api/cookie/sync`(app.py:3225) 与 `/api/cookie/contribute`(app.py:3260) 入池前被 `verify_cookie` 拒——优酷走 `_verify_generic` 用 yt-dlp 试解析，yt-dlp 优酷提取器对会员内容报 -3007→返回 False→拒收。App 推优酷、网页贡献优酷双双失败（用户此前见「云端同步出错」即此）。

已改代码（未提交）：
1. **`server/cookie_pool.py`**
   - 新增 `_STRUCTURAL_FIELDS`：`youku.com→{P__yk__uck,yktk,cna,x5sec,sess_vkey,unb}`；`v.qq.com/qq.com→{vus_session,video_platform,uid_tt,login_ecookie}`
   - 新增 `_structural_check(domain, header)`：命中白名单站关键登录态字段即返回 True（结构有效放行），未命中返回 None（降级走通用验真）
   - 改 `verify_cookie`：chrqj 专属验真；其余先 `_structural_check`（True 即放行），未命中再 `_verify_generic`；**口径统一「仅明确 False 才拒，None 放行」**（与 `verify_and_prune` 第 391 行对 None 保留一致）
2. **`server/app.py`**
   - `/api/cookie/sync`(3225) 与 `/api/cookie/contribute`(3260) 拒绝文案细化（提示优酷需含 P__yk__uck 等字段），逻辑保持 `ok is False` 才拒（优酷现在返回 True/None，不再被拒）
3. **`web/app.js`**（`contributeCookie` 约 3133）
   - 改为 fetch + 状态分流：200→toast「已贡献到共享登录态池，网页端解析将自动复用」；400→验真未过；429→限频；其他→同步异常。不再静默 catch

### C. 已验证（本地 TestClient，脚本 verify_youku_pool.py）
- 优酷结构 Cookie POST /api/cookie/contribute → **200 ok added=True**（根因修复确认）✓
- 立即二次贡献 → 429 限频生效 ✓
- get_cookie('youku.com')→HIT，池文件落盘 1 条 source=contrib ✓

---

## 3. 当前进度 + 未完成待办

**进行到**：Cookie 改造代码已写完、本地验证 1-4 项通过；第 5 项「downloader._base_options 对优酷 URL 自动注入池 Cookie」验证结果 = NO（池里有 Cookie 但未注入），**根因未查清**。

**待办清单**：
- [ ] **查清 [5] 为什么 NO**：池里有 youku.com Cookie，但 `downloader._base_options("https://v.youku.com/...", cookie="", proxy="")` 未注入。可能：host 提取、`cookie_host` 回退（`v.youku.com`→`youku.com` 经 `_candidates` 应命中）、或 `_base_options` 早期分支提前 return。查 downloader.py:1465-1522 实际走向。
- [ ] 确认网页端解析优酷真正自动注入池 Cookie（用户不手动粘贴即生效）
- [ ] commit 改动（建议 message：`fix(cookie): 优酷等站结构校验放行入池，打通 App↔网页端共享登录态`）
- [ ] push web-dev，轮询 `https://hanyuxz.top/api/version` 的 `git_sha` 确认 Railway 自动部署生效
- [ ] curl `/api/cookie/pull-diag` 线上自查无回归
- [ ] 用真实优酷 Cookie（App「同步 Cookie 到云端」或网页端贡献）在线上验证 -3007 是否消失
- [ ] 清理临时验证脚本 `verify_youku_pool.py`

**更早记下的长期待办（与本次无关，勿混）**：会员制限流放开、CF Pro 升级、App 版完美拼接、百度网盘端点。

---

## 4. 关键约束 / 修改要求 / 注意事项

### 用户工作方式铁律（务必遵守）
- **用户不执行命令行运维**（SSH/Railway 后台/手动部署）。AI 自己完成：VPS 改文件/重启、push 后轮询 `/api/version` 的 `git_sha` 确认部署、线上 curl 自查。
- **只在 web-dev 分支开发**（除非用户明确要求 app-dev 桌面打包）。
- **关键凭据绝不从截图 OCR**（AppKey/Token/密码等须用户手动粘贴文本）。

### 部署方法论
- Railway 绑定 GitHub web-dev，push 即自动部署，**无需用户 Redeploy、不要问"部署好了吗"**。
- 唯一权威验证：`https://hanyuxz.top/api/version` 的 `git_sha` == 刚 push 的 commit。
- 线上验证一律 curl 自查，不让用户去页面看。

### Cookie 池安全边界（本次改造不可破坏）
- 仅白名单域（chrqj/bilibili/youku + hardened 派生 + env VDL_COOKIE_POOL_DOMAINS），不扩大。
- 入池前仍：白名单校验 + 限频 + 验真/结构校验 + 加密存储（VDL_COOKIE_ENC_KEY，未配降级 600 明文）+ TTL 30 天 + 后台 verify_and_prune 明确无效才剔。
- 存储仍双份物理隔离：Railway 持久卷 `RAILWAY_VOLUME_MOUNT_PATH/cookie_pool` vs App 本机 `~/.videodownloader/cookie_pool`。App→Railway 单向推送（`sync/to-cloud`→`/api/cookie/sync`）维持；网页→App 反向非重点（App 本机浏览器优先）。

### 代码位置速查
- 公共池：`server/cookie_pool.py`（`_BASE_DOMAINS` 白名单、`verify_cookie`、`_structural_check` 新增、`get_cookie`、`add_cookie`）
- 端点：`server/app.py` 的 `/api/cookie/sync`(3208)、`/api/cookie/contribute`(3232)、`/api/cookie/sync/to-cloud`(3331)、`/api/cookie/pull-diag`(3120)
- 自动取池：`server/downloader.py:1482-1502`（用户未贴 Cookie 时 get_cookie 注入）
- App 推送前端：`web/js/desktop-app.js:77` `syncToCloud` → POST `/api/cookie/sync/to-cloud`
- 网页贡献前端：`web/app.js:3133` `contributeCookie`
- 强反爬域清单：`server/downloader.py:1201` `_COOKIE_HARDENED_DOMAINS`（含 youku.com/v.qq.com）

### 环境
- 运行时：managed Python `/Users/suixindelang/.workbuddy/binaries/python/envs/default/bin/python`（已装 fastapi/httpx/requests/yt-dlp）
- 本地验证需 `sys.path.insert(0, ".../server")` 后裸导入 `import cookie_pool, app, downloader`（app.py 内 `import platform_model` 是裸导入，必须在 server/ 上下文）
- TestClient 用法：`TestClient(app.app)`（FastAPI 实例在 app.py 变量名 `app`）
- 隔离池目录：设 `VDL_COOKIE_POOL_DIR` 指向临时目录避免污染

---

## 5. 直接可执行的下一步命令（接手用）

```bash
cd /Users/suixindelang/WorkBuddy/video-downloader
git branch --show-current   # 确认在 web-dev
# 查清 _base_options 为何未注入 youku Cookie（读 server/downloader.py:1465-1522）
# 修复后：
git add server/cookie_pool.py server/app.py web/app.js
git commit -m "fix(cookie): 优酷等站结构校验放行入池，打通 App↔网页端共享登录态"
git push origin web-dev
# 轮询部署
for i in $(seq 1 20); do sleep 10; sha=$(curl -s https://hanyuxz.top/api/version | python3 -c "import sys,json;print(json.load(sys.stdin).get('git_sha',''))"); echo "$sha"; done
# 比对 sha 是否等于刚 push 的 commit
```
