# 香港节点切换手册（阿里云/腾讯云香港轻量 替代 Railway）

> 2026-09-11 备货预案。触发条件：阿里香港轻量 2核2G 补货（或改买腾讯锐驰型）。
> 目标：`hanyuxz.top` 从已死的 Railway 切到香港机，大陆用户 220ms → 30-50ms，egress 费归零，免备案不变。

## 0. 购置参数（哪个有货买哪个）

| 项 | 阿里云轻量（首选） | 腾讯云轻量（备选） |
|---|---|---|
| 入口 | `https://swasnext.console.aliyun.com/buy?regionId=cn-hongkong` | `https://cloud.tencent.com/product/lighthouse` |
| 地域 | 中国香港 | 中国香港 |
| 规格族 | **通用型**（网站搭建/Web应用/音视频服务端）；备选 **容量型**。**禁选「国际型」** | 无规格族之分 |
| 套餐 | **通用型** 2核2G / 40G SSD / 200M峰值 不限流量（**≈¥56-58/月**）；容量型 2核2G/60G ≈¥66/月 | 锐驰型 2核2G 200Mbps（¥40-55/月）；入门型 20M/500G 勉强够冷启动 |
| 镜像 | Ubuntu 22.04 系统镜像（纯净，勿选宝塔/Docker 应用镜像） | 同左 |
| 时长 | 1 个月试水 | 1 个月 |

### 真实价目（2026-09-11 下单页实测；**别信旧促销价**）

| 档位 | 配置 | 价格 | 结论 |
|---|---|---|---|
| 2核 **1G** | 30G / 200M 起 不限流量 | **¥34/月** | ⚠️ 可用但勉强：`deploy_hk.sh` 会为它自动建 **4G swap**，构建和转码走 swap（明显变慢），并发下载余量小、有被 OOM 杀掉的风险 |
| 2核 **2G（推荐）** | 40G / 200M 不限流量 | **≈¥56-58/月** | 本手册原定配置，一次到位 |
| 2核 2G 容量型 | 60G / 200M 不限流量 | **¥66/月** | 同上网，多花 ~¥8 买用不到的 20G 系统盘 |

> ⚠️ 手册早期写的「2核2G ¥24-39/月」是 2025 促销期的旧价，**现在买不到**。
> 下单价里「30 多」那一档＝ **2核1G**，不是 2G；2核2G 从来没出现过 30 多的价。

### ⚠️ 下单页最容易选错的 5 项（2026-09-11 实测踩点）

阿里云轻量的下单页**默认落点不是香港**，且默认套餐是 1G 内存档；不逐项核对很容易买错。
逐项对照下表再点「立即购买」：

| 下单页字段 | ❌ 默认/易错值 | ✅ 必须选 | 选错的后果 |
|---|---|---|---|
| **地域** | 新加坡（默认） | **中国香港** | 全盘计划作废：手册/`deploy_hk.sh`/CF 切域名都按香港写；大陆用户延迟也更差 |
| **规格族**（顶部 tab） | 容量型（存储向）；**尤其别碰「国际型」** | **通用型**（网站搭建/Web应用/音视频服务端） | 「国际型」官方原文写的是 BGP（**非中国优化**）线路 —— *"服务中国内地用户有较高的公网时延与丢包率，仅适用于不需要对中国内地用户提供服务的应用场景"*。我们服务对象以内地用户为主，选它＝自我降级。容量型能用（同为普通 BGP），只是系统盘更大、每档贵 ¥10 |
| **套餐** | 2 vCPU / **1 GB** / 30 GB（¥34 档） | **通用型 2 vCPU / 2 GB / 40 GB**（≈¥56-58/月）；容量型 2核2G/60G（¥66/月）亦可 | 1G 跑 Docker 构建（含 bgutil PO token 编译）极易 OOM，运行期 yt-dlp+ffmpeg 更悬；脚本里专门建 2G swap 就是为 2G 内存机准备的 |
| **镜像** | Alibaba Cloud Linux 3.x | **Ubuntu 22.04（系统镜像）**；下拉只有 24.04 时也可用（swap / get.docker.com / cloudsmith Caddy 源在 noble 上均正常） | `deploy_hk.sh` 第 7 步装 Caddy/自动 HTTPS 走的是 `apt-get`；Alibaba Cloud Linux 是 `dnf`，该步直接失败（主部署步骤虽不挑系统，但 HTTPS 会缺） |
| 镜像类型 | — | 系统镜像（**勿选**宝塔/Docker 等应用镜像） | 应用镜像会预占端口、带无关服务，与 Docker 方案冲突 |
| 数量 / 时长 | — | 1 台 / **1 个月**（可勾自动续费） | 香港常断货，不勾自动续费下月可能补不回货 |

> ⚠️ **2026-09-12 实测教训（重要）**：阿里云轻量的香港低价档走的是**限时秒杀**——库存有限、
> 有购买数量限制、多为新用户专享，0 点后很快售罄。**别在下单页现场研究规格**，先把「买哪个」定死。
> 秒杀档售罄时，**同一页面往下通常还有「常规特惠档」可买**（贵一些，但不用抢）——先翻一遍再下结论。
> 若香港当晚确实无货：
> ① **腾讯云香港轻量「锐驰型」2核2G / 200Mbps / 40G / 无限流量 ≈¥55/月** —— 手册备选列，
> 属**常规在售、无需抢购**的档位（门槛是需开通腾讯云实名认证）；
> ② 或次日晚 0 点再来（先把配置抄下来，到点只点「立即购买」）。
> **不要**为了「有货」降级买 1G 档，也不要买「国际型」。

### 🎯 抢购操作卡（23:50 做好，0:00 只点一下）

**23:50 之前先把这些做完**（占掉所有需要思考的时间）：

1. 登录阿里云，确认**实名认证已通过**（未实名连下单页都进不去）；
2. 打开 `https://swasnext.console.aliyun.com/buy?regionId=cn-hongkong`；
3. 按下面卡片把**每一个下拉都选好**，停在页面底部，**不要点**「立即购买」：

```
地域        中国香港
规格族      通用型            ← 不要 国际型 / 不要把「容量型」当默认
套餐        2 vCPU / 2 GB / 40 GB   （容量型 2核2G/60G 也行）
镜像        系统镜像 → Ubuntu（22.04 首选，只有 24.04 也可）
数量/时长    1 台 / 1 个月         （勾「自动续费」，香港常断货）
```

**0:00 到了之后**：刷新页面 → 重新确认「地域 = 中国香港」没被重置 → 点「立即购买」→ 付款。**不要在下单页研究任何东西。**

**若点进去显示售罄**：
- 先**往下翻**找「常规特惠档」（不是秒杀，通常有货，贵一点）；
- 还没有 → 立即改走**腾讯云香港轻量「锐驰型」2核2G ≈¥55/月**（常规在售，不用抢；需先开通腾讯云实名）；
- 都不行 → 记下当晚情况，次日 23:50 再来（配置已定死，第二次只需 5 秒）。

**买到之后**：把 IP 发给我，我跑 `bash desktop/deploy_hk.sh root@<IP>` 一条命令部署 + 冒烟。

## 1. 部署（一条命令，约 10-15 分钟）

```bash
ssh-copy-id root@<IP>                      # 首次：输密码装公钥
bash desktop/deploy_hk.sh root@<IP>        # 部署 + 冒烟
# 验证: curl http://<IP>:8888/api/version
```

脚本做的事：swap 2G → `git archive web-dev` 传源码（不受本地 app-dev 工作树影响）→
Docker 构建（**含 bgutil PO token server**，香港数据中心 IP 访问 YouTube 必需）→
`docker run -e VDL_REGION=global -p 8888:8888 --restart unless-stopped`。

## 2. 域名切换（需你在 CF dashboard 操作，约 2 分钟）

1. Cloudflare → `hanyuxz.top` → DNS → A 记录 `@` 改为 **香港机 IP**；
2. **代理状态改为「仅 DNS」灰云**——关键！橙云会让大陆用户仍绕 CF 海外边缘（220ms），提速归零；
3. 然后跑 `bash desktop/deploy_hk.sh root@<IP> hanyuxz.top` 装 Caddy（自动签 TLS）；
4. 防火墙/轻量控制台放行 **80、443、8888、22**。

## 3. 代码侧改动（切域名当天，web-dev 分支）

- `web/app.js` 里 `UC_UPLOAD_ENDPOINTS = [location.origin, 'https://web-production-b9993.up.railway.app']`
  ——删掉 railway 兜底（或换成香港机域名），commit 后**两个节点都要重新部署**（广州跑 `run.sh` 的照常 rsync）；
- 广州节点 `/opt/vdl-worker` 同步最新 web-dev（保持 `VDL_REGION=cn` 不变）。

## 4. 验收清单

- [ ] `dig +short hanyuxz.top` → 香港机 IP（无 CF 段 IP = 灰云生效）
- [ ] 本机 `curl -w "%{time_connect}"` → TCP 连接 **<0.08s**（旧基线 0.17-0.22s）
- [ ] `https://hanyuxz.top/api/version` 200；首页/app.js 正常
- [ ] **真实下载一条 YouTube 链接**（验证 bgutil PO token 生效，无 "Sign in to confirm you're not a bot"）
- [ ] 晚高峰 21:00 复测延迟/丢包/下载吞吐（阿里国际 BGP 的赌点就在这）
- [ ] `/api/nodes` 两节点各报 `region=global` / `region=cn`

## 5. 收尾与回滚

- **Railway 退役**：不再续费即可（应用已删，无残留依赖——`UC_UPLOAD_ENDPOINTS` 改完后）；
- `cn_tunnel_client` 保持 `disabled`（回 Railway 时才 `systemctl enable --now`）；
- 广州机上之前装的 `cloudflared`（CF Tunnel 备用方案，已作废）：可留可卸，`/usr/local/bin/cloudflared` 删除即净；
- **回滚**：CF DNS A 记录改回/改指广州 `8.138.223.3`，入口退化为 `http://IP:8888`（CF 不代理非标端口，橙云方案对 8888 不可用）。

## 6. 已知风险

- 阿里香港轻量走的是**普通 BGP**（非 CN2 GIA）。注意与「国际型」的 *BGP 非中国优化* 不同 —— 后者官方明示内地高时延/丢包，手册已禁选；香港普通 BGP 的**晚高峰质量仍待实测**（验收第 6 条），这正是先买 1 个月试水、不直接包年的原因；
- 2核2G 并发转码能力有限，不够再升 2核4G；
- 版权投诉会到云厂商（香港机房同样受理），与 Railway 无本质差异。
